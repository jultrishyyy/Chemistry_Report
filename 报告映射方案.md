# 报告字段映射方案（待实现）

> 解决"报告模板的取值映射"与"录入数据动态变化（增删样品/项目）"之间的矛盾。
> 本文件是设计方案，尚未实现。落地后把要点并入 README 第六节，本文件转入 OPTIMIZATION.md 归档。

---

## 一、问题

### 现状（绑定机制）

- 报告模板（project）通过 `linked_record_template_id` 关联一个原始记录模板。
- 报告里"检测结果表"`report_result_table` 是一张**固定行列的网格**：`{columns[], rows[], cells:[{rowId,colId,binding}]}`。
- 每个单元格的取值 `CellBinding` 是**按位置写死**的，典型是 `record_cell{matrix_code, sample_idx, param_code}`——即"取矩阵 M 第 `sample_idx` 个样品的 `param_code` 列"。
- 这套映射在**报告模板设计期**配好（`ReportResultTableCanvas` 画布里逐格绑定）。

### 矛盾

原始记录的 `data_matrix` 配了 `allow_add_remove_samples: true`——**录入期样品数是动态的**。于是：

| 场景 | 后果 |
|---|---|
| 录入时**新增**样品（模板假设 3 个，实际录了 5 个） | 报告结果表只有 3 行写死的 binding，第 4、5 个样品**根本不进报告** |
| 录入时**减少**样品（实际只录 2 个） | 第 3 行的 `sample_idx=2` binding 解析为空（`—`），报告里出现空行 |
| 样品**顺序/编号**变化 | 按 index 绑定全部错位 |

**根因**：报告结果表是"静态网格 + 按索引绑定"，而数据源是"动态矩阵"。两者抽象层级不匹配。

### 用户的几个问题，对应回答

1. **数据和原始记录模板不一样了，报告模板的映射怎么办？** → 不能再靠"逐格按索引绑定"。改为"表 ← 矩阵"的**结构级动态绑定**，渲染时按实际数据展开。
2. **新增样品如何直接进报告？** → 动态绑定下**自动进**（渲染读实际录入的矩阵，有几个样品出几行）；特殊情况再用报告实例编辑器（P2）手动补。
3. **配置映射的时间不对吗？** → **时间对、粒度错**。结构映射就该在报告模板设计期定（工程师一次配好），但要配在**矩阵级**而不是**单元格级**；剩余的个案差异交给"报告实例编辑期"。
4. **在哪配置比较好？** → 见第三节"三层职责"。
5. **数据录入出错怎么办？** → 见第四节。

---

## 二、方案核心：结果表从"静态网格"升级为"矩阵驱动的动态表"

给 `report_result_table` 增加一种**动态模式**（与现有静态模式并存）：

```
result_table.source = {
  mode: 'matrix',                 // 'matrix' = 动态 | 'fixed'(默认) = 现有逐格 binding
  matrix_code: 'burn_matrix',     // 绑定原始记录里的哪张数据矩阵
  row_per: 'sample',              // 每个样品一行（未来可扩展 'parameter'）
  columns: [                      // 选哪些参数列进报告 + 列头/单位
    { param_code: 'sample_label', label: '试样' },
    { param_code: 'burn_rate',    label: '燃烧速率', unit: 'mm/min' },
    ...
  ],
  conclusion: {                   // 结论列（替代现在按 is_conclusion 行/列的写死逻辑）
    from: 'summary_col' | 'param' | 'rule',
    ...
  }
}
```

**渲染时**（`renderReportResultTableTypst`）：
- 读取该报告锁定版本对应的 `record_data` 里 `matrix_code` 的实际值（已 flatten 的 `record_flat_data`）。
- 有几个样品就生成几行，列按 `columns` 取——**新增样品自动出现、减少样品自动消失、顺序跟随录入**。
- 汇总行/汇总列同理从矩阵的 summary 动态取。

**配置时**（`ReportResultTableCanvas` 编辑器）：
- 工程师选"动态模式"→ 选关联矩阵 → 勾选要进报告的参数列 → 指定结论列。**不再逐格点绑定**。
- 静态模式保留：用于确实需要固定版式的表（如不随样品变的指标表）。

> 普通字段（`record_field`/`record_meta`/`order`）的绑定不受影响——它们本来就是"按 code 取值"，与样品数无关。受影响的只有"镜像矩阵"的结果表。

---

## 三、配置映射放在哪、何时配（三层职责）

| 层 | 谁 | 何时 | 配什么 |
|---|---|---|---|
| **结构映射（模板期）** | 工程师 | 报告模板设计 | "结果表 ← 哪张矩阵、哪些参数列、结论怎么判"（矩阵级动态，一次配好，适配任意样品数）|
| **实例微调（报告期）** | 文员 | 报告生成后编辑 | 个案差异：手动加一行说明、改某个值、删多余行（已由 P2 实例编辑器支持，P3 留痕+偏离告警）|
| **数据纠错（录入期）** | 主检/审核 | 录入与审核 | 数据本身的对错（增删样品、改数值）走录入 + 审核退回流程 |

**结论**：映射的"时间"没错（结构性的东西就该在模板期定），错的是"粒度"（不该按单元格/索引，应按矩阵/结构）。动态化之后，"数据和模板不一样"在绝大多数情况下被自动吸收；真正的个案落到报告实例编辑器。

---

## 四、数据录入出错怎么办（纠错路径）

报告是从**已审核、按版本锁定**的 record_data 渲染的，所以一线纠错在录入侧：

1. **录入期发现错** → 主检直接改（record_data upsert，审核状态重置 pending）。
2. **审核期发现错** → 审核员**退回 + 备注**（`reject`），主检改完重提，自动 +1 版本，全程审计。
3. **报告已生成后才发现错**：
   - **数据本身错** → 回录入侧改 + 重审 → **重新生成报告**（报告是快照，旧报告留痕）。
   - **只是报告呈现需要微调**（不涉及原始数据对错）→ 报告实例编辑器改，P3 的"偏离原始数据告警 + 留痕"会标红记录，便于复核。
4. **动态绑定的额外好处**：新增/减少样品这类"结构变化"不再是"错"——重新生成报告即自动反映，无需改任何映射配置。

> 原则：**原始数据的对错只在录入侧改（合规、留痕、永不被报告回写）**；报告侧只做呈现层的实例化调整，且全程告警留痕。

---

## 五、实现要点 / 影响文件

| 改动 | 文件 |
|---|---|
| `result_table` 增加 `source`（动态模式）类型 | `shared/types.ts` |
| `renderReportResultTableTypst` 加"矩阵驱动"分支：读 `record_flat_data` 的矩阵展平键，按实际样品展开行 | `shared/typst-generator.ts` |
| 结果表画布编辑器加"静态/动态"切换 + 选矩阵 + 勾选参数列 + 选结论列 | `client/components/FieldEditor/MatrixEditor/ReportResultTableCanvas.tsx` |
| 结论判定逻辑改为支持动态结论列（兼容现有 `is_conclusion`/`conclusion_col_id` 优先级） | `server/routes/reports.ts` 的 `buildReportTypst` |
| 实例文档（P2 content_doc）渲染同样走新分支（生成与重渲染统一 `renderContentDoc`，天然一致） | 已统一，无需额外改 |
| 矩阵展平键格式复用现有约定 `{matrixCode}__{sampleId}__{paramCode}` 等 | `shared/matrix-flatten.ts`（只读） |

**兼容**：默认 `mode: 'fixed'`，存量报告模板（逐格 binding）行为不变；新建/改造的表才用 `mode: 'matrix'`。

---

## 六、分期

1. ✅ **P-Map-1（已实现 2026-06·后端）**：`result_table.source`（`mode:'matrix'`+`matrix_code`+可选 `columns`/`conclusion`/`show_sample_label`）+ `shared/typst-generator.ts` 的 `renderMatrixResultTableTypst` 矩阵驱动分支（`renderReportResultTableTypst` 顶部按 `source.mode` 分流，缺省走静态向后兼容）。样品 id 由 `deriveMatrixSampleIds` 从 flat 键派生（顺序=录入序，通用于编辑器 mock 与真实出报告）；样品名优先 `record_raw_data[matrix_code].sample_labels`、回退 `{row_header_prefix} N`；单元格读 flat（含 computed）。**实测**：模板零写死行数，flat 3 样品→3 行、10 样品→10 行；真实 typst 编译出片 OK。`buildReportTypst` 无需改（已备好 record_flat_data/record_raw_data）。**待**：P-Map-2 画布 UI（现 source 需手填模板 JSON）。
2. ✅ **P-Map-2（已实现 2026-06）**：结果表画布编辑器的"动态模式" UI。`ReportResultTableCanvas.tsx` 顶部 `Segmented`「静态逐格 / 动态·矩阵驱动」切换——动态时显示配置面板（从关联原始记录的 data_matrix 里选镜像矩阵 + 样品名列开关/自定义列头 + 勾选进报告的参数列[可改列头/单位] + 结论列从参数列或录入型汇总列选），静态时显示原逐格画布（整段静态 UI 用 `{!isDynamic && (<>…</>)}` 包裹）。写 `field.result_table.source`，与 P-Map-1 渲染器同形状（round-trip 实测：自定义列头/单位/样品名列/汇总列结论全部正确）。tsc client 0、vite build 通过。
3. ✅ **P-Map-3（已实现 2026-06）**：动态结果表 `result_table.source` 的引用完整性校验并入 `shared/binding-integrity.ts`。`RecordFieldIndex.matrices` 加 `summaryCols` 集；`validateReportBindings` 末尾单独遍历各报告字段的 `result_table.source`（非 CellBinding），校验镜像 `matrix_code` 存在、选中 `param_code`/结论参数是该矩阵参数列、结论汇总列 `col_id` 是该矩阵汇总列；未关联记录/未选矩阵也告警。因服务端 PUT/approve/`binding-check` 与 `ProjectEditor` 实时 Alert 都共用该校验器，**一处改动三端生效**。实测：有效配置 0 告警；删参数列/汇总列、镜像不存在矩阵、未关联各正确告警；静态表（无 source）向后兼容 0 告警。tsc client 0。

---

## 七、与已有能力的关系

- **报告实例可编辑（P2）+ 留痕/偏离告警（P3）**：是本方案的"个案兜底层"——动态绑定吸收结构变化，实例编辑处理剩余特例。
- **binding 引用完整性校验（OPTIMIZATION.md 🟠）**：本方案让"按索引绑定失效"这类问题大幅减少，二者互补。
- **版本锁定渲染**：报告按 record_data 的 `template_version_id` 取数，动态展开读的是"当时锁定版本的实际录入值"，合规回放不受影响。

---

## 八、首页映射精简 + 多样品显示 + 检测结论表（🟡 设计待评审 2026-06）

> 回应用户反馈：首页（cover/front）的「选择数据来源」太杂乱；样品名称多样品要带编号、可调排版；检测结论表要按样品分组、支持子结论、并解决"结论在原始记录里位置不统一"导致抓取不可靠。

### 8.1 两个接口的职责划分 + 映射来源按场景精简

**接口 1.1（委托单录入）vs 1.2（报告取号）不能合并**——它们答两个不同问题：

| | 1.1 委托单（`work_orders.payload`） | 1.2 取号（`ReportMeta`） |
|---|---|---|
| 答 | "客户委托了**什么**" | "这份报告是**哪一份**" |
| 字段 | order（单位/地址/期限…14）+ sample（名/条码/序号/型号）+ test（项目名/标准/方法…14） | 报告编号·检验码·签发日期·出具机构抬头·CMA/CNAS 资质·**报告范围** |
| 粒度 | per 委托单 | **per 报告**（一单可出多份取号报告，各自编号） |

唯一重叠：`customer_name`/`customer_address`。1.2 不重发委托内容，靠 样品名+项目名 挂回 1.1。故二者互补、必须并存。

**「太杂乱」根因**：`BindingPickerModal` 对所有模板一律显示 9 个来源（字面量/委托单/委托单·样品/委托单·测试项目/报告接口1.2/系统/原始记录/矩阵单元/矩阵汇总）。但**首页是订单级**，用不到需要"具体样品/项目上下文"的那 5 个（样品/测试项目/原始记录/矩阵——它们只在项目报告里 resolve）。

**改法**：映射来源**按模板类型（editorMode）过滤**：
- **首页(cover/front)**：只留 `字面量 / 委托单字段 / 报告接口(1.2) / 系统字段`（4 个）。
- **项目报告(project)**：才显示 `委托单·样品 / 委托单·测试项目 / 原始记录字段 / 矩阵单元 / 矩阵汇总`。
- 影响文件：`client/components/ReportEditor/BindingPickerModal.tsx`（按 `editorMode`/`linkedRecord` 显隐 tab）。纯 UI 过滤，不动 `CellBinding` 类型。

### 8.2 样品名称：多样品带编号 + 可调排版

数据本就有（`payload.samples[]` 各带 `sort_no`+`name`），但 `order.sample_name` 是后端拼好的单串，无法控制格式。

**改法**：新增"样品清单"绑定（或给 `order` 源加 `sample_list` 形态），配置：
- **带编号**：`1#：中央通道、9#：中央通道薄膜样板-黑色`（编号取 `sort_no`）。
- **排版**：`inline`（顿号分隔一行）/ `lines`（每样品一行）。
- **单样品**：自动不显编号。

**配在哪**：**格式在首页模板定（统一规范），值生成时自动填，文员在「编辑首页」只微调字面量**（本份快照特例）。不让文员每次自排，避免不一致。
- 影响文件：`shared/types.ts`（绑定/字段加格式配置）、`shared/typst-generator.ts`（`resolveBinding` 读 `ctx.order` 暴露 samples 列表并按格式拼）、`server/routes/reports.ts`（ctx 里带上 samples 数组，现仅 joined `sample_name`）、`BindingPickerModal`（配置 UI）。

### 8.3 检测结论表：统一原始记录的"结论"语义 + 分组结论表

**现状**：`report_conclusion_table` **一项目一行**（`project_summary=[{index,name,standard,conclusion}]`），结论靠扫 `result_table` 的 `is_conclusion` 行 + `conclusion_col_id` 列（优先级见 README「结论判定」）。**不支持**样品分组、子结论。

**核心问题（用户点出）**：结论在原始记录里位置不统一（有的在试验数据表格、有的在结论分区）→ 抓取靠"猜哪行哪列"，不可靠；两个子结论更没法区分。

**改法①——原始记录用统一的「结论」语义字段（单一事实来源）**：
- 原始记录模板里，结论一律用带 `semantic_role='conclusion'` 的字段声明，每个结论 = `{ 子项目名(可空), 值 }`。
- **值可手填**（符合/不符合下拉）**或绑定**到矩阵汇总格/计算判定——**数据不搬家**，只加"这是结论"的指针。所以"结论算在表格里"还是"写在结论分区"都行，用这个字段把它指出来即可。
- 一个项目可有 **1 或 N** 个结论字段（N=子项目）；两个子结论=两个结论字段，各带子项目名。
- 抓取唯一确定：每个项目的记录 → 收集所有 `semantic_role='conclusion'` 字段 → 各生成一行。替代"扫 is_conclusion 行列猜"。

**改法②——结论表扩展成分组表（对齐用户图例）**：
```
样品 | 序号 | 项目              | 结论
1#   | 1    | 测试项目1          | 符合
1#   | 2    | 测试项目2          | 不符合
2#   | 3    | 测试项目3          | 符合
2#   | 4    | 测试项目4子项目1    | 符合       ← 序号 4 rowspan 2
2#   | 4    | 测试项目4子项目2    | 不符合
```
- `project_summary` 从 per 项目 → **per (样品 × 项目 × 子结论)**：`{ sample_no, sample_name, index, project_name, sub_conclusions:[{sub_name, conclusion}] }`。
- 列＝`[样品 序号 项目 结论 (+标准可选)]`；**样品列按样品 rowspan 分组**；一项目多子结论时**序号 rowspan**、项目列填子项目名（子项目名空→用项目名）。
- **单样品时自动隐藏 样品列 + 序号**（用户要求）。
- `conclusion_table` config 加：`group_by_sample?`、`show_sample_col?`/`show_index?`（缺省按样品数自动）、列扩 `'sample'`。
- 影响文件：`shared/types.ts`（结论语义 + `conclusion_table`/`project_summary` 扩展）、`server/routes/reports.ts`（`buildReportTypst` 按"结论字段"建 per 样品×项目×子结论 的 summary）、`shared/typst-generator.ts`（`renderConclusionTableTypst` rowspan 分组渲染）、原始记录模板编辑器（加「结论」字段类别/语义 + 子项目名）。

### 8.4 对用户三个最终问题的直接回答

1. **映射配置显示哪些**：按模板类型过滤——首页只 字面量/委托单/报告接口(1.2)/系统；项目报告再加 样品/测试/原始记录/矩阵。
2. **原始记录项目名称和结论是否要统一**：**要**。结论统一用「结论」语义字段标记（值可填可绑）；项目名优先用委托单 `test.project_name`，子项目名写在结论字段上。
3. **结论表如何抓才正确**：不扫表格猜，而是抓每个项目记录里所有「结论」字段（子项目名+值）→ 按 样品/序号 分组 rowspan 渲染。

### 8.5 分期建议

- ✅ **P-Map-4（已实现 2026-06）**：映射来源按模板类型过滤——`BindingPickerModal` 加 `PROJECT_ONLY_KEYS`（sample/test/record_field/record_cell/record_summary）+ `isVisible()`，首页/封面（`linkedRecord=null`）只显示 字面量/委托单/报告接口(1.2)/系统 4 源，项目报告显示全部；activeKey 钳到可见 tab。纯 client UI，不动 `CellBinding` 类型/渲染。tsc 0。
- ✅ **P-Map-5（已实现 2026-06）**：样品清单绑定。新增 `CellBinding` source `order_samples`（`numbered`/`layout:inline|lines`/`separator`）+ `ReportRenderCtx.order_samples` + `resolveBinding` 拼 `1#：名称、…`（单样品不显编号、lines 走 \n→multiline 换行）；`buildReportTypst` 从 `payload.samples` 建 `order_samples` 注入 cover/project/content_doc 各 ctx；`BindingPickerModal` 加「样品清单」tab（带编号开关 + 排版 inline/lines，首页可见）；`CoverEditor.MOCK_CTX` 加示例。tsc 0，PNG 实测一行/分行/单样品全对（含用户原例 `1#：中央通道、9#：…`）。
- ✅ **P-Map-6（已实现 2026-06）**：原始记录「结论」语义。`FieldDefinition.conclusion_field{sub_name}`；`FieldPropsPanel`（record 模式·值类型）加「作为检测结论」开关 + 子项目名。`buildReportTypst` 扫每个项目原始记录的 conclusion_field（值＝flat[code]）作结论来源，无标记回退旧 result_table 推断。结论值可手填或用 computed 从矩阵算（数据不搬家）。
- ✅ **P-Map-7（已实现 2026-06）**：分组检测结论表。`project_summary` 扩 `sample_no/sample_name/sub_name`；`conclusion_table` 加列 `'sample'` + `sample_col:'auto'|'show'|'hide'`。`buildReportTypst` 按样品号排序、跑 index、展平（每子结论一行）。`renderConclusionTableTypst` 按 样品/序号 `rowspan` 分组渲染（单样品自动隐藏样品列；`1#` 转义 `\#`）。结论表配置 UI + `CoverEditor.MOCK_CTX` 分组样例。tsc client/server 0；PNG 实测完全对齐用户图例（1# rowspan2 / 2# rowspan3 / 序号4 子结论 rowspan2 / 单样品隐藏样品列）。

- ✅ **P-Map-8（已实现 2026-06）**：矩阵汇总行直接标结论（免建 computed 指针字段）。`MatrixSummaryRowDef.conclusion_field{sub_name}`；`HeaderConfigCard` 加 `showConclusion` 开关 + 子项目名，`MatrixCanvas` 汇总行卡片传入（非 per_column_aggregate）、画布显示绿色「结论」Tag；`buildReportTypst` 结论扫描追加"扫各 data_matrix 的 summary_rows 标记"（值＝`flat[code__summary__rowId]`）。集成实测：标了的汇总行判定值正确进结论表、未标的（如平均值）跳过。tsc client/server 0。

- ✅ **P-Map-15（已实现 2026-06-23）**：首页样品名称/零件号 **单/多样品自动切换**（把"单样品直显、多样品出表"这条规则固化进系统渲染期，文员一套模板通吃，无需每份报告判断、也无需为单/多分别建模板）。痛点：`report_sample_table` 旧实现单样品也照画 1 行表、`report_sample` 内联绑定多样品堆顿号长串——靠 Alert 提示文员手动二选一，但文员编辑模板时根本不知道将来是单还是多。改法对齐 P-Map-7 的 `sample_col:'auto'` 同款约定：① `sample_table.mode:'auto'(缺省)|'always'`——`renderSampleTableTypst` 在 auto 且 `order_samples.length<=1` 时返回空串（折叠），单样品由首页「样品名称/零件号」内联字段直接显示；② `CellBinding{source:'report_sample'}` 加 `multi_text?`——`resolveBinding` 在 `order_samples.length>=2` 时返回 `multi_text||'见后续页。'`（常量 `REPORT_SAMPLE_MULTI_DEFAULT`），单样品仍直显该样品值；③ UI：`FieldPropsPanel` 样品信息表加「单/多样品 自动/始终」Radio + 改 Alert 讲清自动切换；`BindingPickerModal`「样品信息」Tab 加「多样品占位文案」Input（缺省 `见后续页。`）+ tab 切换/选键保留 `multi_text` + 改 Alert；`field-types.ts` hint 同步。影响文件：`shared/types.ts`、`shared/typst-generator.ts`、`client/.../FieldEditor/{FieldPropsPanel,field-types}.ts(x)`、`client/.../ReportEditor/BindingPickerModal.tsx`。client tsc 0、server 仅存量 rootDir 噪声；tsx smoke 实测六态全对（单样品内联直显/多样品内联「见后续页。」/多样品自定义文案/单样品表折叠/多样品表 3 行/单样品 always 强制出表）。`SampleSortNo`/`Model` 真实接收口 `external-orders.ts:183` 已入 `payload.samples`（数据通道完整）。

- ✅ **P-Map-16（已实现 2026-06-23）**：① 首页「检测周期」改为**数据驱动时间范围**。委托单无"整单检测周期"字段，故 `buildReportTypst` 跨全单材料分单聚合 `start_date`/`end_date`：`OrderMetaKey` 加 `test_period`(开始~结束合成串，两端相同折单值、缺一侧显另一侧)/`test_start`/`test_end`，写进 `ctx.order_meta`；`BindingPickerModal`「委托单字段」Tab 加这三键；首页 seed (`scripts/front-template.ts`) 的 `test_cycle` 字段绑 `{source:'order',key:'test_period'}`；`CoverEditor.MOCK_CTX` 加 `order_meta` 预览值。② **样品信息表表头去掉底色**——`renderSampleTableTypst` 表头单元去掉 `fill: rgb("f5f5f5")`（仅保留加粗）、`InstanceEditor` WYSIWYG 表头去掉 `background:#fafafa`。client tsc 0、server 仅存量 rootDir 噪声；node/tsx smoke 实测：跨分单 min开始~max结束聚合正确（含仅一侧/边界）、`resolveBinding(order.test_period)` 读 `order_meta` 正确、表头无 fill 且仍加粗。

- ✅ **P-Map-17（已实现 2026-06-23）**：首页图片表——示例渲染填 mock 图 + 满宽。根因：首页种子模板用了 `report_image_gallery`（图库，靠关联原始记录 image 字段取图），但首页是订单级、`coverCtx` 无 `linked_record_template` → 预览与真实报告都只出「（未关联原始记录模板）」占位、且 `renderReportImageGalleryTypst` 经 `wrapFigure` 无 `width:100%` 不满宽。修法：① 首页种子 `scripts/front-template.ts` 的「原始样品」字段改 `report_image_gallery` → `report_photo_table`（首页原生：文员上传、`renderPhotoTableTypst` 已 `#block(width:100%)` 满宽、`withMockPhotoTables` 已注入示例照片）；② `renderReportImageGalleryTypst` 输出包 `#block(width:100%)` → 图库也满宽（与原始记录图片组/原样照片表同口径，含项目报告）；③ `mock-data.ts` 导出 `MOCK_IMAGE_DIR` + `mockPhotoItem()`，`CoverEditor.MOCK_CTX` 加 mock `linked_record_template`(一个 image 字段)+`record_raw_data` → 首页编辑器预览的图库字段也出样图（仅预览）。client tsc 0、server 仅存量 rootDir 噪声；tsx smoke 实测：photo_table 出 before.png+width:100%+1fr 满宽表；gallery（带 mock 关联记录）出 before.png+满宽+无占位。

- ✅ **P-Map-18（已实现 2026-06-23）**：图片记录分区**占位**满宽。`generateImageGroupContent`（`section_role='images'` 含 image 字段的分区，模板期占位）原用 `#table(columns: 1, …)`——`columns: 1` 是单个 **auto 宽**列、收缩到内容 → 渲染成窄框（用户截图：首页「图片记录占位」只占左侧一小块，与上方满宽的检测结论表不齐）。改 `#block(width: 100%, breakable: false)` + `columns: (1fr)`，与**数据期** `renderImageGroupTypst`（早已 `columns:(1fr)`）及检测结论表同口径 → 占位与实际图片表都满整列宽。tsx smoke 实测占位含 `width:100%`+`columns:(1fr)`、不再 `columns: 1`。注：首页（订单级）的**plain `image` 字段无录入数据通道、永远停在占位**；要在首页放可上传的原样照片用 `report_photo_table`（见 P-Map-17）。

- ✅ **P-Map-19（已实现 2026-06-23）**：首页三张自动表（样品信息表 `report_sample_table` / 检测结论表 `report_conclusion_table` / 原样照片表 `report_photo_table`）补齐**标题+备注+间距**编辑，与项目报告图表（result/equipment/image_gallery 走 `wrapFigure`）同口径。痛点：样品信息表原 `applyBlockStyle` 渲染、编辑器无「显示标签/备注/间距」入口。改法：① **渲染端**三个 renderer 改走 `wrapFigure`——`renderSampleTableTypst`/`renderPhotoTableTypst` 末尾 `applyBlockStyle`→`wrapFigure`（标题=field.label 受 hide_label 开关、备注=field.caption、间距=label_gap/caption_gap、题注位置/段前后间距统一生效；样品表默认 `hide_label:true` 不显标题、照片表说明行 caption_label/text 与表头 header 仍在 body 内）；`renderConclusionTableTypst` 自带居中标题改吃 `field.label_style`(样式)+`field.label_gap`(标题↔表距离，缺省 0.6em)、末尾包 `wrapFigure`（补 field.caption 备注，自带标题不与 wrapFigure 标题重复——hide_label 缺省）。② **编辑器**（`FieldPropsPanel`）：`FIGURE_LAYOUT_TYPES` + `NO_FORMAT_BINDING` 加 sample/photo → 基础 Tab 出「显示为标题」开关、得「版式」Tab（标题样式+标题与表距离/题注位置+备注样式+备注与表距离/对齐间距）；备注信息输入框条件加这三类；结论表「显示标题」开关从「类型配置」移到「基础」Tab（走 `conclusion_table.show_title`，标题文字仍在类型配置改），`titleShown` 加结论分支。client tsc 0、server 仅存量噪声；tsx smoke 实测：三表 显示标签/备注/label_gap/caption_gap 全生效，且默认态（样品/照片不显标题、结论显居中标题、间距 0.6em 兜底）向后兼容。

- ✅ **P-Map-20（已实现 2026-06-23）**：检测结论表——① **标题 bug 修复**：原自带 `#align(center)` 居中标题（`conclusion_table.show_title`/`title_text`），与「显示为标题」开关（驱动 `field.hide_label`→wrapFigure 的 `field.label` 标题）打架——点开关无效、却恒显一个居中标题。改：renderer **删除内置居中标题**，结论表并入 `FIGURE_LAYOUT_TYPES`、走标准 figure 路径（标题＝`field.label`、**居左、跟随模板字体**、`hide_label` 开关控制）；`show_title`/`title_text` 标 `@deprecated`（renderer/编辑器/InstanceEditor 均不再读），新建/seed 默认 `hide_label:false`（标题默认显示）。回退 P-Map-19 给结论表加的所有特判分支（基础开关/`titleShown`/figure-layout 装配/配置 Tab 的「表格标题」输入）。② **列宽/行高配置**（用户要求）：类型配置 Tab 加「列宽」（按当前「列」逐列填 fr/cm/pt/%，留空＝默认）+「行高（单元格上下留白）」（`cell_inset_y`→`inset:(x:8pt,y:N)`）。`col_widths` 渲染端**改按列名映射**（`widthByKey` 从 `cfg.columns`/默认 3 列 zip），样品列因 `sample_col` 自动增减时其余列宽不串位（旧按 final-cols 下标会错位）。client tsc 0、server 仅存量噪声；tsx smoke 实测：显示标题→`field.label` 居左无居中标题、默认无开关不显标题、列宽 1.5cm/6cm + 行高 inset(y:12pt) 生效、加 sample 列后结论列仍 6cm 不错位。

- ✅ **P-Map-21（已实现 2026-06-23）**：检测结论表列宽——① **旋钮 UI**：逐列「InputNumber 旋钮 + 单位 Select（fr/%/cm/pt）」替代自由文本（与行高同款手感）。② **样品列可调**：原列宽只列「列」里选中的列（样品列因 sample_col 自动出现、不在「列」里→无法调）；改为可调列＝`样品列(除非 sample_col:'hide')` + 已选列，样品列宽也能设。③ **按名存** `conclusion_table.col_widths_map`（`{sample/index/project/standard/result → 宽度串}`）取代位置数组——样品列自动增减/列重排都不串位；旧 `col_widths`（位置数组，`@deprecated`）渲染端仍兜底读、编辑器回显。renderer `widthByKey` 合并"旧位置数组(legacy)→新 map(覆盖)"。client tsc 0、server 仅存量噪声；tsx smoke：map 含样品列 1.2cm/序号 1cm/结论 5cm + 行高 inset(y:10pt) 全生效、项目列默认 1fr、旧位置数组兜底 6cm、sample_col:hide 不泄漏样品列宽。（用户另问"类型配置是否改用表格画布"——结论表列固定且内容自动展开，full canvas 偏重，当前旋钮+样品列已够；如需可视化再单独做。）

- ✅ **P-Map-22（已实现 2026-06-23）**：样品信息表（`report_sample_table`）配置优化——① **列宽（旋钮+单位）+ 行高**：与结论表同款；列宽按名存 `sample_table.col_widths_map`（`index/name/model`）取代位置数组 `col_widths`(`@deprecated` 仍兜底）；行高 `cell_inset_y`→`inset:(x:6pt,y:N)`（缺省 6pt）。② **第一列默认标签 `序号`→`样品编号`**（该列值＝接口 `SampleSortNo`，本就是样品编号；renderer/编辑器/InstanceEditor 默认全改）。③ **类型配置重做**（原 Alert 一大段 + 三个裸列名输入太抽象）：精简 Alert；「列」改成**逐列一行**＝`显示开关 + 列名输入 + 列宽旋钮(单位下拉)`，所见即所配；加行高旋钮。列可勾选显隐（写 `columns`，空兜底全显）。client tsc 0、server 仅存量噪声；tsx smoke：默认第一列＝样品编号、map 编号 2cm/零件号 5cm、隐藏名称列只 2 列、行高 inset(y:10pt)、旧位置数组兜底 4cm 全对。

- ✅ **P-Map-23（已实现 2026-06-23）**：① 样品信息表「列」配置的 `addonBefore` 由语义名（"样品编号列"…）改成 **`列1/列2/列3`**（语义靠 placeholder 表达，标识更干净）。② **「添加分区」新增首页预设「样品信息表」**（`section-presets.ts` 的 `cover_sample_table`，仅 report-cover）：建一个 `hide_title:true` 分区 + 一个 `report_sample_table` 字段，**默认显示标签**（`hide_label:false`）、标签＝**`样品信息：`**、**标签↔表距 `label_gap:'12pt'`**，列默认 `index/name/model`。client tsc 0；tsx smoke：预设字段 label/hide_label/label_gap 正确，渲染出「样品信息：」标题 + `below: 12pt` 居于表上方。

- ✅ **P-Map-24（已实现 2026-06-23）**：新增**时间范围字段类型 `daterange`**（配检测周期）。`FieldType` 加 `daterange` + `FieldDefinition.date_range:{start?,end?:CellBinding, separator?}`；两端各一个绑定——`literal`＝手填日期、`order/record/…`＝绑定字段（如 `test_start`/`test_end`）。渲染 `formatDateRange`＝`开始 ~ 结束`（缺一端只显另一端、都缺空、separator 缺省 ` ~ `），走 `injectReportFieldsIntoTypst` 注入 `#let data`（`collectTypstDataKeys` 默认收 daterange code）。编辑器：`field-types` 调色板加「时间范围」（report-cover/project）+ typeToCategory/createField/convertField 三处；`FieldPropsPanel` 加 `daterange` 类型配置（两个 `CellBindingButton`＝开始/结束 + 分隔符）、并入 `NO_FORMAT_BINDING`（不出通用单 binding Tab）。首页种子 `front-template.ts` 的检测周期 由 `text`+`order.test_period` 改为 `daterange` 绑 `order.test_start`/`test_end`。client tsc 0、server 仅存量噪声；tsx smoke：绑定/手填/缺一端/自定义分隔符/端到端注入 `#let data` 全对。
  - 关于「字段间距无法一起调样品表+原样图片」：用真 typst 编译实测（document `line_gap` / section `block_spacing` / 字段 `space_before` 三种控件 × 样品表/原样照片表/图片占位），**三种间距对三类图表均正确响应（每档 +40pt）**，未能在当前代码复现"间距冻结"。判断系 P-Map-19/P-Map-17 把这几张表统一并入 `wrapFigure`/`#block(width:100%)` 后**已修复**；待用户重启 dev / 重出报告确认，或指明具体失效控件+场景再深挖。

- ✅ **P-Map-25（已修复 2026-06-30，回应"统计行『平均值』绑定后报告取不到值"）**：根因＝**绑定选择器没认 `per_column:true` 统计行**。P-Map-13a 把矩阵每列统计从 `per_column_aggregate` 改成 `input_number`+`per_column:true`（手填/逐格公式，系统术语＝「统计行」，区别于跨列「汇总行」），值按列存（键 `{code}__summary__{rowId}__{paramCode}`，见 `matrix-flatten`/`typst-generator` 的 `per_column_aggregate || per_column` 判定）。但两处绑定 UI 仍只判 `source_type==='per_column_aggregate'`：`BindingPickerModal`（"数据表格"选源弹窗）把 `per_column` 统计行渲染成**整行单选**→ `onPick` 出 `record_summary`**无 `param_code`**；`BindingEditor`（下拉式）则**不显「参数列」下拉**。两者都产出无 `param_code` 的绑定 → 出报告时按跨行键 `…__summary__{rowId}` 取，而值在按列键里 → **永远空**（用户「平均值」抓不到即此）。修复：两处 `isPerCol`/`isPerColumn` 统一改为 `source_type==='per_column_aggregate' || !!per_column`——统计行改按【每列一格】选中→绑定带 `param_code`，与展平/渲染口径一致；弹窗顶栏文案/计数区分「汇总行 vs 统计行(逐列)」。另：把现网密度报告「平均值」汇总列那条**无 param_code 的存量绑定补上 `param_code=col_5`**（`audit-report-mappings.ts` 可复查）。client tsc 0；抓包实测信封/渲染得平均值 `1.17`。⚠️ 浏览器待实测：弹窗里「平均值」行应显示为每列可点的格子（而非整行一条）。

- ✅ **P-Map-26（已修复 2026-06-30，回应"拆出的每份报告样品信息表都列错样品/列了别的样品"）**：根因＝**首页样品信息表（`report_sample_table`）的 `order_samples` 收敛逻辑脆弱**。`renderSampleTableTypst` 直接铺 `ctx.order_samples`；而 `buildReportTypst` 旧逻辑＝拿委托单 `payload.samples` **全量**、再按各 assignment 原始记录 `sample_external_id` **过滤**，且"匹配不到任何样品就保底不过滤（列全部）"。于是只要接口单的 `sample_external_id` 与委托单样品 id/名 体系对不上（真实接口常见）→ 过滤命中 0 → **退化成列出整单全部样品**；或模糊多键误命中 → 列出 scope 外样品。表现：拆报告（按样品/项目/取号）每份首页样品表都不对（如某报告 scope 是「茶山绿」却列出「卡其白+航空银」）。修复：改成**从本报告 scope（各 assignment 记录的 `sample_external_id`，去重保序）正向构建 `order_samples`**——逐个 sid 去 `payload.samples` 多键(id/名/条码/序号)回查明细，查不到则用 sid 兜底成一行；**绝不**退化列全部、**绝不**混入 scope 外样品。与「检测结论表」同源（都跟 assignments 走），故"调整样品/项目"改 scope 后两表一致增减。`server tsc` 0；dev 实测：样品A单报告只列样品A、样品B单报告只列样品B、整单列全 A+B。⚠️ 现网**存量已生成报告需重渲染/重算**才会套用新逻辑（旧 final_typst 是按旧逻辑出的）；部署后新生成即正确。

> 评审通过后按 modify-system 分期实现；与第二节"结果表矩阵驱动"是两条并行线（一个管"结果表逐格→动态"，一个管"首页映射+结论表"）。

---

## 九、表头可绑定 + 统一绑定选择器 + 项目模板只暴露原始记录来源（🟡 设计待评审 2026-06-20）

> 回应用户三点：① 项目检测结果表的**表头**也要能绑定数据来源（如「客户要求/标准要求」绑原始记录里的选择框、表头**单位**绑原始记录录入时所选单位）；② 逐格绑定遇到「录入样品数不一样」仍无法解决；③ 文字字段绑定（`BindingPickerModal` 十个 tab）与结果表单元格绑定（`ReportResultTableCanvas` 内嵌另一套源选择卡）是**两套独立 UI**，要统一；且项目模板映射只需考虑原始记录来源。

### 9.0 三个问题是同一个根因

现模型把**表头**和**单元格**当成两种东西：
- **单元格内容** = 可绑定（`CellBinding` 13 源，经 `resolveBinding`）；
- **表头 `label`/`unit`/`note`、结论列名、样品列名** = **死字符串**（模板写死或从矩阵静态继承，**不过 `resolveBinding`**）。

于是表头不能绑、单位不能跟录入走；两类东西各配各的 UI（`BindingPickerModal` vs `ReportResultTableCanvas` 内嵌卡——长得像但是两份实现）。而问题②（样品数变化）其实**第二节的动态矩阵模式 `source.mode='matrix'` 早已解决**（P-Map-1/2/3 已实现，按实际录入样品展开行），痛点是它**没成为主路径**——默认"静态逐格"，工程师顺手逐格绑，又掉回索引绑定老问题。

**统一对策＝"一切皆可绑定槽位 + 单一绑定选择器 + 动态为默认"**，把三问题一并解决。

### 9.1 核心抽象①：表头也变成"可绑定槽位"

把列头/结论列/样品列的 `label`、`unit`、`note` 从 `string` 升级为「静态文本 **或** 绑定」（增量、向后兼容）：

```ts
// 静态模式 columns[]、动态模式 source.columns[]、conclusion、show_sample_label 列名 等同步加：
label?: string;        label_binding?: CellBinding;   // 有 binding 走 resolve，否则用字面量
unit?: string;         unit_binding?: CellBinding;
note?: string;         note_binding?: CellBinding;
```

渲染端 `renderMatrixResultTableTypst` / `renderReportResultTableTypst` 的列头那几行（现 `c.label ?? p?.label`、`c.unit ?? p?.unit`、`ann = [unit, note]…`）改成 `resolveBinding(x_binding) ?? x`。**这是问题①的核心**——改完后「客户要求/标准要求」「单位」都能跟原始记录走。

### 9.2 核心抽象②：新增来源 `record_header`（取原始记录矩阵表头的录入值）

> 决策（2026-06-20）：载体**两种都有**——故两条路都要支持。

- **「客户要求/标准要求」若是原始记录里独立的 choice/radio 字段** → 直接用现有 `record_field` 绑定，无需新增。
- **若是数据矩阵表头上录入时选的备注/单位**（如「判定要求(客户要求/标准要求)」、单位）→ 它录入后存在 `DataMatrixValue.*_overrides`（见 编辑器改进方案 162 行的统一表头备注模型），现 `resolveBinding` **没有来源能取到**。新增 `record_header` 来源：`{ source:'record_header', matrix_code, param_code, part:'unit'|'note' }`，渲染时从 `record_raw_data[matrix_code]` 的对应 override 取录入值。
- **动态模式下的"自动继承"**：动态参数列的 `unit`/`note` 缺省**直接取该矩阵列表头的录入 override**（而非模板写死的静态单位），连手动绑都省了——"单位随录入选择变化"天然成立。手动 `unit_binding` 仅用于"指到另一列表头"等特例。

### 9.3 核心抽象③：单一 `<BindingPicker>` 组件，三处复用 + 按上下文过滤来源

> 决策（2026-06-20）：项目模板**只留原始记录相关来源**。

把 `BindingPickerModal` 与 `ReportResultTableCanvas` 内嵌的源选择卡**合并成同一个 `<BindingPicker allowedSources=…>`**，文字字段 / 单元格 / 表头槽位全调它，UI 彻底一致。`allowedSources` 按场景过滤：

| 场景 | 暴露的来源 |
|---|---|
| **项目报告(project)** | 原始记录字段 `record_field` / 矩阵单元 `record_cell` / 矩阵汇总 `record_summary` / **矩阵表头 `record_header`(新)** / 字面量 `literal` |
| 首页/封面(cover/front) | 字面量 / 委托单 `order` / 样品清单 `order_samples` / 报告接口 `report_meta` / 系统 `system`（沿用 8.1 已实现的过滤） |

项目模板里委托单/样品/测试/报告接口/系统**全部移除**——这些信息经原始记录间接流入，项目报告只面对"关联的那一份原始记录"这一个数据源，心智最简。选择器从 10 tab 瘦到 ~5 tab，且表头与单元格点开的是**同一个面板**。

### 9.4 问题②：样品带（重复区）模型 —— 比"整表动态"更优的统一解

> 决策（2026-06-20，第二轮细化）：采用用户提议的**"样品带 / 明细带"（detail band）模型**作为主模型；现有"整表动态矩阵"（P-Map-1~3）**保留为一键便捷入口**；带子**行/列两种朝向都做**。

**根因复述**：可变的只有"样品数量"这一个维度，而原始记录的 `data_matrix` 模型**已经**把"样品 = 矩阵的行"定义清楚（flat 键 `{matrix_code}__s{样品id}__{参数code}`、`sample_labels`、`allow_add_remove_samples`），所以"哪里是样品"在录入侧已确定，**不用再在原始记录里加标记**。报告把样品铺成行还是列，是**报告侧排版选择**。

**模型**：在逐格网格底座上加**一个新概念**——把结果表的**某一行（或某一列）标记为"样品带"**，绑到矩阵 `{matrix_code}`：
- 带子内的格子绑定参数用**"当前样品"相对引用**（`sample_idx:'$band'`），而非写死索引；带外格子照旧逐格绑（固定字段/字面量都行）。
- 渲染：用 `deriveMatrixSampleIds` 取实际样品数 N，把这条带子复制成 N 行/列，每份用样品 i 解析；带外格子只渲一次。
- 朝向 `band.axis = 'row' | 'col'`（样品纵向展开 / 横向展开，转置）。
- **整表动态矩阵 = 样品带的特例**（一条覆盖整表的带子）。现有"选矩阵+勾列"UI 保留为"一键生成整表样品带"。

**比整表动态强在**：一张表里能**混固定格 + 重复样品带**（整表动态做不到）、行/列两种朝向、逐格灵活性保留。表头仍走 9.1~9.3 的"可绑定槽位 + 统一 BindingPicker"。

### 9.5 三个问题的对应解

| 用户问题 | 解 |
|---|---|
| 表头要能绑（客户要求/标准要求） | 抽象①+③：表头槽位接同一 `BindingPicker`，绑 `record_field` 或 `record_header` |
| 表头单位跟录入走 | 抽象②：`record_header` 取矩阵表头单位 override；动态模式默认自动继承 |
| 样品数不一样 | 抽象④（9.4）：**样品带**（逐格底座 + 一条重复区，行/列朝向）；整表动态是其特例、保留为便捷入口 |
| 文字字段 vs 单元格 UI 不一致 | 抽象③：合并成一个 `<BindingPicker>` |
| 项目模板只显原始记录来源 | 抽象③：`allowedSources` 在 project 模式只留原始记录相关 |

### 9.6 影响文件（预估）

| 改动 | 文件 |
|---|---|
| 表头槽位类型 `*_binding`；新增 `record_header` 到 `CellBinding` | `shared/types.ts` |
| 列头渲染走 `resolveBinding`；`resolveBinding` 加 `record_header` 分支（读 `record_raw_data` overrides）；动态列单位/备注缺省继承录入 override | `shared/typst-generator.ts` |
| 合并出单一 `<BindingPicker allowedSources>`；project 模式只留原始记录来源 | `client/.../ReportEditor/BindingPickerModal.tsx`（升级）、`FieldEditor/MatrixEditor/ReportResultTableCanvas.tsx`（删内嵌源卡、改调 BindingPicker） |
| 表头配置卡接 BindingPicker（label/unit/note 各可"静态 or 绑定"切换） | `FieldEditor/MatrixEditor/HeaderConfigCard.tsx` |
| 动态设默认；Segmented 默认落动态 | `ReportResultTableCanvas.tsx` |
| `record_header` + 表头绑定 纳入引用完整性校验 | `shared/binding-integrity.ts` |

### 9.7 分期建议

- ✅ **P-Map-9（已实现 2026-06-20·地基）**：`CellBinding` 加 `record_header`（读 `parameter_unit_overrides`，无 override 回退矩阵静态 unit、仍无返回 `''`）；动态结果表参数列**单位/备注缺省自动继承录入 override**（优先级 显式 binding > 列字面量 > 录入 override > 矩阵静态）；表头三槽位 `label_binding`/`unit_binding`/`note_binding` + `source.sample_label_binding` + `conclusion.label_binding` + 静态 `columns[].label_binding/note_binding` 渲染端经 `resolveHeaderSlot` 走 `resolveBinding`；`binding-integrity` 加 `record_header` 校验 case。**纯类型+渲染、无 UI**。smoke 实测：动态表单位跟随录入 cm/min、`record_header` 取另一列录入单位、`record_field` 绑「客户要求」均正确；client tsc 0、server 仅存量脚本噪声。⚠️ 表头槽位绑定的**设置 UI** 在 P-Map-11；纳入 `collectReportBindings` 校验在 P-Map-12。
- 🟡 **P-Map-10（渲染层已实现 2026-06-20，画布 UI 待 P-Map-11）**：静态结果表加"样品带"——`result_table.band{axis,matrix_code,ref_id}` 标某行/列为重复区；带内格子用新来源 **`record_cell_sample`**（当前样品某参数）/ **`record_sample_label`**（当前样品名）相对绑定（未重载 `record_cell`，避免全代码 ripple）；`shared/typst-generator.ts` 的 `expandResultTableBand` 在进入现有静态渲染器前按实际 N 复制成 N 行/列（带内绑定按各 sid 落字面量、带外只渲一次），返回普通静态表 → 合并格/汇总/行高等逻辑零改动；**行/列两朝向**（axis='col' 时样品名自动成列头）。`binding-integrity` 已加两源校验。smoke 实测：row 朝向 2 样品展开 + 带外固定列每行重复、col 朝向转置数据 + 样品名成列头均正确；client/server tsc 0（仅存量脚本噪声）。整表动态（P-Map-1~3）= 覆盖整表的样品带特例，保留为便捷入口。**待 P-Map-11**：画布上标记带子 + 带内"当前样品"相对绑定的 UI（现靠 band JSON 配置）。
- ✅ **P-Map-11（已实现 2026-06-20，11a+11b+11c）**：统一 BindingPicker。**11a**：`BindingPickerModal` 加 `allowedSources` 白名单 + `record_header` tab（`MatrixHeaderPicker`）+ `title`；`ReportResultTableCanvas` 删自带内嵌源卡（4 子 picker）改用同一弹窗，`allowedSources=RESULT_TABLE_SOURCES`（项目结果表只暴露原始记录来源）；`BindingSummary` 补三新源。**11b**：`HeaderConfigCard` 加 `allowBinding`/`linkedRecord`/`bindingSources` + 「表头取值绑定」段（标题/单位/备注各可绑，内嵌 BindingPickerModal，落 `label_binding/unit_binding/note_binding`），静态列 + 动态参数列两处接上。**11c**：画布顶部「样品带」面板（关/出行/出列 + 镜像矩阵 + 哪行哪列）+ 带内格子绿色高亮 + 统一弹窗 `record_band` tab（`BandCellPicker`：样品名/参数列，by `targetInBand`+`bandMatrixCode`）。client tsc 0 + vite build 通过。⚠️ 仅编译级验证，点击交互待浏览器实测。
- ✅ **P-Map-12（已实现 2026-06-20）**：引用完整性校验把【表头槽位】绑定收进 `collectReportBindings`——静态 `columns[].label_binding/note_binding`、动态 `source.columns[].{label,unit,note}_binding`、`source.sample_label_binding`、`source.conclusion.label_binding` 都过 `checkRecordBinding`（path 形如「… · 表头[速率 单位]」）；样品带单元格（record_cell_sample/record_sample_label）经 cells 遍历本就纳入。smoke 实测：失效表头 record_header + 失效带内 record_cell_sample 各被标出、有效绑定不误报（共 2 处失效）；client/server tsc 0。服务端 PUT/review/binding-check 与 ProjectEditor 实时 Alert 共用该校验器，一处生效三端。
- ✅ **P-Map-12 补（2026-06-20，回应"汇总行/列表头无法右键编辑卡片、无法绑定，应与所有表头一致"）**：`summary_rows[]/summary_cols[]` 加 `label_binding/note/note_binding`；画布汇总行/汇总列表头从"行内改名"改为**双击/右键弹 `HeaderConfigCard`**（同数据列：标题·备注·表头取值绑定·删除，`sumRowCard`/`sumColCard`）；`renderReportResultTableTypst` 汇总列表头 + 汇总行首格 label/note 经 `resolveHeaderSlot` 解析绑定；`collectReportBindings` 收汇总表头绑定。smoke：汇总列/行表头 label_binding+note 解析为「客户要求（cm）」「客户要求（近似）」正确；tsc 0 + vite build 通过。至此**所有表头（数据列/汇总行/汇总列）编辑与绑定完全一致**。
- ✅ **表头绑定改到「表格下方面板」+ 名称/备注分别绑定（2026-06-20，回应"绑定不要放右键卡片，应像格子一样在下方选，且可选绑名称还是备注/单位"）**：`Target` 加 `col_h/sumrow_h/sumcol_h` 三种表头目标；**点表头＝在下方面板出现绑定区**（两行：「名称」「备注/单位」，各可绑定/改绑/清除，二者皆可绑或只绑其一，走统一 `BindingPickerModal`、写 `label_binding`/`note_binding`）；**右键表头＝弹卡片只改固定文字/分组/删除**（卡片不再含绑定段——`allowBinding` 不再从结果表传入）。选中表头蓝色描边高亮。data/汇总格仍是单一数据源面板。文件：`ReportResultTableCanvas.tsx`（getHeaderBinding/setHeaderBinding/hdrPart + 面板分支 + 表头 th/td onClick→target、onContextMenu→card）。client tsc 0 + vite build 通过。⚠️ 浏览器点击流待实测。
- ✅ **左上角对齐渲染 + 样品带面板清晰化（2026-06-20，回应截图反馈）**：① **去掉左侧管理列**——编辑器表格第一个真实列头（如「试样编号」）即报告表格左上角，与渲染一致；行操作（行号 + 上下移 + 删除 + 行高拖拽）移到**最右侧「操作」列**（仅编辑器、不进报告）。汇总行 label 改放 col0、body colSpan 相应 -1、尾部补「操作」占位，跨页 rowspan 自动把操作列推到最右（已重算 thead/数据行/汇总行/footer 的列数）。删除了行内改名残留（`editing`/`commitEditing`/`rename*`/`EditOutlined`）。② **样品带面板**：`Segmented`→`Radio.Group buttonStyle="solid"`（选中蓝色块明显），三档加方向箭头「关（固定行列）/ 按样品出行 ↓ / 按样品出列 →」+ 旁边「？这是什么」Tooltip + 绿字说明两档区别；镜像矩阵/样板行(列) 各加文字标签 + Tooltip，样板行选项显示「第 N 行（标签）」。client tsc 0 + vite build 通过。⚠️ 浏览器待实测（尤其多汇总列+多数据行时操作列对齐）。
- ✅ **样品带再简化 + 样品序号 + 表头来源收窄（2026-06-20，回应反馈）**：① **去掉"样板行/列"下拉**——样品带固定作用于第一条数据行/列（画布不再选具体格；`expandResultTableBand` 与画布 isBandRow/Col 在 ref_id 失效时都回退第一条）；② **新增 `record_sample_index` 来源**（样品序号，按展开顺序自动 1,2,3…）——`BandCellPicker` 加「样品序号（自动 1,2,3…）」按钮，解决"试样编号全是 1"；`expandResultTableBand` 把它落成 `String(i+1)`，校验/Summary 同步；③ **平均值不会被当样品**——`deriveMatrixSampleIds` 本就跳过 `__summary__`/`__sumcol__` 键，样品带只展开真正样品行（原始记录里平均值应是"汇总行"，报告里用"汇总行"+绑定矩阵汇总取，UI 已注明）；④ **表头绑定只暴露原始记录来源**——表头(名称/备注)绑定用新的 `HEADER_SOURCES=['literal','record_field','record_header']`（去掉矩阵单元格/汇总行——那是数据格的事）。smoke：试样编号自动 1/2/3、3 样品(不含平均值)、ref_id 失效回退第一行均正确；tsc 0 + build。
- ✅ **样品序号 round-trip + 按列汇总行（2026-06-20，回应"序号没递增 / 平均值需多列求值"）**：① `record_sample_index` 补进 `BAND_CELL_SOURCES` + `BindingPickerModal` 的 initialTab/reset/isVisible（之前漏了，导致"样品序号"tab 在某些情况下不出现/不回显，序号绑不上）；② **按列汇总行**——`summary_rows[].per_column?:boolean` + `cells?:[{colId,binding}]`：`addSumRowPerCol` 新增「+ 汇总行（按列分别填）」（缺省 label「平均值」），渲染 `renderReportResultTableTypst` per_column 分支＝首列标签 + 每个数据列一格（取 cells[].binding），画布 per_column 行每格是 `sum_row_cell` 目标（点格→下方单格绑定），`collectReportBindings` 收 cells 绑定。解决「平均值要逐列求值」：首列填平均值、各参数列绑矩阵的逐列平均(`record_summary` 带 param_code)。原"整行一个值"汇总行保留为「+ 汇总行（整行一个值）」。smoke：样品行展开 + 按列平均值 15/150 对齐各列、序号 1/2 正确；tsc 0 + build。
- ✅ **P-Map-12 交互/清晰度修复（2026-06-20，回应截图反馈）**：① 开卡 handler 上移到整个 `<th>/<td>`——点击/右键表头任意处都开卡（原只在内层小 div、点边距无效，故"列表头右键无效"）；② `HeaderConfigCard` 加 `closeOnOutsideClick`，结果表所有卡片点卡外即关（不必点确定；数据矩阵编辑器不开、保持受控）；③ 拖拽把手加 `stopPropagation` 防误开卡；④ 左上角「行\列」管理栏表头→窄灰「#」栏，第一个真实列头成为视觉左上角、与渲染一致；⑤ **单位绑定清晰化**：record_header tab 改名「单位/要求（跟随录入）」+ `MatrixHeaderPicker` 文案具体化（列出该列录入可选项）+ HeaderConfigCard 绑定段文案改"表头随录入变化"并举例；⑥ **样品带绑定可视化**：`BandCellPicker` 改为渲染原始记录矩阵骨架，点【列头】＝每样品取该列、点「试样」列头＝样品名，并说明"样品在矩阵里是行、报告按行/列展开与原始记录无关"。client tsc 0 + vite build 通过；纯前端交互/文案，未动渲染/类型。⚠️ 浏览器点击流仍需实测。

- 🟡 **P-Map-13（13a 已实现 2026-06-22 / 13b 待实现）——试样带方向自动推导：删手动"按行/按列"，两端各声明试样轴、系统自判转置**（下方"分期建议"后列 13a/13b 落地状态）：

  **动机（用户反馈）**：现在报告侧绑样品数据"太复杂"——要分别 ① 拨"按样品出行/出列"radio（`band.axis`）、② 把试样编号/序号绑到某格、③ 选矩阵，其中 ①② 表达的是**同一件事**（样品往哪个方向铺），冗余且易互相矛盾。

  **核心洞察**："样品往哪个方向展开"不该是独立开关，它已被**"试样编号绑在哪条表头"**隐含决定：试样编号在**左侧表头列** → 每行一样品（轴=row）、顶部表头=参数；试样编号在**顶部表头行** → 每列一样品（轴=col、转置）、左侧表头=参数。

  **关键修正（推翻 9.4 的旧假设）**：本方案 9.4/264 行曾断言"原始记录 data_matrix 已把样品=行定义清楚"。**这是错的**——存储键 `s{idx}__{参数code}` 只是*名义上*行=样品，schema 对"`parameters[]`/`sample_count` 谁是样品"无强约束，作者完全可把样品建成列、参数建成行（types.ts:207 注释"行列倒置使用矩阵（参数当行）"即承认此用法）。故**原始记录的样品轴也是变量**，转置与否取决于**两端**，不是一个变量。

  **统一模型（同一条规则套两端）**：
  | | 样品轴来源 |
  |---|---|
  | **原始记录模板** | 新增按钮 `DataMatrixConfig.sample_axis: 'row' \| 'col'`（作者显式声明，唯一事实来源） |
  | **报告模板** | 从"试样编号绑在顶部表头 / 左侧表头"**推导**（不再手动 radio） |

  判定：`原始.sample_axis === 报告样品轴` → **直接拉**；不同 → **自动转置**。画布回显一行人话（如「识别：试样编号在顶行 → 样品按列展开；原始记录样品在行 → 自动转置」）。

  **底层不变 / 不影响取数正确性**：数据始终按 `(样品, 参数)` 键存取，永远能取到正确值；转置本质是**排版方向**而非"会不会取错数"。`sample_axis` 的两个真实作用：① 让原始记录矩阵按作者意图方向**显示**（col 时参数为行、样品为列）；② 作为报告侧绑定时的**参照系**（`BandCellPicker` 骨架按此方向摆，点"试样编号表头"直观、自动转置有明确两端输入做 XOR）。

  **影响文件（预估）**：
  | 改动 | 文件 |
  |---|---|
  | `DataMatrixConfig` 加 `sample_axis?:'row'\|'col'`（缺省 'row'）；报告侧 `band.axis` 由手选改为推导结果（保留为缓存字段或移除） | `shared/types.ts` |
  | 原始记录矩阵编辑器加"行是样品 / 列是样品"按钮 + 转置显示 | `client/.../MatrixEditor/MatrixCanvas.tsx`、`MatrixEditor/index.tsx` |
  | `sample_axis='col'` 时转置渲染原始记录矩阵 | `shared/typst-generator.ts`（`embedDataMatrixTypst`） |
  | 样品带展开方向改为读"两端轴"自动判 | `shared/typst-generator.ts`（`expandResultTableBand`） |
  | 去掉"按样品出行/出列"radio，改"试样编号放哪→推导"+ 状态回显 | `client/.../MatrixEditor/ReportResultTableCanvas.tsx` |
  | `BandCellPicker` 骨架按原始 `sample_axis` 方向摆 | `client/.../ReportEditor/BindingPickerModal.tsx` |

  **要防的坑**：① 报告侧在左列和顶行**都**放了试样编号 → 歧义，picker 限制只能放一处（或后放覆盖前放 + 警告）；② 一个试样编号都没放但确实想重复 → 回退（保留"一键生成整表样品带"便捷入口）。

  **兼容**：`sample_axis` 缺省 'row'，存量原始记录矩阵显示/出片不变；存量报告 `band.axis` 显式值优先、缺省回退推导。

  **分期建议**：P-Map-13a 原始记录矩阵编辑器地基（类型字段 + 新建表默认 + 配色 + 布局 + 改名）；P-Map-13b `sample_axis` 切换按钮 + 画布/PDF 转置显示 + 报告侧推导（删 radio）+ 状态回显 + 自动转置判定 + `BandCellPicker` 方向对齐 + smoke（row/row 直拉、row/col 转置、col/col 直拉、col/row 转置四象限）。

  **改名决策（2026-06-22 用户拍板）**：数据矩阵 / 报告结果表域里「样品 → 试样」（送检样品=样品、试件=试样，只改矩阵/结果表这一域，**委托单物理样品域不动**）；仅改用户可见中文文案，不改 `sample` 代码标识/source_type 字符串。

  - ✅ **P-Map-13a（已实现 2026-06-22）——原始记录矩阵编辑器地基**：① `DataMatrixConfig.sample_axis?:'row'|'col'`（缺省 'row'，注释说明"底层 s{idx}=试样恒定、仅排版方向"，13a 不消费、为 13b 铺路）；② **新建试验数据表默认骨架**（`field-types.ts` DEFAULT_MATRIX）＝试样 1/2/3 行（`default_sample_labels:['1','2','3']` 表头只标数字）+ 参数列 + 「平均值」汇总列（`per_row_aggregate` average，每行对各参数求均=重复测量取均值）+ 「备注」汇总行（`input_text`），之后任意改；两处建表入口都深拷贝新数组；③ **配色区分**（`MatrixCanvas.tsx`）＝试样数据格/试样轴表头蓝(`C_SAMPLE`/`C_SAMPLE_HDR`)、其他行紫(`C_OTHER`，与试样蓝拉开)、汇总橙(`C_SUMMARY`)、每列统计绿(`C_AGG`)、单元格公式青、选中蓝边 + 表下方图例 `LegendItem`，落实"严格区分可变试样数据"；④ **增删按钮收进表格上方统一工具栏**（试样行/参数列/汇总列▾/汇总行▾/其他行▾），表内不再散落 +按钮（删掉表头 +列/+汇总列 th、+试样行 行、底部 +其他行/+汇总行 行及其对齐占位列），精确"前后插入"保留在表头双击/右键卡（左插/右插/上插/下插）并加提示；按钮文案随 `sample_axis` 出"试样行/列"；⑤ **改名** 样品→试样（`MatrixEditor/*` 全量中文文案 + `section-presets.ts` 试验结果 hint；委托样品字段"样品描述/样品照片"保留）；⑥ **移除自动统计（用户决策 2026-06-22）**：去掉编辑器创建"每列统计"(`per_column_aggregate`)入口 + `addAggregateRow` + `configureSummaryFormula` 的聚合分支；新建表「平均值」列由 `per_row_aggregate`→`input_number`（手填，需算时自行配公式）；所有计算统一走**手动公式**。`per_column_aggregate`/`per_row_aggregate` 标 `@deprecated`、仅保留**渲染/展平兜底**兼容旧模板（不破坏 `record_summary` 报告绑定——它按 row_id 读值、与是否聚合无关）。三分类（试样/其他/汇总）行列均为手填可选类型（文本/数字/选择），「其他列」对偶概念归 13b。client tsc 0 + vite build 通过；server 仅存量 `mock-data.ts` rootDir 噪声。⚠️ 浏览器交互（工具栏新增/精确插入/配色观感）待实测。
  - 🟡 **P-Map-13b（进行中）——方向开关本体**：
    - ✅ **13b-1（已实现 2026-06-22）——试样轴开关 + PDF 转置**：原始记录矩阵工具栏加「试样排布 行/列」`Segmented`（写 `sample_axis`）；试样为列时编辑器上方横幅说明"编辑按行、出片转置、看右侧预览"；`embedDataMatrixColAxisTypst` 实现 PDF 转置——参数→行、试样→列、汇总列→底部行、汇总行→右侧列（逐列其他行=每参数一格、跨列汇总行=跨参数 rowspan 单值），**取数键与行模式逐一对应**（存储/绑定/报告零改）；多级参数分组/行分组场景 gate 回退行模式。client tsc 0 + server tsc 0（仅 mock-data 噪声）+ vite build；**smoke 实测**：row/col 两朝向均编译通过，col 版 PNG 实测试样成列、限值/备注成右列（备注 rowspan）、平均值成底部行、版式正确。⚠️ 编辑画布本身暂不转置（按行编辑），交互式画布转置见 13b-2。
    - ✅ **13b-2（已实现 2026-06-22，待浏览器实测）——交互式画布转置 + 开关上移**：① 试样为行/列开关从 `index.tsx` 基本设置 Form 移到 `MatrixCanvas` **表格正上方醒目蓝条**（`Segmented` 试样为行↓/为列→）；② 新增 `renderColTable()`——`sample_axis='col'` 时画布真转置（试样为列头、参数为行头、汇总列→底部行、汇总行/其他行→右侧列；跨列汇总=右列 rowspan 单格；配置卡 openCfg 复用同套 'row'/'param'/'sumrow'/'sumcol' 句柄，标题/插入/删除/公式齐全；数据格 `renderDataCell` 行列共用）；③ **行模式 JSX 原样保留**，col 模式独立分支（`colMode` 仅在无多级参数分组/无行分组时启用，与 PDF gate 一致），零回退风险；④ 增删按钮文案随轴（试样行/列、参数列/行），其他行/汇总行/汇总列文案不变（按用户要求）。client tsc 0 + vite build。⚠️ 画布转置 + 各配置卡点击流**无法自测**，需浏览器实测；转置画布暂不含列宽/行高拖拽把手（次要，后续补）。
    - ✅ **13b-X（已实现 2026-06-22）——四类汇总/其他模型修复（不随轴转置）**（回应用户：汇总列变逐行、其他行变列）：确立**四类固定为可视行/列、不随试样轴转置**——汇总行/汇总列=跨（整行/整列一个值）、其他行/其他列=逐（每可视格手填）；只有试样×参数核心网格转置。① `MatrixSummaryColDef.per_row?`（缺省/ true=其他列逐行、false=汇总列跨行，与 summary_row.per_column 对偶；存量无字段=其他列、行为不变）；② 新增「其他列」+「汇总列(跨行)」工具栏入口（汇总行/汇总列=跨、其他行/其他列=逐）；③ 展平 `collectMatrixSummaryDataKeys` 轴感知（逐格按当前可视轴键：行模式其他行↔参数/其他列↔试样，列模式互换）+ 汇总列跨行单键 `__sumcol__{id}`（新 `matrixSummaryColSpanFlatKey`/`matrixVisibleColEntities`/`matrixVisibleRowEntities`）；④ PDF 行渲染器加汇总列跨行 rowspan；PDF 列渲染器 `embedDataMatrixColAxisTypst` 汇总改为**不随转置**（汇总行/其他行=底部行、汇总列/其他列=右侧列，与行模式同位置）；⑤ 画布 `renderColTable` 同步翻回 + 行表汇总列跨行 + 图例。**smoke 实测**：row/col 四类 PNG 版式均正确（其他列右侧逐行、汇总列右侧跨行 rowspan、其他行底部逐试样、汇总行底部跨试样）；client tsc 0 + server tsc 0 + vite build。⚠️ **录入侧 FormRenderer 未改**：新「汇总列跨行单值」录入(应 1 格)、列模式逐格录入/转置录入仍待做，需浏览器实测——见 13b-3'。
    - 🟡 **13b-3'（待实现）——录入侧对齐 + 列格公式**：FormRenderer 支持汇总列跨行单值录入 + 列模式转置录入 + 逐格按可视轴；给其他列/汇总列格子开放单元格公式（满足"平均值手动配公式只取某几列"）。
    - ✅ **13b-4（已实现 2026-06-22）——报告侧朝向跟随原始轴 + 改名**：**设计调整**：原方案"删 radio + 从试样编号位置推导"——但底层按 `(试样,参数)` 键存、报告朝向本是自由排版（不存在取错数），故**保留 radio、改为"默认跟随原始 `sample_axis` + 显示原始轴提示"**（自动正确 + 保留显式覆盖，比删 radio 稳）。① 动态结果表选镜像矩阵时 `orientation` 默认跟随该矩阵 `sample_axis`（试样为列→报告默认按试样出列）；试样朝向旁加 `Tag` 显示「原始记录试样为行/列」+ 不一致提示；② 静态试样带 radio 旁加「原始记录试样为行/列 → 建议按试样出行/列」提示，回应用户"选试样带时方便选行/列"；③ **改名 样品→试样**：`ReportResultTableCanvas.tsx` 全量（委托样品来源注释除外）、`BindingPickerModal` 试样带/当前试样、`typst-generator` 结果表默认「试样」表头 + 「暂无试样数据」（委托单/order_samples/结论汇总表的"样品"＝委托物理样品，保留）。client tsc 0 + server tsc 0 + vite build。⚠️ `BindingPickerModal` BandCellPicker 内零散"样品"标签（与委托域同串）保留细修；报告渲染 col 朝向为既有能力、未改。
    - 🟡 **P-Map-13c（草案待评审，2026-06-22）——引导式「试样带」结果表（走 A：增强静态 band）**：

      **动机（用户）**：报告项目模板检测结果表希望能"选试样按行/列；试样轴=试样编号递增、另一轴=自己加的参数；行/列表头交叉的格子逐个从原始记录试样带选内容；并自动识别是否转置"。

      **关键事实（决定实现方向）**：底层数据按 `(试样, 参数)` 键存（`record_cell_sample` 取"当前试样"某参数值），所以报告只要选定自己的试样朝向、每格按 `(试样i, 参数p)` 取值即可——**不管原始记录试样是行还是列都自动对位，不存在"取反/取错"，无需手动转置算法**。用户说的"需要转置"由键存模型自动处理。

      **走 A（增强现有「静态 + 试样带」band 模型）**：相比动态镜像(报告参数=原始参数 1:1)，静态 band 支持**逐格自定义映射**（报告侧参数名可≠原始、可混固定格），最贴合用户"自己加参数、格子逐个选"。现有 band（`result_table.band{axis,matrix_code,ref_id}` + `record_cell_sample`/`record_sample_label`/`record_sample_index`）已具备能力，**痛点是配置 UX 太绕**（手动：开朝向→选哪行/列是带→给带头绑试样序号→逐格绑参数）。13c = 把它做成引导式，不新增数据键。

      **引导流程（目标体验）**：
      1. **朝向开关**「试样按行 / 按列」(写 `band.axis`)，**默认跟随镜像矩阵的 `sample_axis`**（延续 13b-4）。
      2. 选定后**试样轴自动出「试样编号」头**（自动绑 `record_sample_index`，1/2/3 递增），无需手动绑。
      3. **另一轴「+ 加参数」**：填报告侧表头名；其试样带格**默认绑 `record_cell_sample`**，用户只在下拉里选"对应原始记录哪个参数"（逐格可改 → 报告参数可≠原始参数）。
      4. 画布即时提示「数据自动按试样对位，无需手动转置」+「原始记录试样为行/列」。

      **影响文件（预估）**：`ReportResultTableCanvas.tsx`（band 面板改引导式：朝向默认、自动试样编号头、+加参数、逐格选原始参数）；`BindingPickerModal.tsx`（`record_cell_sample` 选参数体验，复用 BandCellPicker）；`shared/typst-generator.ts` `expandResultTableBand`（已支持行/列两朝向，确认 + 必要微调）；**无需新增数据键/类型**（复用 band + record_cell_sample/record_sample_index）。

      **兼容**：复用既有 band 结构与来源，存量静态/动态结果表不受影响；动态镜像模式保留为"参数 1:1 一键成表"的便捷入口。

      **分期**：13c-1 引导面板（朝向默认 + 自动试样编号头 + +加参数 + 逐格选原始参数）；13c-2 画布对位提示 + smoke（报告行/列 × 原始行/列 四象限均自动对位正确）。⚠️ 报告画布交互无法自测，需浏览器实测。

      - ✅ **13c-1（已实现 2026-06-22）**（`ReportResultTableCanvas.tsx`）：① 朝向切换(关/按试样出行/列)时**试样轴自动绑「试样编号」**——出行→样板行 col0 格绑 `record_sample_index`、出列→样板列列头 `label_binding` 绑 `record_sample_index`（合并进一次 `update`，避免状态覆盖）；② 新增「**+ 试样带参数**」按钮（`addBandParam`）：出列加一参数行/出行加一参数列，其试样带格子**默认绑 `record_cell_sample`**（取镜像矩阵首参数，点格经 BandCellPicker 可改对应原始参数）；③ 引导文案改为「试样轴已自动出试样编号；+加参数；**数据按试样自动对位、无需手动转置**（原始试样行/列都不影响）」；朝向默认跟随原始 `sample_axis`（13b-4）。复用既有 band/record_cell_sample/record_sample_index，**无新键**；渲染端 `expandResultTableBand` 行/列两朝向为既有能力（P-Map-10）。client tsc 0 + vite build。⚠️ 报告画布点击流/出片对位需浏览器实测。
      - ✅ **13c-2（已实现 2026-06-22，待浏览器实测）——切换朝向整表转置(回应用户"按行/列时表头不转置")**：`transposeGrid()` + band 朝向 `onChange`：从一种朝向切到另一种时**整表数据转置**——`columns↔rows`、格子 `rowId↔colId`、合并跨度 `colspan↔rowspan`、`summary_rows↔summary_cols` 互换，再按新朝向重挂试样编号；编辑器(及出片，因数据一致)随朝向翻转。**对偶不完全**(已注释)：列头绑定(label_binding 等)转成行后丢失(行无表头绑定)、`per_column` 汇总简化为整列单值——故试样编号自动重挂、参数名/普通格保留；合并格转置后跨度互换需复核。另：`BindingPickerModal` BandCellPicker 残留"样品序号/样品名称/样品数"等改为"试样"。client tsc 0 + vite build。⚠️ 报告画布点击 + 切换转置 + 出片需浏览器实测;复杂表(多汇总/合并格)转置后需检查。
      - 🟢 **目标模型（用户确认 2026-06-22，对齐原始记录四类）**：报告结果表试样带改为**结构化四类**——**试样轴**(朝向，试样编号自动占左上角)/ **参数**(试样带数据，随朝向转置)/ **其他**(逐参数手填、逐格可绑任意来源)/ **汇总**(跨行列单值，逐格可绑)；**转置只作用于"试样×参数"核心，其他/汇总留在原位不转**。两图为据：按列=参数行+试样列+右侧汇总/其他列；按行=试样行+参数列+右侧汇总/其他列。开放几何细节：同一"平均值"按列时是逐参数列(其他列)、按行时是跨试样行单值(汇总列)——非自动跟随，按朝向显式加。
      - ✅ **13c-2 修正2（已实现 2026-06-22）——切换朝向"按规范结构重建"(替换硬转置)**：硬转置会把"试样编号列+试样带"误当普通行/列翻过去,导致**按列结构错乱**(用户图8:试样编号既是列头又成了行)。改为 `extractBandParams`+`rebuildBand`：切换朝向时**提取当前带里的参数(record_cell_sample 格)**,按新朝向铺**规范结构**——按行=「试样编号列(格=试样序号)+各参数列+样板行」;按列=「试样编号列(格=参数名)+试样序号带列(向右展开)+各参数一行」;**汇总(summary_*)原样保留、不转**。⚠️ 只保留参数+试样编号,杂项列/合并格/per_column 汇总格转置后或需重配。首次启用(off→轴)保留现有结构、仅挂带(非破坏)。client tsc 0 + vite build。⚠️ 报告画布需浏览器实测。
      - ✅ **13c-2 修正3（已实现 2026-06-22）——报告结果表加「行头」编辑(按列参数行可编辑/绑定)**：回应"转按列后结果表头不能编辑了"。结果表原本是列头中心、行无表头。新增：① 类型 `result_table.rows += label_binding/note/note_binding`、`columns += row_header?:boolean`；② `rebuildBand` 按列时把参数放成**行**(行头列 `nameCol.row_header=true`)、参数名/单位绑定从原列头**带过来**(不丢)；③ 画布：行头列的格子渲染为**行头**(行名 + `HeaderConfigCard` 右键改固定文字/上下移/删除 + 点击下方面板绑名称/备注，`row_h` 目标，与列头同款)；④ 渲染端 `renderReportResultTableTypst`：行头列格子渲染行名(`resolveHeaderSlot(row.label_binding)` + note)，`expandResultTableBand` 列展开保留行头列、带列头展开成试样序号。**smoke 实测**：按列 band 结果表 PNG 正确——试样编号(角)|试样序号1/2/3(列头)|平均值/客户要求/结论(右列)，「结果(g/cm³)」为行头。client+server tsc 0 + vite build。⚠️ 画布点击流待浏览器实测。
      - ✅ **13c-3（已实现 2026-06-22）——「其他列」+ 四类统一 + 结构工具栏上移**（回应"两类汇总统一成 其他/汇总、三类按钮移表格上方、像原始记录"）：① **其他列**：`summary_cols += per_row?+cells?`(镜像 summary_rows.per_column)——per_row=true=其他列(逐数据行每格绑定,新 `sum_col_cell` 目标 + getSumColCell/setSumColCell)、false=汇总列(跨行单值);渲染端 `renderReportResultTableTypst` 汇总列分 per_row 逐行/跨行 rowspan。② **统一四类**：底部"汇总行(整行一个值)/(按列分别填)"→ 顶部「汇总行(跨列)/其他行(逐列)/汇总列(跨行)/其他列(逐行)」,与原始记录矩阵同模型。③ **结构工具栏上移**：表格上方加「试样带参数/数据行/参数列/汇总行/其他行/汇总列/其他列」,删底部按钮(表头内联 +列/+汇总列 暂留作快捷)。**smoke 实测**：按列 + 其他列(逐参数 ≥85/≥3000)+ 汇总列(跨行 不符合)PNG = 用户图2 版式正确。client+server tsc 0 + vite build。
      - 🟡 **13c-5（进行中 2026-06-22）——表头单击选中 + 操作栏（用户方案）**：① 删试样带面板「镜像矩阵/选数据矩阵」选择框(绑定靠点格、矩阵自动取关联记录首张) + 删重复的「试样带参数」按钮(顶部工具栏已有)；② **单击表头=选中**(`toggleTarget`,再次单击/点别处取消)→ 表格上方出**操作栏**(删除 + 移动；列头=左/右、行头=上/下；汇总/统计=删除)，复用既有选中(同时驱动下方绑定面板)；③ **顶部"+"按钮按选中态插入**：选中列头→其右插、选中行头→其下插、未选→末尾(addCol/addRow/addBandParam)；④ **汇总/统计 行列也接入操作栏**(回应"只有参数列有左右移动"):`moveSumCol`/`moveSumRow`——汇总列/统计列(均 summary_cols)左/右移、汇总行/统计行上/下移；addSumCol/addSumRow/addSumRowPerCol 按选中汇总表头`insertAfterSel`(选中汇总列→其右插)；⑤ **插入识别"格子选中"**(回应"选中参数行点列还在最右"):`selRowIdx/selColIdx`——点中表头**或该行/列里的数据格**都算选中,addRow/addCol/addBandParam 据此在所在行/列旁插入(之前只认点表头、点格子退化成末尾)。client tsc 0 + vite build。
      - ✅ **13c-6（已实现 2026-06-22）——非试样带「自动序号列」**：列加 `auto_index?:boolean`；列配置卡(右键)动作「设为自动序号列(1,2,3…)」切换；该列每行**自动出行号 1,2,3…**(无需逐格绑定)——画布显示数字 + 「自动序号」标签,渲染端 `renderReportResultTableTypst` 自动序号列格子 = `String(ri+1)`。用于不使用试样带时试样编号列自动编号。smoke 实测渲染 [1][2][3] 正确。client+server tsc 0 + vite build。工具栏按三类(参数｜统计｜汇总)分组排列。**未做(下一步)**：删「操作」列 + 给所有行普遍化行头(非试样带表在「试样编号」下加行头列,否则无行头的行删不掉)+ 原始记录矩阵同款操作栏。⚠️ 报告画布点击流需浏览器实测。
      - ✅ **13c-7（已实现 2026-06-22）——绑定弹窗/卡片打磨（用户4项）**：① **「字面量」→「自定义」**(全系统文案:BindingPickerModal/BindingEditor/FieldPropsPanel/ReportResultTableCanvas)；② **合并「矩阵单元格」+「矩阵汇总行」两 tab 为单一「数据表格」**：`MatrixCellPicker` 加 `mode='both'`(数据格绿+汇总格黄同表皆可点;点数据格→record_cell、点汇总→record_summary),`isVisible/initialTab/onChange` 新增 `record_table` 键;③ **试样序号**仍在独立「试样带·当前试样」(record_band)tab、不进数据表格,且 auto_index 列免绑定——已满足"分开绑定";④ **列宽/行高数字旋钮进配置卡**:列配置卡 InputNumber 列宽(fr,所有列)、行头配置卡 InputNumber 行高(pt,即按列模式行头行);拖拽把手保留。client+server tsc 0 + vite build。⚠️ 非试样带行的行高需待"普遍化行头";绑定/卡片交互需浏览器实测。
      - ✅ **13c-8（已实现 2026-06-22）——序号/表头模型重做（用户确认表）**：**Part A**：统计列/汇总列配置卡也加列宽 InputNumber(updateSumCol width);去掉表头上的列宽文字显示(只在卡片里调)。**Part B 序号**：① 新建结果表**默认含灰色「序号」列**(`auto_index`,首列;开关保留=列配置卡「设为自动序号列」);② **序号列可手改**——格子=灰底可编辑输入,默认占位 `ri+1`、手填存 literal 覆盖、清空恢复默认(渲染端 auto_index 优先取 literal 覆盖否则 `ri+1`);③ **试样带序号(record_sample_index)在编辑器渲染为灰色自动**(数据格"1,2,3… 自动" + 按列 band 列头"序号 1,2,3… 自动")、**不再显示为可点绑定**(消除"还需要绑定"困惑;PDF 仍按录入试样自动展开 1..N);④ 参数表头默认"参数N"可改(rebuildBand 已设)。规则对齐用户表:非band=可改字面序号、band=自动序号,均无需数据来源绑定。smoke 验证序号列默认 [1][3]+手填覆盖 [2a]。client+server tsc 0 + vite build。⚠️ 画布交互需浏览器实测。
      - ✅ **13c-9（已实现 2026-06-22）——序号收敛到试样带 + 拖拽全移除（用户3项）**：① 删「设为自动序号列」卡片动作;**试样带序号(record_sample_index)表头不可编辑**——`isSampleIdx`(按列 bandCol label_binding=record_sample_index / 按行 idxCol 的 band 行格=record_sample_index)走灰色纯展示 th(无配置卡/无 toggle),rebuildBand 把序号列改名「试样序号（自动设置）」;② 新建结果表默认**去掉 auto_index 序号列**(field-types 两处)——非试样带表恢复**全可编辑表头**、无自动序号;③ 列宽/行高**只在配置卡数字旋钮**调:移除数据列/汇总列/行 三处拖拽把手 + 清理 drag/rowDrag/frOf/cmOf state。client+server tsc 0 + vite build。⚠️ 非试样带行的行高暂无控件(待普遍化行头);画布交互需浏览器实测。
      - ✅ **13c-10（已实现 2026-06-22）——修两个回归**：① **auto_index 彻底无效化**——之前默认建的表里残留的 auto_index 序号列仍显示自动 1,2,3;现移除编辑器 + typst 两处 auto_index 特判 → 这些列变回**普通可编辑列**(表头可改、格子普通绑定),非试样带表不再有自动序号(smoke 验证渲染 `[—]` 空格非 1/2/3);② **列宽/行高旋钮挪进弹出卡片**——根因:旋钮原放在 HeaderConfigCard 的 `children`(Popover 触发器=表头常显);新增 `extra` prop 渲染在弹出表单内(确定键上方),列/汇总列/行三处旋钮改用 `extra` → 只在右键卡片里出现、不再显示在表头。client+server tsc 0 + vite build。
      - ✅ **13c-11（已实现 2026-06-22）——序号检测精确化（修"所有表头都变自动序号"）**：旧启发式(label_binding/band 行格)疑似在遗留表上误判,导致普通表头被当序号、不可编辑。改为**显式标记 `sample_index`**(rebuildBand 给序号列打:按行 idxCol、按列 bandCol),列头检测只认 `sample_index===true || label_binding=record_sample_index`(后者仅试样带列才有、精确不误判);去掉"按行 band 行格"启发式。结果:**非试样带表行/列表头全可编辑、无序号**;按行=idxCol 序号(行表头自动)+参数列可编辑;按列=bandCol 序号(列表头自动)+参数行头可编辑。**遗留表**:旧表可重新切一次试样朝向以补 `sample_index` 标记。client+server tsc 0 + vite build。
      - ✅ **13c-4（已实现 2026-06-22）——三项打磨**：① **参数按钮按轴**：试样带激活时报告工具栏参数按钮轴感知、单一——按列出=「参数行」、按行出=「参数列」(addBandParam)；② **其他→统计 改名 + 区分**(both 原始记录 MatrixCanvas + 报告 ReportResultTableCanvas)：「其他行/列」→「统计行/列」；**统计(逐行/列)=紫(`C_STATS`#f9f0ff,与原始记录其他统一)+ Tag「统计」**、**汇总(跨)=橙 + Tag「汇总」**，类别/颜色彻底分开(原报告里统计错显示成"汇总"且同色)；③ **表头内联 +列/+汇总列 按钮 + 其对齐占位列彻底移除**(表头/数据行/统计/汇总行的占位格同步删，非 per_column 汇总行 colSpan 减 1)，表格只显示表格、所有新增在上方工具栏。仅编辑器侧改动(渲染逻辑未变)；client+server tsc 0 + vite build。⚠️ 报告画布点击流/对齐需浏览器实测。
      - ✅ **13c-12（已实现 2026-06-22）——三模式表头规范化（修"表头完全错误"）**（`ReportResultTableCanvas.tsx`，回应用户：不使用试样带/按试样出行/按试样出列三种表头错乱）：确立三种模式的**规范表头**——① **不使用试样带**：普通静态表，列头与行头都是普通可右键编辑的数据表头(无灰色/锁定)；② **按试样出行**：左上角列「**试样编号**」=普通可编辑角标列(此前误建成灰色 `sample_index` 列、标题硬写「试样序号（自动设置）」并泄进 PDF 列头)、试样序号落在**行格**(`record_sample_index`→灰色 1,2,3…作各行行头)、「试样编号」右侧各列=可编辑参数列；③ **按试样出列**：「试样编号」角 + 右侧试样序号带列(列头自动 1,2,3… 灰)、下方各行=可编辑参数行头。**实现**：a) `rebuildBand('row')` 的 idxCol 去掉 `sample_index`、label 改「试样编号」(渲染端 `c.label` 直出、不再泄注解)；b) 三档 radio `onChange` 统一——关→`unband()`(把带内参数还原成普通列、恢复全可编辑)、关→带/带→带 一律走 `rebuildBand`(此前关→带走"只打标记非破坏"分支、不重建规范结构是错乱根因)；c) `extractBandParams` 加**无带分支**(从现有普通数据列提参数，首次启用试样带保留用户已建列)；d) `rebuildBand` 加空表兜底(取镜像矩阵首参数作默认参数占位)。client tsc 0。⚠️ 报告画布三模式切换/出片对位需浏览器实测；切模式按规范重建会丢手建的合并格/杂项格(沿用 13c-2修正2 既有语义)。
      - ✅ **13c-13（已实现 2026-06-22）——不使用试样带的表默认带「试样编号」行头列 + 默认 3 行**（回应用户：非试样带表丢了试样编号列）：非试样带（关）模式下，结果表最左固定一列「**试样编号**」=`row_header:true` 行头列（每行行头默认 `1/2/3`、可右键编辑），默认 **3 行**；用既有 `row_header` 原语（画布行头渲染 + `typst-generator` 行头列渲染均已支持，无新键、无渲染改动）。落点：① `field-types.ts` 两处 `report_result` 默认 `result_table`（建字段/转换）prepend `{id:'col_sn',label:'试样编号',row_header:true}` + rows 改 `1/2/3` 三行（去掉 vestigial `is_conclusion/conclusion_col_id`——全代码无消费，结论取自"每行最后一列"）；② `ReportResultTableCanvas.tsx` 内联兜底默认同步；③ `unband()` 还原普通表时也最左加「试样编号」行头列 + 默认 3 行（1/2/3）、带内"当前试样"相对绑定丢弃（无带语境无意义，逐格重绑）。`extractBandParams` 无带分支本就跳过 `row_header` 列，故从默认表启用试样带时「试样编号」自动成序号轴、不会被当参数。client tsc 0。⚠️ 画布/出片需浏览器实测。
      - ✅ **13c-14（已实现 2026-06-22）——删表格最右「操作」列**（回应用户：操作列意义不大）：移除结果表画布最右侧仅编辑器用的「操作」列（表头 th + 每数据行 td[行号/上移/下移/删除/行高] + 汇总行占位 td 三处）。行的**增删/上下移/行高**改走既有入口：① 顶部工具栏「数据行/参数行」按选中位置插入；② 右键「行头」(试样编号/参数行)弹 `HeaderConfigCard`→上移/下移/删除本行 + 行高 InputNumber；③ 单击行头→上方选中操作栏移动/删除。前提是**每表都有行头列**(13c-13 非试样带默认「试样编号」、按列 nameCol、按行单样板行无需行管理)。列数三处同步 -1，汇总行 colSpan 不受影响（本就按 `columns.length-1`）。client tsc 0。⚠️ 画布交互需浏览器实测。
      - ✅ **13c-15（已实现 2026-06-22）——删除「动态·矩阵驱动」结果表模式 + 全部代码**（回应用户：该功能无用，连「结果表模式」切换一并删）：彻底移除 P-Map-1/2/3 的动态（矩阵驱动）结果表——结果表只保留**静态网格 + 试样带**一条路径。落点四文件：① `shared/types.ts` 删 `result_table.source`（整段 `{mode:'matrix', matrix_code, orientation, columns, conclusion, summaries…}` 类型）；② `shared/typst-generator.ts` 删 `renderReportResultTableTypst` 里 `source?.mode==='matrix'` 分发 + 整个 `renderMatrixResultTableTypst`（151 行）+ 清理仅它用的 import `matrixSummaryColCellFlatKey`；③ `shared/binding-integrity.ts` 删 `collectReportBindings` 的 `rt.source` 表头收集块 + `validateReportBindings` 的 P-Map-3 动态 source 校验块 + 文档注释项；④ `ReportResultTableCanvas.tsx` 删「结果表模式」`Segmented`(静态逐格/动态·矩阵驱动) + 整个 `{isDynamic && …}` 动态配置面板(镜像矩阵/朝向/参数勾选/结论/汇总/结构预览，约 200 行) + 全部动态派生 state(`src/isDynamic/setSource/setMode/dynMatrix/dynParams/dynSumCols/includedCodes/colMetaOf/materializeCols/toggleParamCol/setColMeta/dynSumRows/toggleSummary/conclVal/setConclVal`) + `!isDynamic` 外层包裹(改为恒静态) + 清理 unused import(`Empty/Select/Checkbox`)。`matrixFields` 保留(试样带选镜像矩阵/自动绑「当前试样」仍需)。client tsc 0 + server tsc 0(仅存量 mock-data rootDir 噪声) + vite build 通过。**兼容**：存量若有 `source` 模板，`source` 字段被忽略、走静态 columns/rows 渲染(动态表本就行空、属用户认可的废弃功能)。⚠️ 画布需浏览器实测。
      - ✅ **13c-16（已实现 2026-06-22）——表头已填固定文字在绑定处直接显示 + 弹窗「自定义」预填**（回应用户：汇总列表头双击/右键卡填了「平均值」，下方绑定面板「名称」却只显示灰占位「固定文字（右键表头编辑）」，要点进弹窗确认才显示）：① **面板显示**（主修复）：`ReportResultTableCanvas.tsx` 表头绑定面板「名称/备注」行未绑时，从恒显灰占位改为——有固定文字则直接显示「固定文字：**平均值**」、仅真空才显灰占位；`getHeaderFixedText(part)` 读当前 `cfg` 的 `label`/`note`，卡片/双击改名 `update()`→重渲染即自动反映、无需点进弹窗。② **弹窗预填**：统一绑定弹窗 `value` 兜底由 `{source:'literal', text:''}` 改为 `{source:'literal', text: getHeaderFixedText(hdrPart)}`，选「自定义」即带出已填名称（`BindingPickerModal` 本就 `setDraft(value)`+输入框 `value=draft.text`，无需改弹窗）。仅未绑时生效；已有 binding 仍回显该 binding。client tsc 0。
      - ✅ **13c-17（已实现 2026-06-22）——数据矩阵加「试样数据 / 统计数据」用途开关，统计表不出试样带**（回应用户：① 不同表格在单位/绑定处表头雷同——查 DB 实为两张表列配置完全相同[试验数据 vs 数据统计 都是 col_1..col_4 同名同单位]，是"统计表被建成试样表克隆"而非选择器 bug；② 试样带只能抓试样表、统计表抓不到）：`DataMatrixConfig.kind?:'sample'|'stats'`（缺省 sample，存储不变、仅 UI 试样语义 + 报告是否提供试样带）。落点：① `shared/types.ts` 加 `kind`；② `MatrixEditor/index.tsx` 顶部「表格用途」下拉(切 stats 同时清 `sample_axis`)；③ `MatrixCanvas.tsx` `kind==='stats'` 隐藏「试样为行/列」蓝条、改显紫色"统计数据表"提示；④ `ReportResultTableCanvas.tsx` 试样带的 `matrixFields` 过滤掉 `kind==='stats'`（统计表不进试样带；只剩统计表时面板提示"请在『数据表格』tab 逐格绑定"）；⑤ `BindingPickerModal.tsx` 数据表格 picker 矩阵下拉给 stats 表标「（统计）」。统计数据走既有逐格绑定 record_cell（按行索引）/record_summary，渲染端无需改。client tsc 0 + server tsc 0 + vite build。⚠️ 画布/出片需浏览器实测；统计表新建行名仍默认「试样 N」(可改)、编辑器画布内"试样行"等文案未逐处改名(功能不受影响)。
      - ✅ **13c-18（已实现 2026-06-22）——绑定摘要文案打磨（用户）**：① 试样带格子摘要 `record_cell_sample`/`record_sample_label` 及画布小标签、band tab、提示文案「当前试样」→「**所有试样**」(更准确表达"按试样带全部展开")；② `record_cell` 单格摘要「行N」→**行名**——`BindingSummary` 从 `mf.matrix.default_sample_labels[sample_idx]` 取原表格行标签(无则回退「{前缀} N」、无关联记录回退「行N」)，显示成「数据统计 · 平均值 · 弯曲强度」而非「… · 行3 · …」。纯显示层，绑定值/渲染不变。client tsc 0 + vite build。
      - ✅ **13c-19（已实现 2026-06-22）——移除「合并单元格」功能（用户：前端 Shift 框选始终不可用）**：`ReportResultTableCanvas.tsx` 删除合并工具条（「合并选区/取消合并」按钮）+ 顶部说明里的合并条目 + Shift 框选选区交互（`selAnchor`/`selRange`/`onCellClick` 的 shiftKey 分支）+ `mergeSelection`/`unmergeSelection`/`selCount`/`selectedIsMerged` + `covered`(useMemo) + 数据格的 `colSpan/rowSpan/inSel/merged` 渲染 + `MergeCellsOutlined`/`SplitCellsOutlined` import。**保留**：`cells[].colspan/rowspan` 类型与 `typst-generator` 的合并渲染（兼容存量，但前端不再产生 span）；汇总列跨行 `rowSpan={rows.length}`、汇总行跨列 `colSpan` 是结构性的、与用户合并无关，**未动**。检查结论：合并仅存在于本结果表画布，数据矩阵(MatrixCanvas)与录入(FormRenderer)本就没有用户级单元格合并。client tsc 0 + vite build。
      - ✅ **13c-20（已实现 2026-06-22）——「试样序号（自动设置）」列/行可点设列宽/行高（用户：现在灰色不可选）**：`ReportResultTableCanvas.tsx` 把试样带的序号格从"灰色纯展示、不可点"改为**可点开一个轻量 `Popover`，只含列宽(按列 bandCol)/行高(按行 band 行格)旋钮**——序号/名称仍自动、不可改文字/绑定。① 按列 `isSampleIdx` 列头：`cursor:pointer` + `Popover`「试样列宽（应用到所有试样列）」`InputNumber(fr)→updateCol(width)`；② 按行 `record_sample_index` 行格：`Popover`「试样行高（应用到所有试样行）」`InputNumber(pt)→updateRow(height)`。因所有试样列/行由同一条模板列/行经 `expandResultTableBand`（`...tmplCol`/`...tmplRow` 带 width/height）展开，故改一次即应用到全部。client tsc 0 + vite build。
      - ✅ **13c-21（已实现 2026-06-22）——汇总列/统计列/汇总行表头：绑了名称就显示绑定值（修"仍显示统计/汇总"）**：`ReportResultTableCanvas.tsx` 画布里汇总列(`sumCols`)与汇总行(`sumRows`)表头此前恒显静态 `sc.label`/`sr.label`（缺省「统计/汇总」），忽略 `label_binding`。改为**有 `label_binding`（字段/自定义）就渲 `<BindingSummary>`、否则才显固定标签**（与数据列头同款），类型 Tag「统计/汇总」保留。纯编辑器显示层——PDF 端 `renderReportResultTableTypst` 本就 `resolveHeaderSlot(sc.label_binding) ?? sc.label`、出片正确，只是画布没跟上。**补**：选中表头时上方操作栏/绑定面板标题(`targetTitle`)也加 `headerName(item, fallback)` helper——绑了名称(自定义→文字 / record_field→字段名 / record_header·cell·summary→参数名)就显绑定值，否则回退固定 label；数据列/行/汇总列/汇总行/各格标题统一走它。client tsc 0 + vite build。
      - ✅ **13c-22（已实现 2026-06-22）——术语文案优化（用户：试样带/按试样出行列/手填抓取 太抽象）**（纯 UI 文案，代码标识 `band`/`record_cell_sample`/`literal`/`binding` 全不变）：① **「试样带」→「试样自动展开」**（结果表面板标题/Tooltip）；绑定摘要绿 Tag「试样带」→「试样」；画布带内小标签「试样带·所有试样」→「所有试样」；弹窗 tab「试样带·所有试样」→「所有试样」；「（试样带）」标题→「（所有试样）」；记录矩阵编辑器相关 Tooltip 同步（MatrixCanvas/index.tsx）。② **试样带方向「按试样出行 ↓/按试样出列 →」→「每行一个试样 ↓/每列一个试样 →」**（含"建议…"提示）。③ **文本字段来源（`FieldPropsPanel`）：「手填」→「自定义」、「抓取」→「映射」、「抓取来源」→「数据来源」、「手填内容」→「自定义内容」**（radio value `manual/fetch` 不变）。涉及 `ReportResultTableCanvas`/`BindingPickerModal`/`FieldPropsPanel`/`MatrixCanvas`/`MatrixEditor/index`。client tsc 0 + vite build。
      - ✅ **13c-23（已实现 2026-06-22）——选中表头操作栏标题「统计/汇总」前缀按类型显示（修 bug）**：`targetTitle` 里 sum_row/sum_col/sumrow_h/sumcol_h/各格的前缀此前**写死「汇总行/列」**，选中【统计行/列】(per_column/per_row)时仍显示「汇总」。改为按 `per_column`(行)/`per_row`(列)标志取「统计行/列」或「汇总行/列」。区别：**汇总＝跨（整行/列一个值，row 跨数据列 colspan、col 跨数据行 rowspan）；统计＝逐（每个数据列/行一格、各自绑定，如各列平均值、各试样判定）**。client tsc 0 + vite build。

- ✅ **P-Map-14（已实现 2026-06-23）——首页样品三件套绑定 + 样品信息表 + 首页数据来源裁剪**（回应：样品名称/零件号/样品编号要能在首页绑、单样品直显多样品出表、首页来源只留三类）：
  - **样品三件套（独立来源 + 单样品直显）**：样品信息**单独成一类数据来源**——**新 source `report_sample`**（key `name`/`sort_no`/`model`），概念上取自【报告编号接口 1.2】而非委托单(1.1)（用户更正）。`CellBinding` 加该 source + `ReportRenderCtx.report_sample`；`resolveBinding` 加 case；服务端 `buildReportTypst` 算 `coverCtx.report_sample`（**单样品=该样品值、多样品=顿号连接兜底**），`coverCtx.order.sample_name` 仍设为该名（兼容存量 order.sample_name 绑定）；`BindingPickerModal` 加独立「样品信息」tab（样品名称/样品编号/零件号 → `report_sample.*`）+ `BindingSummary`/`initialTab` 对应 case。**未** 塞进委托单字段。
  - **样品信息表（多样品自动表）**：新字段 `report_sample_table`（`editors:['report-cover']` 可添加），`sample_table.{columns,column_labels,col_widths}`；`renderSampleTableTypst` 从 `ctx.order_samples`（扩展带 `sort_no/model/barcode`）列 序号/样品名称/零件号、**`#block(width:100%)` 满宽**、单元格走 `escapeTypstMarkup`（样品序号含 `#`）；锚点 `__REPORT_SAMPLE_TABLE__`；InstanceEditor `SampleTableEditor`（自动预览 + **单样品黄色提示改用直接字段** + 列名可改）；FieldPropsPanel 模板期配列名。
  - **首页数据来源裁剪**：`CellBindingButton` 对首页(linkedRecord=null)传 `allowedSources=['literal','order','report_sample','report_meta']`——只留 自定义/委托单字段/**样品信息**/报告字段；`order_samples` tab 删「排版」控制（换"多样品请用样品信息表"提示）。
  - smoke：样品表 1#/9# 含 `#` 转义后编译 200 PDF；`order.sample_model`→PP-T20 解析正确；client+server tsc 0。⚠️ 浏览器交互待实测。

**兼容**：全部新键可选，存量模板无 `*_binding`/`record_header` → 走静态字符串、出片逐位不变。

> 评审通过后按 modify-system 五阶段分期实现，回本节标「✅ 已实现」。

---

## 十、自由表格 + 语义标记 + 存储模型 + 项目模板继承（🟡 设计待评审 2026-07-13）

> 承接用户两个诉求：① **所有编辑器表格像 Excel 一样自由**（自定义几行几列、自由合并单元格、表头做成可选）；② **项目模板编辑器初始改造**——直接继承原始记录的字段 + 已配好的映射，在其上编辑而非从零开始。并回答一个核心存储问题：**矩阵表格该不该从现在的「类 DataFrame」改成「每格打散、各自带表头」的存法。**

### 10.1 核心矛盾：完全自由 ↔ 自动映射 / 动态取数 / 公式 / 合规

Excel 的本质是「一堆没有语义的格子」——系统不知道每一格是什么。而本系统的最大卖点恰恰依赖「系统知道每一格是什么」：

| 能力 | 依赖系统知道 |
|---|---|
| 报告自动拉取原始记录值 | 哪一格是「数据」、要被哪条绑定取 |
| 录入期样品数可变、报告自动展开 | 哪一行/列是「样品维度」 |
| 公式 / 限值自动判定 | 抓哪一格算、写到哪一格 |
| 按锁定版本可复现（合规） | 每一格有稳定身份可回放 |

**结论：不能做成「纯 Excel、完全无约束」**——否则上述能力全断。可行解是 **「自由布局 + 语义标记」**：外观尽量自由，但底层保留一层「这格/这轴是什么」的语义标记，映射与动态取数靠**标记**而非**位置/文本**。

### 10.2 决策：把表格拆成三层（身份 / 值 / 版式）

| 层 | 存什么 | 谁用 |
|---|---|---|
| **定义层（身份）** | 参数定义（稳定 `code`+label+unit）、**样品轴标记**、样品实例有序列表（稳定 id） | 绑定、公式、动态展开 |
| **值层** | 每格的值，按 `(样品id, 参数code)` 定位（本就是打散的稀疏字典） | 存取 |
| **版式层** | 合并/跨格(span)、多级表头分组、额外杂项行列、样式 | 展示 / 出片 |

现状的 `DataMatrixValue`（`sample_ids[]` + `parameters[]` + `cells{sid__param}`）**已经有了定义层 + 值层**；任务一真正缺的是**版式层**（合并、自由行列、可选表头）——它应作为**叠加在语义网格之上的表现层**，而不是把语义网格推翻。

### 10.3 动态样品：为什么按「标记样品轴 + 稳定 id」而不是「按表头文本匹配」

用户设想：每格存自己的行/列表头，样品数可变时「找到表头是样品的那些格」。**这个方向要谨慎**——用**表头文本**当匹配键是经典反模式：
- 表头会被**改名**（模板期/录入期都可能）、会**重名**（两列都叫「厚度」）、会**合并**（合并表头文本不是干净的样品身份）；
- 样品本就是录入期动态新增的，它的表头「试样3」是**显示**、不是**身份**。
- 一旦用户改了样品表头，"找样品格"的逻辑就全错；公式/绑定/版本回放也失去稳定锚点。

**更好的做法（也是系统已实现的「试样带」P-Map-10/13c）**：
1. 模板期**标记某一轴为样品轴**（band，方向 行/列）——这正是用户直觉里「标记哪些是样品行/列」，**是必需的、不是可选**；
2. 样品实例是**有序 id 列表**，录入期新增样品＝追加一个稳定 id；
3. 绑定写成**相对**的——「取矩阵 M **每个**样品的参数 P」，而不是「第 3 个样品」；
4. 出报告时按**实际录入的样品数**把这一行/列展开成 N 份（`expandResultTableBand`）。

即：**样品数可变 = 显式样品轴 + 稳定 id + 相对绑定 + 渲染期展开**，N 在渲染时从真实数据得出，全程不碰表头文本。**这个机制已经能用**，任务一要做的是把「标记样品轴」接进更自由的表格，而不是换存储去按文本找。

### 10.4 存储模型分析（直接回答「每格打散 + 各自带表头」是否更好）

| 方案 | 评价 |
|---|---|
| **A. 现状（类 DataFrame：轴归一化 + cells 稀疏字典）** | **干净矩形表最优**：表头存一处（改名一处改）、样品轴显式、公式/绑定按稳定 code/id、体积小。**弱点**＝装不下自由合并 / 不规则 / 多级表头（任务一）。 |
| **B. 用户设想（每格打散 + 各格存表头文本 + 按文本找样品）** | **不推荐**：① 表头文本被复制进每一格＝反归一化，改名要改所有格、易不一致；② 按文本匹配样品脆弱（改名/重名/合并即崩）；③ 丢掉公式/绑定/回放要的稳定锚点。 |
| **C. 建议（每格打散，但带「稳定语义坐标 + 角色」，表头仍归一化）** | **任务一要做就走这个**：每格 = 位置(r,c) + 跨格(span) + **角色**(data/header/label/summary) + 数据格再带**语义引用**(param `code` / sample `id` / 「属样品轴」标记)；表头/轴定义仍存一处、合并表头 = 一个带 span 的表头格。既能表达自由/合并表格，又保住动态样品(按 id)、绑定、公式、可复现。 |

**一句话结论**：
- **只要还是干净的「样品×参数」表（绝大多数原始记录就是），现状 A 更好**，且它的试样带已经把动态样品处理得比「按表头文本找」更稳。
- **只有当任务一「真正的自由/合并表格」成为硬需求时，才值得往「打散」走——但要走 C（稳定语义坐标 + 角色标记），不是 B（表头文本 + 按文本匹配）。**
- **无论如何都不要把表头文本当匹配键**：显示文本可变，身份必须用稳定 id/code/标记。

### 10.5 项目模板继承原始记录（任务二）（✅ 已实现 2026-07-15）

**落地**：`shared/report-inherit.ts` 的 `buildProjectGroupsFromRecord(record)`（纯函数，前后端共用）+ `ProjectEditor` 顶栏「从原始记录拉取」按钮 + `List` 新建对话框「从关联原始记录拉取（默认勾选）」+ 空模板醒目横幅（三处入口，走同一转换器 + 整体替换）。转换规则：普通值字段→`record_field` 恒等绑定；语义角色字段（主检/审核/日期）→`record_meta`；`spacer`→原样保留；`free_grid`→结构镜像 + 录入格转逐格绑定 + 自引用样品带转 `source_field` 报告带（详见 §10.7 与 README §五 free_grid 行）；**`data_matrix` 样品表→报告 `report_result_table` 试样带一键成表**（试样编号列 + 每参数一列 + `band`，与 `ReportResultTableCanvas.rebuildBand` 同构，渲染期 `expandResultTableBand` 按记录实际样品数展开）；统计表(`kind='stats'`)/无参数矩阵、`image`/`device_ref` 列入「需手动」清单。**取数注意**：关联记录若无审核通过版本（存量常见，`current_version_id` 为 null），拉取回退到最新草稿字段结构（`client/utils/recordTemplateLink.ts`）。守住了下面两个前提：**独立快照复制、非 fork**（`setTemplate` 只改本模板 groups，走既有 `PUT /:id` 保存，绝不建母子关联）；原始记录后改字段靠 `validateReportBindings` 实时告警（已扩展校验新绑定源 + `source_field` 带）。**新增能力·动态试样**：报告 free_grid 的 `source_field` 样品带在 `expandFreeGridBand` 里按记录该 free_grid 实际录入的试样数（`__sample_count__::bandId`）自动展开——录入期增减试样，报告不改模板自动跟随。**data_matrix 也已纳入自动映射**（2026-07-15）：`convertDataMatrix` 把样品矩阵转成报告 `report_result_table` 的试样带一键成表（试样编号列 + 每参数一列 + 一条样板行 + `band{axis:'row',matrix_code,ref_id}`，带内 `record_sample_index`/`record_cell_sample`），与 `ReportResultTableCanvas.rebuildBand('row')` 同构，渲染期 `expandResultTableBand` 按记录实际样品数展开；统计表（kind='stats'）/无参数列仍进「需手动」。**结果表行高 `cell_inset_y` 对齐原始记录（记录 data_matrix 用 Typst 默认 5pt、结果表渲染器缺省 10pt，故拉取时显式设 5pt 使二者一致，回应用户"拉取后行高变了"）**。**文档样式统一（2026-07-16）**：`buildProjectGroupsFromRecord` 返回 `DEFAULT_PROJECT_THEME_CONFIG`（取自「弯曲强度&弯曲模量」模板：字体 FangSong_GB2312 / 字号 10.5 / 字段间距 line_gap 1.9 / 段距 1.6 / 分区左对齐加粗 / 宽页边距），拉取（`ProjectEditor.handlePullFromRecord`）与新建（`List.handleCreate`）都写进 `layout_options.theme_config`；结果表 `label_gap=12pt`（备注/标题与表格距离，同弯曲模板）。

方向对、建议做，能大幅减少映射配置量。两个必须守住的前提：
1. **要「转换器」，不能机械 1:1 照搬**：普通字段 → 恒等绑定（`record_field`）；矩阵 → 报告的**试样带结果表**（复用 P-Map-13c 一键成表），而不是把录入网格原样塞进报告（两者版式差别大，正是用户点出的难点）。若原始记录表格已按 10.2 标记了样品轴/数据格，**这些标记随导入带过来**，自动生成正确的试样带绑定，动态样品天然可用。
2. **必须「独立复制」，绝不建 fork/母子关联**（对应 #22）：否则母版（原始记录）审核通过会 `syncToChildren` 强推字段、冲掉报告版式。导入 = **一次性快照**，之后各自演进；原始记录后改字段，靠 `validateReportBindings` **实时告警**提醒工程师手改，而不是自动强推。代价要讲清：改了原始记录模板，报告要重新导入或按提示手改。

> **任务一与任务二是同一块地基**：任务二能自动生成「正确映射」，前提是任务一在原始记录表格里做了样品轴/数据格标记——两者应一起规划，标记层是共同基础。

### 10.6 历史教训 & 建议路径（避免重蹈覆辙）

- **13c-15**：曾实现「动态·矩阵驱动结果表」，后**整套删除**，收敛为「静态网格 + 试样带」。→ 说明「全动态」复杂度高、团队已主动回退；不要以「重写存储」的名义把它变相请回来。
- **13c-19**：曾实现「合并单元格」，因前端框选一直不可用**被删**（底层 `colspan/rowspan` 类型 + 出片渲染保留）。→ 合并的**渲染底座还在**，缺的是**可用的编辑交互**；任务一的合并应在此基础上补交互，而非新造存储。

**建议**：**增量扩展，不要推倒重写**。在现有语义网格上：① 复活/接好合并的编辑交互（底层已支持 span）；② 加「版式层」（自由杂项行列 + 多级表头 + 样式）作为叠加；③ 保持样品轴显式标记 + 试样带动态展开。只有当增量确实撑不住"真正自由表格"时，再评估迁到 10.4-C 的打散存储（需完整迁移 record_data + 展平/绑定/公式/出片全链路，属 L 级，务必先小范围验证）。

### 10.7 方向确定（2026-07-14，用户选 A）：以 `free_table` 统一网格为主模型

用户决定走「**统一网格 + 保留自动能力**」——所有格子平等、表头也是格子、自由合并；同时**保留**报告自动从原始记录取值 + 录入样品数可变自动展开。**本节取代 10.2–10.6 的「在类型表上增量加合并」路线**（P1 报告结果表合并只作过渡验证、路线上可弃）。这不是推倒重写，而是**长在已有的 `free_table` 上**。

**关键：统一网格已存在** —— `free_table`（`types.ts:506`）+ 渲染 `renderFreeTableTypst`（`typst-generator.ts:1889`，任意格子 + 自由合并含表头）+ 类型表→网格转换 `buildFreeTableFromField`/`buildResultFreeTable`（`typst-generator.ts:2494/2406`）+ 实例编辑器 `FreeTableBlock`（`InstanceEditor.tsx:1766`）。它已是「所有格子平等、任意合并」的网格，只是①只在**报告实例阶段**、②格子**纯文本无绑定**、③**不展开样品**。补这三点即为主模型。

**数据模型演进（全部加法、向后兼容）** —— 在 `free_table` 上增（把"类型行列"溶成"格子开关"）：
```
free_table {
  columns, rows,
  cells:  Record<`${rowId}::${colId}`, string>,   // 现有：纯文本字面量
  spans:  Record<key, {colspan,rowspan}>,          // 现有：自由合并（含表头）
  // 新增（每格可选属性）：
  cell_bindings?: Record<key, CellBinding>,        // 该格从原始记录/接口取值（＝原「数据格」）
  cell_formulas?: Record<key, Formula>,            // 该格＝公式，按格子坐标(A1式)引用（＝原「统计/汇总行列」）
  header_cells?:  Record<key, true>,               // 该格是表头：加粗 + 跨页 repeat（＝原「表头结构」）
  sample_bands?:  Array<{ id; axis:'row'|'col'; refs: (rowId|colId)[];   // 唯一保留的语义：该行/列随样品重复展开
                          matrix_code?; source_field?; source_band_id? }>, // 报告侧驱动源：matrix_code(data_matrix) 或 source_field(记录 free_grid，项目模板拉取时生成)；记录侧两者皆空=自引用
}
// 新增绑定源（CellBinding）：record_free_cell{field_code,cell_key}=记录 free_grid 某固定格；
//   record_free_cell_sample{field_code,cell_key}=记录 free_grid 样品带某格·当前样品（expandFreeGridBand 逐样品落地）。
```
- **表头** = 打 `header_cells` 的普通格，随便合并。
- **统计/汇总** = 挂 `cell_formulas` 的普通格，不再是特殊行列。
- **数据格** = 挂 `cell_bindings` 的普通格。
- **样品动态** = 唯一保留的标记 `sample_band`：渲染/录入按实际样品数把该行(或列)展开成 N 份，带内绑定按「第 i 个样品」解析（复用 `expandResultTableBand` 思路）。**这是想要"动态样品 + 自动取值"就删不掉的一个标记**（见 10.3/10.4）。

**渲染**：`renderFreeTableTypst` 取值优先级改为 `resolveBinding(cell_bindings[key]) ?? evalFormula(cell_formulas[key]) ?? cells[key]`；渲染前先按 `sample_band` 展开网格。录入端(FormRenderer)与出片端仍共用同一份网格 + 同一 resolve/公式口径（合规逐位一致）。

**并存/迁移**：`free_table` 置位即覆盖类型渲染（`types.ts:504` 注释已是现成开关）；`buildFreeTableFromField` 能把类型表**一键转网格**作种子。故老类型表继续可用、不强制迁移、不动合规出片老路径；新建/改造走统一网格。分期见 `编辑器改进方案.md §20.5`（F0–F5）。

> 评审通过后按 modify-system 五阶段分期实现，回本节标「✅ 已实现」。
