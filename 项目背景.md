# 化学部检测报告系统 v3 — 项目背景

> 配套文档（已归档于 `docs/`）：`docs/背景/系统技术方案_v3.md`
> 受众：项目相关方（业务 / 开发 / 评审 / 新加入成员）
> 阅读时长：约 15 分钟

---

## 一、项目所在的真实业务场景

### 1.1 客户与部门

- **甲方**：广电计量
- **使用部门**：材料部（第一期覆盖材料部，逐步推广到化学部）
- **业务性质**：第三方检测服务，给汽车主机厂（奇瑞、捷途、东风等）、电子电器厂等做合规性检测
- **典型业务**：热变形温度、力学性能、成分分析等

### 1.2 一份检测业务的真实生命周期

```
  客户委托 →  样品送检 →  实验室分项检测 →  填写原始记录 →
  审核原始记录 →  生成检测报告 →  报告审批 →  交付客户
```

每一份业务都对应**两份核心文档**：

| 文档 | 用途 | 受众 |
|---|---|---|
| **原始记录** | 实验过程的全量数据存档 | 内部审核 / 法规审计 |
| **检测报告** | 给客户的最终结论 | 客户 / 监管 |

这两份文档**不是同一份的简化或扩展**——原始记录字段多而细（包括环境条件、设备编号、操作员、误差范围等），报告字段少而精（结论、关键值、签字）。两者之间存在**字段映射 + 公式计算**的关系。

### 1.3 当前实际工作方式（系统化前的痛点）

实验室工程师和文员目前的工作模式：

```
1. 工程师在 Excel 模板里手填数据 → 打印 → 签字 → 归档
2. 文员把 Excel 数据手抄到 Word 报告模板里 → 打印 → 走签字
3. 出错就推翻重来
4. 有公式（如平均值）→ 工程师手算 / Excel 函数算 → 抄到报告
5. 不同客户报告格式不同 → 文员手工调样式
6. 标准/法规更新 → 模板要逐一改 → 改不过来
```

**问题**：
- **慢**：一份报告平均 1-2 小时（包括手抄数据 + 调格式）
- **错**：手抄、手算、手调样式都容易错
- **重复**：相同的数据在原始记录和报告里都要填一遍
- **不可追溯**：手写记录没法做版本管理
- **难规模化**：客户增多 / 标准变化 → 工作量爆炸式增长

---

## 二、项目目标

### 2.1 总目标

**把"实验数据 → 原始记录 → 检测报告 → PDF 交付"的全链路数字化、结构化、模板化**，让一份业务从录入到出报告的时间从 1-2 小时缩短到几分钟，且差错率降低一个数量级。

### 2.2 具体目标

| 目标 | 量化 |
|---|---|
| 报告生成时间 | 1-2 小时 → < 10 分钟 |
| 数据录入差错 | 手抄错误 → 0（仅录入错误，可追溯）|
| 模板维护工作量 | 每次法规变更人改几十份 → 改 1 个 Typst 包，全部生效 |
| 存量模板入库 | 几千份 → 2-3 周脚本 + 工程师微调 |
| 系统上线时间 | 化学部 6 月可用，全员推广 8 月底 |
| 用户学习成本 | 工程师/文员 0 代码、0 命令行 |

### 2.3 非目标（明确不做）

- ❌ 不做客户对接系统（只对接化学部内部系统）
- ❌ 不做审批系统（化学部已有审批系统，我们只交 PDF + 接收打回信号）
- ❌ 不做仪器自动采集（数据由人录入；仪器接入是后期可选项）
- ❌ 不做"画模板编辑器"（这是金现代的路，我们不走）
- ❌ 不做客户报告查询门户（不在第一期范围）

---

## 三、项目要实现的核心能力

### 3.1 三大能力

```
┌──────────────────────────────────────┐
│  能力 1：原始记录数字化               │
│  - Excel 存量模板自动提取字段          │
│  - 工程师可视化编辑模板（不改 Excel）  │
│  - 实验室人员在 Web 录入数据           │
│  - 实时计算公式（平均/判定/换算）       │
│  - 输出 PDF 原始记录                  │
└──────────────────────────────────────┘
                ↓
┌──────────────────────────────────────┐
│  能力 2：检测报告自动生成              │
│  - 公司提供报告样张 → Typst 模板       │
│  - 文员可视化配置字段映射              │
│  - 实验数据 → 报告字段自动填充          │
│  - 复杂公式（多源/跨项目）支持           │
│  - 输出 PDF 报告                      │
└──────────────────────────────────────┘
                ↓
┌──────────────────────────────────────┐
│  能力 3：模板治理                     │
│  - 几千份存量模板 2-3 周入库            │
│  - 模板版本管理 + 回滚                │
│  - 公司级排版规范统一                 │
│  - 临时字段 + 审核流程                │
└──────────────────────────────────────┘
```

### 3.2 核心交互模式：「左编辑右渲染」

这是整个系统的**标志性体验**，在 5 个不同页面被复用：

```
┌─────────────────────────┬─────────────────────────┐
│                         │                         │
│  左：编辑区              │  右：Typst PDF 预览       │
│  （字段、表单、映射）     │  （所见即所得）          │
│                         │                         │
│  改一下                 │  实时刷新                │
│  ─────                  │  ─────                  │
│  改一下                 │  实时刷新                │
│                         │                         │
└─────────────────────────┴─────────────────────────┘
```

复用场景：
- 原始记录模板编辑器（工程师）
- 实验室录入页（实验室人员）
- 报告模板编辑器（文员）
- 报告查看（任何人）
- 模板源码调试（管理员）

### 3.3 用一份业务的完整数据流串起来

```
Step 1  外部系统/接口推送委托单 + 样品 + 项目列表
        ↓
Step 2  系统按测试项目挑出对应的「原始记录模板」（已入库）
        ↓
Step 3  实验室人员打开模板 → 录入数据（左侧表单）
          - 录入: t1=85, t2=86, t3=84
          - 自动算: avg = 85, 结论 = 合格
          - 右侧实时看 PDF 预览
        ↓
Step 4  实验数据存数据库（raw_data + derived_data）
        ↓
Step 5  文员选「报告模板」（已入库）
        ↓
Step 6  系统按字段映射规则（已配置）
          - 报告字段 "测试温度" ← 实验数据 "avg_temp"
          - 报告字段 "客户名" ← 委托单字段
        ↓
Step 7  Typst 渲染 PDF
        ↓
Step 8  PDF 提交化学部审批系统（外部）
        ↓
Step 9  审批通过 → 完成；打回 → 通知对应角色修改
```

---

## 四、项目的技术挑战与难点

### 4.1 难点矩阵

| 难点 | 严重程度 | 解决思路 |
|---|---|---|
| 几千份存量模板如何快速入库 | ⭐⭐⭐⭐⭐ | 字段提取脚本 + 80% 自动 + 工程师微调 |
| 报告格式专业、印刷级保真 | ⭐⭐⭐⭐⭐ | Typst 渲染（不用 docx）|
| 工程师 / 文员不会写代码 | ⭐⭐⭐⭐ | 可视化编辑器 + 12 个预设公式 |
| 原始数据 vs 报告数据的映射关系 | ⭐⭐⭐⭐ | 字段映射可视化 + 中间映射层 |
| 计算公式复杂多变 | ⭐⭐⭐ | 12 个预设公式 + 自定义表达式兜底 |
| 中文字体 / 排版 | ⭐⭐⭐ | 思源宋体 + 公司级 Typst 包 |
| 实时编译性能 | ⭐⭐ | 服务端 Typst CLI + 防抖 + 缓存 |
| 模板版本变化与历史数据兼容 | ⭐⭐⭐ | 版本号锁定 + raw_data 重算 |
| 与化学部审批系统对接 | ⭐⭐ | PDF 上传 + 回调接口 |
| 临时字段不污染主模板 | ⭐⭐ | 提议-审核流程 + 版本号 |

### 4.2 难点详解

#### 难点 1：存量模板入库（最大挑战）

**现状**：
- 化学部 + 材料部加起来有 **几千份**现存模板
- 大部分是 Excel/Word，少量是纸质扫描件
- 格式各异，命名混乱，部分模板已废弃但未清理

**问题严重性**：
- 如果让人一份一份建：1 人 1 天建 5 份，所有人不干别的，要建到明年
- 竞争对手金现代就卡在这——他们的可视化编辑器"画到 8 月"才能用
- **谁先把存量入库谁就赢**

**解决思路**：
```
Excel 文件 → 字段提取脚本（Layer 1 规则 + Layer 2 LLM）
           → 输出结构化 JSON（80% 自动正确）
           → Typst 代码生成器
           → 入库
           → 工程师在可视化编辑器里微调剩余 20%
```

**关键洞察**：我们不画模板，我们**提取**模板。`画模板的过程根本就不应该存在`。

#### 难点 2：专业级 PDF 渲染

**现状**：
- 检测报告是法律文件，**格式必须专业、整洁、印刷级**
- 客户对样式有严格要求（字体、字号、表格、签字位置）
- 标准/法规变更频繁，模板要能快速跟上

**为什么不用 Word/docx**：
- docx 模板填值容易乱（长内容撑爆、表格错位）
- docx 改样式 LLM/AI 不友好（XML 太复杂）
- LibreOffice 转 PDF 在中文场景偶发字体问题

**为什么不用 LaTeX**：
- 学习成本极高，工程师/文员搞不懂
- 编译慢（实时预览受不了）
- 中文配置麻烦

**为什么不用 HTML+Puppeteer**：
- LLM 友好但工程师/文员不会写
- 复杂表格 CSS 不好控制

**最终选 Typst**：
- 编译速度是 LaTeX 的 5%（实时编辑可行）
- 语法极简，比 LaTeX 简单 10 倍
- 纯文本，AI 友好（未来 LLM 编辑可加）
- 中文支持好（思源宋体）
- 现代排版引擎，PDF 印刷级

#### 难点 3：用户 0 代码

**用户画像**：
- **实验室工程师**：懂化学/材料，不懂代码，习惯 Excel
- **文员**：会用 Word，不会编程
- **审核员**：偶尔登录，看 PDF 为主

**约束**：
- 不能让用户写 Typst 代码（虽然 Typst 很简单，但还是代码）
- 不能让用户写公式（虽然 Excel 公式简单，但很多文员怕错）
- 不能让用户配置 JSON / YAML（这是开发文化，不是业务文化）

**解决方案**：
```
所有交互可视化
  字段拖拽（react-dnd）
  公式选下拉（12 预设）
  映射点点（自动匹配 + 手工连）
  排版选模式（vertical / inline / table）
```

用户**永远不写代码**，永远不看 JSON。

#### 难点 4：原始数据 → 报告的映射

**问题**：
- 原始记录字段名（工程师起，如 `t_avg`）≠ 报告字段名（客户/法规规定，如 `result_temp`）
- 一份报告可能引用多个测试项目的数据
- 同一个值可能要多次出现在报告不同位置
- 需要计算转换（单位换算、中英文映射、N.D. 处理等）

**传统做法**：
- 让开发改代码：每来一个新模板，改一次 `shapeXxx()` 函数
- 不可持续，开发成瓶颈

**v3 解法**：
- 中间映射层：报告占位符 ←→ 数据来源（系统做翻译）
- 文员可视化配置（左数据源 + 右映射列表）
- 自动匹配 80%，手工补 20%

#### 难点 5：公式的灵活性 vs 安全性

**用户需求**：
- 平均值、总和、阈值判定（80% 场景）
- 偶尔需要复杂逻辑（嵌套 if、多源组合）

**安全顾虑**：
- 不能让用户写 `eval()` 风格的任意代码（注入风险）

**解法**：
- 12 个预设公式覆盖 95% 场景（参数化，零代码）
- 兜底：自定义表达式用 `expr-eval` 沙箱（仅四则 + 比较，无 eval）
- 公式可保存到"公司公式库"复用

### 4.3 系统级挑战

#### 数据建模复杂

动态字段 + 计算字段 + 临时字段 + 版本管理 + 多语言模板，要在 PostgreSQL JSONB 里灵活存储，又要查询性能可接受。

**解决**：
- 主键固定列（template_id / version / 关联 ID）
- 动态部分用 JSONB（field_definitions / raw_data / mappings）
- 关键字段加索引（GIN on JSONB）

#### 实时编译性能

左编辑右预览要求 < 1 秒延迟，否则体验受损。

**解决**：
- Typst CLI 服务端编译（300-500ms）
- 前端防抖（500ms 内多次改只编一次）
- 服务端缓存（同源码不重复编译）
- 未来可上 Typst WASM（浏览器内编译，0 延迟）

#### 组件复用

TypstViewer 在 5 个页面用，FieldEditor 在 2 个页面用，FormulaEditor 在 2 个页面用。组件设计不当 → 维护噩梦。

**解决**：
- 严格 props 接口
- 单元测试覆盖
- Storybook 可选（第二期）

---

## 五、项目的核心创新

### 5.1 思路上的创新

| 维度 | 传统方案（金现代）| 我们的方案 |
|---|---|---|
| **建模板** | 人工画几千份 | 脚本提取 + 微调 |
| **数据流** | Excel → 手抄 → Word | 结构化录入 → 自动映射 |
| **格式控制** | 编辑器画 | Typst 包统一 |
| **样式更新** | 改每份模板 | 改 1 个包，全局生效 |
| **公式** | 人工算 / Excel | 系统计算 + 拓扑排序 |

### 5.2 工程上的创新

- **TypstViewer 组件**：5 个页面共用一个组件，6 处复用证明设计的可扩展性
- **FormulaEditor 跨场景**：原始记录和报告共享同一个组件 + 同一个执行引擎
- **公司级 Typst 包**：把样式抽成可复用的函数库，模板只关心数据
- **机器学习兜底**：存量模板自动比对调整（机器学习 + Typst 迭代）

### 5.3 业务上的创新

- **速度战胜灵活**：金现代要画到 8 月，我们 6 月上线
- **结构战胜手工**：他们做手工业，我们做流水线
- **入侵打通**：业务流（建单/客服/检测/报告）打通的优势让客户难以替换

---

## 六、项目的边界与风险

### 6.1 项目边界

```
本系统负责
  ├─ 实验室原始记录管理
  ├─ 检测报告生成
  └─ PDF 交付到化学部审批系统

本系统不负责
  ├─ 化学部审批流程（他们有自己系统）
  ├─ 客户报告下载（他们有自己门户）
  ├─ 仪器自动采集（人录入为主）
  └─ 法规标准库（业务方维护）
```

### 6.2 主要风险

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| 业务方需求频繁变化 | 高 | 中 | 契约式白名单，锁死调整范围 |
| 存量模板格式过于诡异 | 中 | 高 | 80%/20% 兜底（自动+人工）|
| Typst 中文渲染瑕疵 | 低 | 高 | PoC 阶段验证，字体一次性配好 |
| 金现代发起价格战 | 中 | 中 | 用速度战，不打价格战 |
| 化学部审批接口对接难 | 中 | 中 | demo 阶段 mock，上线前一个月才必须接通 |
| 用户不愿放弃 Excel 习惯 | 中 | 中 | 老流程保留作为"惩罚选项" |
| 团队对 Typst 不熟悉 | 低 | 低 | 学习成本约 1 周，文档多 |

### 6.3 不可接受的失败

以下任何一项发生即项目重大失败：

- 6 月化学部上线时**没有可用的存量模板**（哪怕只有几份样本）
- PDF 渲染**中文乱码**或**格式严重错乱**
- 实验室录入数据**丢失**
- 报告字段**填错**（导致客户索赔）

---

## 七、项目时间线

```
4 月        v1 方案探索（docx 路线，已废弃）
5 月初      v2 方案（老师指导，Typst + 契约论）
5 月中      v3 方案（用户深化，提取 + 可视化）← 当前
5 月底      PoC 阶段（5 项关键技术验证）
6 月初      化学部小规模上线
6 月中-下   反馈迭代 + 存量模板批量入库
7 月        全面优化 + 性能调优
8 月        全员推广
9 月+       扩展材料部 + LLM 辅助编辑（可选）
```

---

## 八、项目的核心团队角色

| 角色 | 职责 |
|---|---|
| 业务方（化学部）| 提供存量模板、报告排版规范、审批接口 |
| 老师 / 顾问 | 战略决策（Typst 选型、契约论方法）|
| 开发者 | 实现 v3 方案、PoC 验证、demo 开发 |
| 用户（实验室工程师）| 第一期试用、反馈 |
| 用户（文员）| 第一期试用、反馈 |

---

## 九、为什么这个项目值得做

### 9.1 业务价值

- **直接收益**：化学部每月 X 千份报告，单份节省 1 小时 → 年节省人力 N 千小时
- **间接收益**：差错率降低 → 客户满意度 → 续约率
- **战略价值**：业务流打通后，竞品难替换；可复制到材料部、其他事业部

### 9.2 技术价值

- **Typst 生产应用**：国内较早的 Typst 大规模应用案例
- **可视化模板编辑器**：可复用到其他文档生成系统
- **字段映射引擎**：可复用到任何数据驱动的文档系统
- **机器学习兜底**：模板对齐迭代，是新颖的应用

### 9.3 个人/团队成长

- 完整经历"从战略思考到工程落地"
- 接触前沿技术（Typst、可视化编辑器、LLM 辅助）
- 解决真实业务难题（存量数据、用户习惯、性能）

---

## 十、一句话项目定位

> **让化学部从"手填表 + 手抄报告"的工厂式作业，升级为"录入数据自动出报告"的流水线式作业，且每一步可追溯、可版本管理、可法规适配。**

这不只是数字化，是**业务流的根本性重塑**。

---

## 附：相关文档清单

| 文档 | 内容 |
|---|---|
| `docs/背景/系统技术方案_v3.md` | 完整技术方案（22 章）|
| `docs/背景/化学部报告格式及系统方案讨论.md` | 与老师的原始讨论记录 |
| `项目背景.md`（本文档）| 业务背景 + 难点 + 价值 |

---

## 附：关键术语表

| 术语 | 含义 |
|---|---|
| 原始记录 | 实验室检测过程的全量数据存档（PDF / Excel）|
| 检测报告 | 给客户的最终结论文档（PDF / docx）|
| 委托单 | 客户的检测请求订单 |
| 测试项目 | 一类检测，如 ELV、石棉、PAHs、热变形 |
| 字段定义 | 一份模板包含哪些字段、什么类型、单位等 |
| 字段映射 | 报告占位符 ←→ 数据来源（实验数据 / 委托单 / 系统字段）|
| 公式 | 数据计算规则（平均/总和/阈值/换算等）|
| Typst | 现代化的排版引擎，本系统的渲染基石 |
| Typst 包 | 公司级排版规范的代码封装 |
| 拓扑排序 | 计算字段按依赖顺序执行的算法 |
| 临时字段 | 实验室录入时临时新增的字段（不污染主模板）|

---

这就是项目的全貌。再具体的实现细节请看 `docs/背景/系统技术方案_v3.md`。
