# 递归智能报告系统部署与迁移说明

> 文档版本：2026 年 9 月 19 日  
> 适用范围：本机 mock 演示、内网服务器 Docker 部署、离线部署、数据库和上传资料迁移。

## 1. 交付内容

清理后的项目由以下运行内容组成：

| 路径 | 用途 |
|---|---|
| `client/` | React 前端源码 |
| `server/` | Express 服务端源码 |
| `shared/` | 前后端共用的数据类型、公式、表格和报告逻辑 |
| `db/` | PostgreSQL 迁移脚本 |
| `config/` | 应用内部默认配置。部署人员不需要在这里切换环境 |
| `deploy/system.env` | **唯一需要维护的 mock/服务器部署配置** |
| `deploy/docker/` | Docker Compose、数据库初始化和应用入口 |
| `seed-data/` | 当前数据库和上传资料的可移交快照 |
| `fonts/`、`typst-packages/` | PDF 生成需要的字体和 Typst 本地包 |
| `samples/sample-images/` | 模板预览使用的示例图片 |
| `system.sh` | 构建、启动、切换、快照和离线打包的唯一命令入口 |

项目不依赖公网 CDN。前端 JS、CSS、PDF Worker、字体和 Typst 包都会进入应用镜像。首次在线构建需要从软件仓库下载 Docker 基础镜像、Node 依赖、Python 依赖和 Typst；构建完成的离线包不再需要目标服务器访问这些仓库。

## 2. 唯一配置文件

所有部署人员只修改：

```text
deploy/system.env
```

主要配置：

| 配置 | 说明 |
|---|---|
| `APP_IMAGE` | 应用镜像名称 |
| `POSTGRES_IMAGE` | PostgreSQL 镜像，默认 PostgreSQL 16 |
| `DOCKER_PLATFORM` | 服务器 CPU 平台，x86_64 用 `linux/amd64`，aarch64 用 `linux/arm64` |
| `HTTP_PORT` | 浏览器访问端口 |
| `DB_NAME/DB_USER/DB_PASSWORD` | 数据库名称、用户和密码 |
| `SNAPSHOT_*` | 重新导出移交快照时使用的旧开发库及上传目录；平时不参与运行 |
| `LOAD_SNAPSHOT_ON_EMPTY` | 新空库是否恢复本次移交数据，`1` 恢复、`0` 建空库 |
| `MOCK_*` | 本机 mock 的地址和演示管理员 |
| `SERVER_*` | 内网真实系统地址、OA 和 SOAP 地址 |
| `DOCKER_SUBNET` | Docker 内部网段，不能与现场 LAN/VPN 冲突 |

`server` 模式会拒绝默认数据库密码，并检查 OA 和 SOAP 地址；不会静默回退到 mock。`mock` 模式不调用 OA 和 SOAP，默认可使用 `admin / 123` 登录。

修改配置后不需要改源码，也不需要复制不同的 JSON 配置文件。

## 3. 环境要求

### 3.1 推荐方式

本机和服务器都只需：

- Docker Engine 24 或更高版本。
- Docker Compose v2 插件。
- 首次在线构建时能访问 Docker Hub、Debian 软件源、npm、PyPI 和 GitHub。
- 建议至少 4 核 CPU、8 GB 内存、30 GB 可用磁盘。

Ubuntu 可按 Docker 官方文档安装 Docker Engine 和 Compose 插件。安装后执行：

```bash
docker --version
docker compose version
```

无需在宿主机单独安装 Node、pnpm、PostgreSQL、LibreOffice、Typst、Python 或 Ghostscript；这些运行依赖由镜像统一提供。

### 3.2 字体

`fonts/` 中的字体参与 PDF 排版，必须连同代码一起移交。更换字体会改变换行和分页。部署单位应确保这些字体只在已授权的内网环境使用。

## 4. 本机 mock 一键启动

在项目根目录执行：

```bash
chmod +x system.sh deploy/docker/entrypoint.sh deploy/docker/init-db.sh
./system.sh mock
```

首次运行会自动完成：

1. 下载基础镜像和依赖并构建应用镜像。
2. 建立 PostgreSQL 数据卷。
3. 从 `seed-data/cdr_demo.dump` 恢复移交时的数据库。
4. 从上传资料压缩包恢复图片、Excel 和模板附件。
5. 执行数据库中尚未应用的迁移。
6. 启动前端、后端和 PDF 服务。

浏览器打开：

```text
http://127.0.0.1:5173
```

默认 mock 管理员见 `deploy/system.env`。当前快照已验证包含：27 个原始记录模板、28 个报告模板、3 个订单、4 条原始记录、2 份报告和 10 个用户。归档模板、版本历史、项目组、主机厂、设备等也包含在完整数据库快照中。

常用命令：

```bash
./system.sh status    # 容器状态和最近日志
./system.sh logs      # 持续查看应用日志
./system.sh stop      # 停止容器，保留全部数据
./system.sh down      # 删除容器和网络，保留数据卷
./system.sh rebuild   # 不使用构建缓存重新构建应用镜像
```

不要执行 `docker compose down -v`，`-v` 会删除数据库和上传文件卷。

## 5. 切换到内网服务器接口

### 5.1 修改一次配置

打开 `deploy/system.env`：

1. 把 `DB_PASSWORD` 改为强随机密码。
2. 把 `SERVER_PUBLIC_BASE_URL` 改为用户实际访问地址。
3. 核对 `SERVER_OA_LOGIN_URL`。
4. 核对 `SERVER_SOAP_ENDPOINT`。
5. 根据 `uname -m` 设置 `DOCKER_PLATFORM`。
6. 请网管确认 `DOCKER_SUBNET` 不与现场网络冲突。

### 5.2 一条命令切换

```bash
./system.sh server
```

该命令会使用同一套代码、数据库和上传卷重建运行容器，但把外部认证、报告回传、撤回和任务状态切换到真实内网接口。以后需要回到本机模拟接口，执行：

```bash
./system.sh mock
```

模式切换不会清空数据库，也不会覆盖上传文件。如果在同一台机器上先运行 mock 再运行 server，server 会继续使用原数据。

首次创建数据库卷时：

- `LOAD_SNAPSHOT_ON_EMPTY=1`：恢复随代码交付的所有模板、订单和录入数据。
- `LOAD_SNAPSHOT_ON_EMPTY=0`：建立空数据库，应用启动时只执行迁移。server 模式不会自动创建演示订单。

数据库卷建立后再修改此值不会覆盖现有库。若要重新从快照开始，应先备份，再明确删除旧卷；不要在生产环境直接试验删除卷。

## 6. 当前模板、订单和上传数据保存位置

### 6.1 随代码移交的只读初始化快照

| 文件 | 内容 |
|---|---|
| `seed-data/cdr_demo.dump` | PostgreSQL 自定义格式完整数据库快照 |
| `seed-data/uploads.tar.gz` | 当前结构的图片、Excel、模板附件和首页图片 |
| `seed-data/legacy-uploads.tar.gz` | 旧版 `server/uploads` 兼容文件 |
| `seed-data/manifest.json` | 导出时间和文件大小 |

这些文件只在**新数据卷第一次初始化**时使用。初始化后，系统不会在重启时反复导入，也不会覆盖用户后来录入的数据。

### 6.2 Docker 运行中的实际数据

Compose 使用以下持久卷：

| 卷 | 内容 | 是否必须备份 |
|---|---|---|
| `postgres_data` | 模板、版本、订单、原始记录、报告、用户、审核日志、设备等 | 必须 |
| `uploads` | 图片、Excel、普通附件和模板资料 | 必须 |
| `legacy_uploads` | 旧路径附件兼容数据 | 必须，直至完成旧数据迁移 |
| `cache` | PDF 编译缓存 | 不必，可重建 |

数据库不是保存在代码文件中的 JSON。`seed-data` 是移交和首次初始化副本；运行后的最新业务数据始终在 PostgreSQL 卷及上传卷中。

## 7. 更新移交快照

在开发机本地 PostgreSQL 和上传目录内容确认无误后执行：

```bash
./system.sh snapshot
```

该命令会：

1. 使用同一份 `deploy/system.env` 中的 `SNAPSHOT_DB_*` 连接当前开发数据库。
2. 以 `pg_dump -Fc` 导出完整数据库。
3. 用 `pg_restore --list` 检查备份格式。
4. 打包 `SNAPSHOT_UPLOADS_DIR` 指定的当前上传资料。
5. 打包 `SNAPSHOT_LEGACY_UPLOADS_DIR` 指定的旧版上传资料。
6. 原子替换 `seed-data` 中的旧快照并更新清单。

若旧版上传目录已经完成清理、目录不存在或为空，命令会保留现有的
`seed-data/legacy-uploads.tar.gz`，避免重新导出时误删仅存于兼容归档中的历史附件。

重新导出后建议执行：

```bash
node scripts/verify-deployment-snapshot.mjs
```

验证脚本会建立临时数据库、恢复快照、统计关键表，然后自动删除临时库，不修改当前业务库。
`snapshot` 和验证命令在开发机执行，需要本机安装 Node.js 与 PostgreSQL 客户端工具（`pg_dump`、`pg_restore`、`createdb`、`psql`、`dropdb`）；正常 Docker 启动和服务器运行不需要宿主机安装这些工具。

快照含真实录入、用户和附件时属于受控业务资料。提交到代码仓库前应使用单位批准的私有仓库并限制访问，不要上传公共仓库。

## 8. 在线构建和依赖下载

有网络的机器直接执行：

```bash
./system.sh mock
```

如果应用镜像不存在，命令会自动构建。Docker 构建按锁定文件下载：

- 根目录、`client/`、`server/` 的 pnpm 依赖。
- Python `requirements.txt` 中的依赖。
- Typst 0.14.2。
- LibreOffice、Ghostscript、Poppler 和字体工具。
- PostgreSQL 16 镜像。

若使用代理：

```bash
export HTTP_PROXY=http://代理地址:端口
export HTTPS_PROXY=http://代理地址:端口
docker buildx build \
  --build-arg HTTP_PROXY="$HTTP_PROXY" \
  --build-arg HTTPS_PROXY="$HTTPS_PROXY" \
  --platform linux/amd64 -t cdr-report:latest .
./system.sh mock
```

不要关闭 TLS 校验。内网使用镜像仓库时，可在 Docker 服务端配置受信任的仓库镜像源。

## 9. 生成一个离线部署文件

在一台可联网、已安装 Docker 的机器执行：

```bash
./system.sh package linux/amd64
```

ARM 服务器使用：

```bash
./system.sh package linux/arm64
```

命令会构建应用镜像、拉取对应架构的 PostgreSQL 镜像，并生成：

```text
release/cdr-report-offline-amd64.tar.gz
```

该单文件包含：

- 应用镜像和 PostgreSQL 镜像。
- Compose 与统一配置。
- 数据库、当前上传资料快照。
- 启动脚本和本部署说明。

把该文件复制到离线服务器后：

```bash
tar -xzf cdr-report-offline-amd64.tar.gz
cd cdr-report-offline-amd64
docker load -i images.tar
vi deploy/system.env
chmod +x system.sh deploy/docker/init-db.sh
./system.sh server
```

目标服务器无需下载 npm、PyPI、Typst 或系统软件包。

## 10. 日常备份

备份前尽量停止业务写入。数据库备份示例：

```bash
set -a
. deploy/system.env
set +a
mkdir -p backups
docker compose --env-file deploy/system.env -f deploy/docker/compose.yaml \
  exec -T db pg_dump -Fc --no-owner --no-privileges \
  -U "$DB_USER" -d "$DB_NAME" > backups/cdr_report.dump
```

上传卷备份示例：

```bash
docker run --rm \
  -v cdr-report_uploads:/source:ro \
  -v "$PWD/backups:/backup" \
  alpine tar -czf /backup/uploads.tar.gz -C /source .

docker run --rm \
  -v cdr-report_legacy_uploads:/source:ro \
  -v "$PWD/backups:/backup" \
  alpine tar -czf /backup/legacy-uploads.tar.gz -C /source .
```

实际卷名前缀可用 `docker volume ls` 确认。每次升级前至少备份数据库和两个上传卷，并实际验证备份可以读取。

## 11. 数据库迁移到新服务器

### 11.1 停止旧系统写入

```bash
./system.sh stop
```

导出数据库和两个上传卷，复制到新服务器。新服务器先修改 `deploy/system.env`，然后只启动数据库：

```bash
docker compose --env-file deploy/system.env -f deploy/docker/compose.yaml up -d db
```

恢复目标应为新建空库。若新卷已被初始化，不要把备份直接叠加到已有业务表；应另建空卷或空数据库后恢复。

```bash
docker compose --env-file deploy/system.env -f deploy/docker/compose.yaml \
  exec -T db pg_restore --exit-on-error --no-owner --no-privileges \
  -U cdr -d cdr_report < backups/cdr_report.dump
```

再恢复上传卷并启动应用。应用入口会自动执行尚未运行的迁移：

```bash
./system.sh server
```

如果源 PostgreSQL 主版本高于 16，应使用相同或更高版本的 PostgreSQL 镜像并重新验证，不要直接降级恢复。

### 11.2 迁移后检查

至少核对：

1. 原始记录模板和报告模板数量、当前版本和历史版本。
2. 项目组、母子模板、主机厂关联和映射。
3. 订单、原始记录、报告和审核历史。
4. 用户、角色和权限。
5. 历史图片、Excel 和普通附件可以打开。
6. 新上传图片后刷新仍可显示。
7. 中文字体、公式、表格、图片分页和 PDF 下载。
8. OA 登录、报告送审、撤回、外部退回和任务状态通知。

## 12. 更新代码

更新前备份数据库和上传卷。加载新镜像后执行同一模式命令：

```bash
./system.sh server
```

容器启动时自动运行新增迁移。代码回滚不等于数据库迁移回滚；涉及数据库结构的版本不能只替换旧镜像，应按发布说明恢复相应数据库备份。

## 13. 网络检查

健康检查：

```bash
curl -f http://127.0.0.1:5173/api/health
```

从应用容器检查 OA 和 SOAP 连通性：

```bash
set -a
. deploy/system.env
set +a
docker compose --env-file deploy/system.env -f deploy/docker/compose.yaml \
  exec app curl --connect-timeout 5 --max-time 15 -I "$SERVER_OA_LOGIN_URL"

docker compose --env-file deploy/system.env -f deploy/docker/compose.yaml \
  exec app curl --connect-timeout 5 --max-time 15 "${SERVER_SOAP_ENDPOINT}?wsdl"
```

如果部分电脑打开页面一直转圈，检查浏览器对 `/assets/*.js`、`/assets/*.css` 和 `/api/health` 的请求。静态文件由本服务器提供；长期 pending 或 canceled 通常表示客户端到服务器的链路、代理、防火墙或服务器磁盘/负载问题。

系统用于内网部署。需要开放到公网前，应另行完成 HTTPS、反向代理、会话安全、入站接口鉴权、访问审计和备份制度。
