# 功能改进需求梳理（2026-07 用户提出批次）

> **本文件定位**：把用户本轮口头/简略提出的 25 条改进需求，澄清、归类、标注现状后，形成一份**需求清单**，供内部开发人员据此评估与排期。
>
> 本文件只做「梳理需求」——把每条模糊表述澄清成明确诉求、指出它在系统里对应哪块、是否已（部分）实现、和哪份已有设计文档相关、还有哪些要跟用户确认。**具体实现方案 / 影响文件 / 工作量由开发在评估阶段补充**。
>
> **与既有文档的关系**：本清单是「入口」，triage 后各条应分流沉淀到——
> - `OPTIMIZATION.md`（backlog，明确的待办/bug）
> - `编辑器改进方案.md`（编辑器统一重构，多条编辑器交互属其范围）
> - `报告映射方案.md`（绑定/动态样品/表头绑定）
> - `待实现内容.md`（外部接口相关）
>
> **优先级图例**：🔴 当前就坏/阻断 ｜ 🟠 高（业务阻碍/数据正确性）｜ 🟡 中（体验/规模化）｜ ⚪ 低（清理/锦上添花）
> **现状标记**：`新` 全新需求 ｜ `部分` 已部分实现 ｜ `已实现?` 疑似已实现（用户可能看旧构建）｜ `Bug` 缺陷 ｜ `需澄清` 诉求歧义待确认

---

## 一、总览表

| # | 需求（一句话） | 主题 | 现状 | 优先级 | 关联文档 |
|---|---|---|---|---|---|
| 1 | 测试主管应含测试工程师权限；报告审核应含文员权限 | 权限 | 新 | 🟠 | rbac |
| 23 | 用户管理支持删除/管理角色 | 权限 | 需澄清 | 🟡 | rbac |
| 25 | 整行点击进详情；无权限者可预览、有权限者进编辑 | 权限 | 部分/已实现? | 🟠 | rbac |
| 2 | 所有编辑器每 10s 自动保存 | 编辑器 | 新 | 🟠 | 编辑器改进方案 |
| 3 | 编辑卡片关闭按钮挪到右上角 | 编辑器 | 新 | ⚪ | — |
| 4 | 表头编辑卡片「确定」放到最底下 | 编辑器 | 新 | ⚪ | — |
| 11 | 数据录入点左侧位置自动跳右侧对应处 | 编辑器 | 部分 | 🟡 | 编辑器改进方案 §7 |
| 12 | 数据录入用键盘切换输入格子 | 编辑器 | 新 | 🟡 | — |
| 13 | 项目模板切换表格样式后已设内容丢失 | 编辑器 | **Bug** | 🟠 | 报告映射方案 |
| 24 | 编辑报告模板时可查看关联的原始记录模板 | 编辑器 | 新 | 🟡 | — |
| 5 | 录入数字支持科学计数法/有效数字（与小数位二选一） | 录入 | 新 | 🟡 | — |
| 7 | 模板开了「录入可改行列表头」，录入端改不了行名 | 录入 | **Bug** | 🟠 | — |
| 9 | 图片/表格备注可在录入时填写或留空 | 录入 | 部分 | 🟠 | — |
| 10 | 服务编号/样品编号自动拉取，且可改 | 录入 | 新 | 🟠 | — |
| 20a | 录入照片可增加/改名，全部进报告，单张默认置顶 | 录入 | 部分 | 🟠 | — |
| 21 | 新增「样品描述」表格 | 录入 | 新 | 🟡 | — |
| 6 | 限值自动判定：录入限值→自动生成结论 | 判定 | 新 | 🟠 | — |
| 8 | 判定标准移入表格/表头；与单位选择拆成两个组件 | 判定 | 部分 | 🟠 | 报告映射方案 §9 |
| 14 | 未填字段显示「/」；报告拉取时跳过空字段 | 映射 | 部分 | 🟡 | — |
| 15 | 项目名称从委托单接口(ProjectName)抓取并显示 | 映射 | **已实现?** | ⚪ | 报告映射方案 §8.4 |
| 16 | 创建项目模板关联记录后，整表字段+映射一并拉入再编辑 | 映射 | 新 | 🟠 | 报告映射方案 P-Map-13c |
| 17 | 原始记录「检测时间」从送检日期(SendDate)拉取 | 映射 | 新 | 🟠 | — |
| 18 | 接口字段/时间加入可绑定映射来源 | 映射 | 部分/需澄清 | ⚪/🟠 | 待实现内容 §6.0 |
| 19 | 委托单信息显示在数据录入界面 | 映射 | 新 | 🟠 | — |
| 20b | 录入可自由改参数/试样数量（动态映射不丢数据） | 映射 | 试样已实现/参数新 | 🟡 | 报告映射方案 §二/P-Map-10 |
| 22 | 记录导入项目模板要含映射，且不走 fork（避免母子同步） | 映射 | 新 | 🟠 | 报告映射方案 |

> **提示**：现状标 `已实现?` / `部分` 的几条（15、25、18），很可能用户看的是旧构建或缺口很小，评估时先核对是否只差一点点，避免重复开发。

---

## 二、按主题梳理

### A. 权限与访问控制

**需求 1 — 角色权限的包含关系** 🟠 `新`
测试主管(`test_supervisor`)应在自身权限外，额外拥有测试工程师(`test_engineer`)的全部权限（含数据录入）；报告审核(`report_reviewer`)应额外拥有报告文员(`report_clerk`)的权限（含生成/编辑报告）。
- 现状：权限矩阵是扁平数组、无包含/继承（`shared/rbac.ts`）；当前测试主管**不能录入**、报告审核**不能生成/编辑报告**，与诉求相反。
- 备注：鉴权已是「多角色权限并集」，把被包含角色的权限补进矩阵即生效。

**需求 23 — 用户管理支持删除/管理角色** 🟡 `需澄清`
用户管理页应能删除角色 / 做角色管理。
- 现状：用户管理页已能给用户**增删角色**、启用/停用用户、审核角色申请；但**不能删用户账号**，也**不能增删「角色种类」**（角色是代码里的固定枚举，非数据表）。
- ⚠️ **待确认**：「删除角色」指哪一层？① 删用户账号 ② 把某角色从用户身上移除（已能做）③ 新增/删除/自定义**角色类型**（与「固定角色枚举」架构冲突，代价大）。语义不同，工作量差别很大。

**需求 25 — 整行点击进详情 + 按权限区分预览/编辑** 🟠 `部分/已实现?`
（a）委托单列表整行点击即进详情页（现在要点右侧按钮）；（b）无权限用户点击进**只读预览**，有权限用户进**可编辑**。
- 现状：(a) 整行点击**已实现**（用户可能看旧构建），右侧「进入」按钮现在冗余；只读模式(`?readonly=1`)也已存在。(b) **但报告链路目前完全没有权限门控**——现在是「任何能进页面的人都能编辑」，「无权限只能预览」这层需要新加。
- 备注：与需求 1 同属一套权限点，**建议先定需求 1 的矩阵，再做本条门控**，避免返工。

---

### B. 编辑器交互 / UX

**需求 2 — 所有编辑器每 10s 自动保存** 🟠 `新`
三个编辑器（原始记录模板 / 报告模板 / 报告实例）都要定时自动保存，防服务器崩溃丢数据。
- 现状：目前全是**手动保存**，无任何自动保存。
- ⚠️ **约束**：模板走 `draft→pending→approved` 版本流，自动保存只能落**草稿**，且在「审核中(pending)/看历史版本」等只读态必须**跳过**（否则会反复 409）。
- 关联：`编辑器改进方案.md` 未覆盖自动保存，属新增。

**需求 3 — 编辑卡片关闭按钮挪到右上角** ⚪ `新`
指表头编辑卡（右键/双击表头弹出的卡片）。现状只能靠底部「取消」/Esc/点外部关闭，**没有右上角 X**。

**需求 4 — 表头编辑卡片「确定」放到最底下** ⚪ `新`
现状「确定/取消」在卡片**中部**，其下还有一排（插入行列/公式/删除）操作按钮，位置不符直觉。
> 建议 3、4 合并处理（同一组件、同一次改动）。

**需求 11 — 数据录入左点右跳** 🟡 `部分`
数据录入页点左侧字段/位置，右侧 PDF 预览自动滚到对应处。
- 现状：报告实例编辑器 / 三个模板编辑器**已有**这种「编辑器⇄PDF 双向定位」，**唯独数据录入端(FormRenderer)没接**。
- 关联：属 `编辑器改进方案.md §7` 已建机制**向录入端的延伸**。

**需求 12 — 数据录入键盘切换单元格** 🟡 `新`
录入表格时用 Tab / 方向键在单元格间移动，不必逐格点鼠标。
- 现状：录入格是散落的独立输入框，无网格键盘导航。

**需求 13 — 切换表格样式后已设内容丢失（Bug）** 🟠 `Bug`
项目模板结果表切换「试样自动展开」样式（关/按行/按列）后，之前配的内容（杂项列、汇总格、单元格绑定）丢失。
- 现状：切换会**整表重建**、只搬运受支持的少数格，其余静默丢弃；切回普通表甚至清空全部单元格。
- ⚠️ **这是本批唯一「真 bug 且任何 backlog 都没记」的项，建议登记进 `OPTIMIZATION.md` 或 `报告映射方案.md`。**

**需求 24 — 编辑报告模板时可查看关联的原始记录模板** 🟡 `新`
设计报告表格/绑定时，需要并排/弹窗对照看关联的原始记录长什么样。
- 现状：报告项目编辑器已加载关联记录（用于绑定选择/校验），但 UI 上**只显示一个名称 Tag，没有查看结构或预览的入口**。

---

### C. 数据录入体验与数字格式

**需求 5 — 数字支持科学计数法/有效数字** 🟡 `新`
录入数字显示，现在只能设小数位；希望增加「科学计数法」「有效数字」（例 93.9→9.4×10¹），并与「小数位数」**互斥二选一**。
- 现状：数字格式只有 `decimals`（小数位）一个维度，且只作用于矩阵；标量数字字段无任何格式化。
- ⚠️ **约束**：录入预览与出片渲染两处必须走同一格式化逻辑，否则「预览与 PDF 不一致」。
- ⚠️ **待确认**：有效数字与补零语义（2 位有效数字的 100 显示为 `1.0×10²` 还是 `100`）。

**需求 7 — 模板开了「录入可改行列表头」但改不了行名（Bug）** 🟠 `Bug`
- 现状：改列名/改行名是两个独立开关；疑似根因是**转置表(sample_axis='col')下录入网格不转置**，导致「行/列名」与开关语义错位；另有开关默认值口径前后不一致。
- 备注：属确定性 bug，建议优先复现定位（先确认是不是转置场景触发）。

**需求 9 — 图片/表格备注可在录入时填/留空** 🟠 `部分`
- 现状：备注(caption)的数据模型**已完整存在**，但只在「模板期填默认 / 报告实例编辑期改」，**录入期没有入口**；图片上传面板也没有备注/改名输入。
- 备注：需理清 caption 的事实来源优先级（模板默认 vs 录入覆盖 vs 报告实例覆盖）。

**需求 10 — 服务编号/样品编号自动拉取且可改** 🟠 `新`
从委托单接口带出「服务编号」「样品编号」，但保持可手改。
- 现状：这两个是普通文本字段，无接口绑定、无自动拉取。
- 关联：与需求 17、19 共用「录入端引入委托单数据通道」这一前提（见第三节）。

**需求 20a — 照片可增加/改名，全部进报告，单张默认置顶** 🟠 `部分`
- 现状：照片**增/删已支持**、单张置顶模型已有、多张全部进报告已通；缺的是**录入端「改名」入口**。

**需求 21 — 新增「样品描述」表格** 🟡 `新`
- 现状：目前「样品描述」是普通文本字段，不是表格。
- ⚠️ **待确认**：是要「标签:值」属性表，还是试样×参数网格？（前者可复用现有 `data_matrix` 的 stats 模式验证）。

---

### D. 判定 / 限值 / 结论

**需求 6 — 限值自动判定** 🟠 `新`
在原始记录模板里增加「限值自动判定」：工程师录入时填入限值，系统自动把被判定值与限值比较、生成「符合/不符合」结论。
- 用户点出的核心难点（原话）：**表格是自由编辑的，系统不知道抓哪一格的值、拿哪个限值比、把结论写到哪。**
- 现状：判定引擎（threshold/range 比较）+ 单元格可视化选源 + 实时计算链**都已具备**，缺的是把「限值」从**模板写死**变成**录入填入的单元格**，以及一个面向工程师的判定配置入口。
- ⚠️ **待确认**：限值来源三态（固定值 / 录入格 / 接口下发的限值 `LimitContent`）要不要都支持。

**需求 8 — 判定标准移入表格/表头 + 拆分组件** 🟠 `部分`
① 把判定标准从「结论分区」移到**表格里**；② **表头**也能选判定标准、选后直接显示；③ 目前「判定标准选择」和「单位选择」**共用一个组件**，希望拆成两个独立组件。
- 现状：「判定标准」当前横跨两套载体——结论表的「标准」列（服务端按项目标准号机械拼接、不可逐条选）+ 矩阵表头的备注(note)；单位与判定标准在模板端确实共用同一套「固定/可选项」编辑范式，录入端则是两个高度重复的近似组件。
- 关联：「表头可绑定 + 统一绑定选择器」已在 `报告映射方案.md §9` 设计。
- ⚠️ **前置**：先统一「判定标准」的单一事实来源（现在是两套机制），再谈拆组件/移位；建议与需求 6 合并规划。

---

### E. 映射 / 绑定 / 委托单数据通道

> **共同根因**：原始记录模板 / 录入端**没有「绑定」机制、也拿不到委托单接口数据**——这是 10、15(记录侧)、17、18(记录侧)、19 的共同底座。绑定目前只存在于报告模板侧。

**需求 14 — 未填字段显示「/」+ 报告跳过空字段** 🟡 `部分`
- 现状：PDF 里空值现在渲染成下划线 `______` 或 `—`，不是「/」；空字段**不会跳过**（每个字段恒定出行）。
- ⚠️ **注意**：「显示斜杠占位」与「整行不出现」是**两种相反诉求**，需区分是哪种场景（或都要、由模板选）。

**需求 15 — 项目名称从接口抓取并显示** ⚪ `已实现?`
- 现状：接口 `ProjectName → test_item_name` 已通，报告侧已有 `test.project_name` 绑定来源、录入页也以 Tag 显示项目名。**报告侧基本已满足**；唯一缺口是「原始记录 PDF 上稳定显示项目名」（需给记录模板放一个承载字段，随录入端数据通道一起做）。

**需求 16 — 创建项目模板时整表导入记录字段+映射** 🟠 `新`
关联原始记录后，把记录的字段和映射**一并拉进**新项目模板，在此基础上编辑，而非从空白起步。
- 用户点出的难点：报告表格与原始记录表格差别大，机械照搬会导致表格难编辑。
- 现状：创建项目模板现在只设「关联记录 id」，报告从空白/复制另一张报告模板起步，**不导入关联记录字段**。
- 关联：与需求 22 是同一件事（16=导入什么，22=用什么机制导入）。

**需求 17 — 检测时间从送检日期(SendDate)拉取** 🟠 `新`
- 现状：无「检测日期」语义角色，检测时间目前手填；SendDate 数据在库但只有报告侧能取。
- ⚠️ **待确认**：检测日期在合规上是「实际检测发生日期」，未必等于送检日期 SendDate——SendDate 更像默认参照值，需确认语义（建议「预填可改」而非「锁定绑定」）。

**需求 18 — 接口字段/时间加入可绑定来源** ⚪/🟠 `部分/需澄清`
- 现状：**报告侧**绑定来源已很全（含委托单 order 字段、test 的项目名/标准/起止日期、record_meta 的检测/审核时间等）。
- ⚠️ **待确认**：缺口具体是「报告里某接口字段绑不到」（多半只是选择器 UI 未暴露某分类，小改），还是「原始记录侧也要能绑接口/时间」（=给记录引入绑定机制，大改，与 17/19 同批）。

**需求 19 — 委托单信息显示在数据录入界面** 🟠 `新`
把委托单接口传来的信息（标准号/检测方法/检测条件/送检日期/主机厂/限值等）显示在录入页，录入人员不用再去别处查。
- 现状：录入页顶部只显示「样品/项目」两个 Tag，看不到委托单全量 meta（数据在库，未展示）。
- 备注：这是需求 17、10（记录侧）的**数据前提**，建议作为「录入端引入委托单数据通道」的核心一步先做。

**需求 20b — 录入可自由改参数/试样数量（动态映射）** 🟡 `试样已实现/参数新`
- 用户点出的难点：自由改数量后，报告模板里按位置绑定的映射可能覆盖不到，生成报告拉不到数据。
- 现状：**「试样数量」动态已有答案**——`报告映射方案.md` 的「试样带(band)」模型（P-Map-10/13c）已实现，可直接推广使用；**「参数数量」动态未覆盖**，需新设计或明确「参数由报告模板固定」的产品约束。
- ⚠️ **待确认**：参数数量是否真需要录入期动态（多数检测项目参数是固定的）。

**需求 22 — 记录导入项目模板含映射、且不走 fork** 🟠 `新`
- 现状：fork（母子模板+强制同步）只用于同类型，跨不了「记录→报告」；系统里「以现有模板为骨架」创建已经是独立复制、无母子关系。
- 结论方向：「关联记录导入」应做成**一次性独立复制**（生成字段+绑定后与记录解耦），**绝不建母子关系**，否则母版审核通过会把记录字段强推报告、破坏报告结构。记录与报告的关联仍保持「软引用 + 实时校验告警」。

---

## 三、可打包的批次（供排期参考）

多条需求有共同根因或强耦合，建议成组评估，避免各做各的返工：

1. **权限批**（1 + 25）：先定角色权限矩阵（1），再铺报告链路的预览/编辑门控（25）。
2. **录入端委托单数据通道批**（19 + 17 + 10 + 15 记录侧 + 18 记录侧）：核心是「让录入端拿到并展示委托单 payload」，一处通道打通后，检测日期/编号预填、项目名显示等顺势而成。
3. **记录→报告导入器批**（16 + 22）：一次性独立复制记录字段+自动生成绑定，复用「试样带」把矩阵物化成报告结果表，**不走 fork**。
4. **判定批**（6 + 8）：限值判定与判定标准归位高度耦合，需先统一「判定标准/限值」的事实来源，再一并设计。
5. **编辑器小改批**（3 + 4）：同组件同次改。
6. **动态映射**（20b 参数轴 + 13）：都落在报告结果表/试样带模型，归 `报告映射方案.md`。

---

## 四、需向用户确认的问题（评估前先澄清）

| # | 待确认点 |
|---|---|
| 23 | 「删除角色」指删用户账号 / 移除用户身上的角色（已能做）/ 还是增删角色**类型**？三者代价差别巨大 |
| 5 | 有效数字与补零语义（100 显示为 `1.0×10²` 还是 `100`） |
| 14 | 「显示斜杠占位」和「整行跳过」是两种相反诉求，要哪种、还是都要 |
| 17 | 检测日期是否等同送检日期 SendDate（合规语义）；用「预填可改」还是「锁定绑定」 |
| 18 | 缺口是报告侧某来源没在选择器暴露（小改），还是要给原始记录侧也引入绑定（大改） |
| 20b | 「参数数量」是否真需要录入期动态，还是可约定由报告模板固定 |
| 21 | 「样品描述表」是「标签:值」属性表还是试样×参数网格 |
| 8 | 「判定标准」以哪一处为单一事实来源（结论表列 vs 矩阵表头备注） |

---

## 五、快速核对：现状与预期的差距提醒

评估时优先复核这几条，可能省下重复开发：
- **15、25(整行点击)、18(报告侧)**：大部分/基本已实现，用户可能看的是旧构建，先核对差距再动手。
- **13、7**：确定性 **Bug**，其中 13 未被任何 backlog 记录——建议先登记再排。
- **20b 试样轴、9 备注模型、20a 照片增删**：底层能力已具备，属「补入口/推广」而非「从零实现」。
