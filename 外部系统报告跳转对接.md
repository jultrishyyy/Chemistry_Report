# 报告系统「深链跳转 + 免登」对接说明（给外部系统开发）

> 面向**外部业务系统 / 递归智能的开发人员**。
> 目标：你们系统取完号后，用户点一个按钮，浏览器**直接跳到化学部报告系统中该订单的「报告生成」页面，且无需再次登录**。

---

## 0. 对接信息

| 项 | 值 |
|---|---|
| 报告系统地址 | `http://172.18.0.158:5173` |
| 共享密钥 `key` | `4ef330d4fd25823f5358487b566d509a286c6a6527b391f0b2ba222279d1c8a4` |

> 共享密钥由双方约定、仅内部持有；请勿对外公开。

---

## 1. 场景

```
你们系统（已登录用户，取号完成）
        │  用户点击「生成报告」按钮
        ▼
打开报告系统深链 URL（带 共享密钥 + 工号）
        ▼
报告系统校验密钥 → 自动登入该工号 → 落在该订单的报告生成页
```

---

## 2. 跳转地址（只需这一个）

```
http://172.18.0.158:5173/report/order/{订单号}?key={共享密钥}&job={工号}
```

示例：

```
http://172.18.0.158:5173/report/order/C202512086592?key=4ef330d4fd25823f5358487b566d509a286c6a6527b391f0b2ba222279d1c8a4&job=10086
```

- 把 `{订单号}`、`{工号}` 换成实际值，`{共享密钥}` 用第 0 节的固定值。
- **不需要签名、不需要时间戳、不需要密码。**
- 验签成功后，报告系统会**自动把 URL 上的 `key`、`job` 从地址栏清除**。

---

## 3. URL 参数

| 参数 | 必填 | 说明 |
|---|---|---|
| `key` | ✅ | 第 0 节的共享密钥，原样传 |
| `job` | ✅ | 当前登录用户的**工号**。若贵系统的登录账号本身就是工号，直接传登录账号即可 |

> 还有两个可选参数 `name`（姓名）、`dept`（部门），仅在该工号**首次**进入报告系统时用于显示，不传也行（含中文需 URL 编码）。

---

## 4. 订单号怎么来

`{订单号}` = **你们通过接口 1.1 推送的委托单号 `OrderNumber`**（与 1.2 取号用的是同一个号）。必须与报告系统里已存在的订单号完全一致，否则跳过去会找不到订单。

---

## 5. 按钮怎么实现

把上面的 URL 拼好，点击时用浏览器打开即可。例如：

```html
<a href="http://172.18.0.158:5173/report/order/C202512086592?key=4ef330d4...&job=10086"
   target="_blank">生成报告</a>
```

或在代码里拼（任意语言，本质就是字符串拼接）：

```javascript
const base  = 'http://172.18.0.158:5173';
const key   = '4ef330d4fd25823f5358487b566d509a286c6a6527b391f0b2ba222279d1c8a4';
const job   = '10086';           // 当前登录用户工号
const order = 'C202512086592';   // 当前订单号
const url = `${base}/report/order/${encodeURIComponent(order)}?key=${key}&job=${encodeURIComponent(job)}`;
window.open(url, '_blank');      // 新标签打开报告系统
```

```csharp
// C#
string url = $"http://172.18.0.158:5173/report/order/{Uri.EscapeDataString(order)}"
           + $"?key={key}&job={Uri.EscapeDataString(job)}";
```

> 提示：`key` 是固定值，可放在你们**后端配置**里，由后端拼好 URL 返回给前端打开（避免把密钥硬编码进前端源码到处复制）。

---

## 6. 跳转后的行为

- 报告系统校验 `key` 通过 → **自动以该工号登入**，展示该订单的报告生成页；
- URL 上的 `key`、`job` 自动从地址栏清除（防泄露）；
- 这次会话保持登录，后续操作无需再带参数。

---

## 7. 错误与排查

| 现象 | 原因 / 处理 |
|---|---|
| 提示「单点登录失败：密钥校验失败」 | `key` 与报告系统配置的不一致 → 核对第 0 节密钥是否完全一样（无空格、大小写一致） |
| 提示「缺少 job / key」 | URL 少了 `key` 或 `job` 参数 |
| 提示「SSO 未启用」 | 报告系统服务端尚未配置密钥 → 联系我方开启 |
| 跳过去空白 / 找不到订单 | 订单号与报告系统里的不一致，或该订单尚未通过接口 1.1 推送进来 |
| 进去了但操作提示「无权限」 | 该工号首次进入、尚未分配角色 → 由报告系统管理员分配后即可 |

---

## 8. 联调前确认

1. **密钥**：用第 0 节的值，两边完全一致。
2. **地址**：`http://172.18.0.158:5173`。
3. **订单号口径**：跳转用的订单号 = 接口 1.1 推送的 `OrderNumber`（= 1.2 取号的订单号）。

---

## 9. 快速自测

把下面的 URL（订单号换成报告系统里实际存在的）贴到浏览器：

```
http://172.18.0.158:5173/report/order/C202512086592?key=4ef330d4fd25823f5358487b566d509a286c6a6527b391f0b2ba222279d1c8a4&job=10086
```

应**直接进入该订单的报告生成页且不弹登录**。失败则对照第 7 节。
