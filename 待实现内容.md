# 待实现内容（外部接口 + 待实现设计）

> 本文件记录本系统与【外部系统】之间所有接口的对接状态：**已实现（含 mock）/ 当前实现细节 / 尚未实现**。
> 每次涉及接口的改动，都更新对应小节。
>
> 与 `OPTIMIZATION.md` 一样，这是 README 第六节「不要再开 markdown 文档」约定的一个特批例外——
> 接口对接是跨系统契约，单列一份便于对接方查阅。系统内部现状仍写 README。
>
> **状态图例**：🟢 已实现（真实）｜ 🟡 已实现（mock 占位，待换真实）｜ 🔴 未实现

---

## 接口总览

> **递归智能接口（《递归智能接口文档》）四个函数**：1.1 `PushOrderInfos`（委托单）、1.2 `PushReportInfos`（报告取号）、
> 1.3 `SyncReportModifyInfo`（报告改号）、1.4 `AcceptReportFromDiGui`（接收我方报告）。接收落点统一在 `server/src/routes/external.ts`。

| 接口 | 方向 | 状态 | 接缝（替换点） |
|---|---|---|---|
| **1.1 委托单数据源** `PushOrderInfos`（订单/样品/材料分单全字段） | 外部 → 本系统（推送） | 🟢 接收+解析已实现 / 🟡 真实 HTTP 待联调 | `routes/external.ts` POST `/orders` + `services/external-orders.ts` `parseOrderInfos` |
| **1.2 报告取号** `PushReportInfos`（报告编号/检验码/页眉页脚/报告范围） | 外部 → 本系统（推送） | 🟢 接收+匹配+取号生成已实现 / 🟡 真实 HTTP 待联调 | `routes/external.ts` POST `/reports` + `services/external-report-info.ts` |
| **1.3 报告改号** `SyncReportModifyInfo`（SysNumber→新 ReportNumber） | 外部 → 本系统（推送） | 🟢 已实现 | `routes/external.ts` POST `/report-modify` |
| **1.4 接收报告** `AcceptReportFromDiGui`（我方 PDF Base64 回传外部） | 本系统 → 外部（出站） | 🟡 mock 已实现（接缝+路由+留痕）/ 真实 HTTP 待联调 | `services/external-report-delivery.ts` + `routes/external.ts` POST `/requisitions/:id/deliver` |
| 报告页眉页脚元数据（检验码/报告编号/签发日期/公司信息…） | 本系统(订单号) ⇄ 外部 | 🟡 mock（将由 1.2 按报告号回传取代） | `services/external-report-meta.ts`（mock）+ 主题页眉页脚已渲染 |
| 身份认证 SSO | 外部 → 本系统 | 🟡 mock | `client/src/auth.tsx` + `services/template-versions.ts:readActor` |
| 化学部审批系统对接（发报告 PDF） | 本系统 → 外部 | 🔴 未实现 | 待建 `routes/external-approval.ts` |
| 外部报告反馈（是否需修改 + 建议，触发退回） | 外部 → 本系统 | 🟡 mock 已实现（P-Flow-2） | `services/external-report-feedback.ts` + `routes/external.ts` POST `/report-feedback`（见第 7 节）|
| 仪器自动采集 | 外部 → 本系统 | 🔴 未实现 | 待建录入适配层 |
| 客户报告查询门户 | 本系统 → 外部 | 🔴 未实现（范围外） | — |

---

## 1. 委托单数据源 `PushOrderInfos`（接口 1.1）🟢 接收+解析已实现

**业务含义**：上游 LIMS / 委托管理系统（递归智能）告诉本系统"当前有哪些委托单、每张单下有哪些样品、每个样品要做哪些测试项目（材料分单）"。**数据录入**和**报告生成**两端都依赖它。**调用时机：推送**（外部主动 POST 我方）。

### 1.1 字段映射（《递归智能接口文档》1.1 → 内部 `ExternalOrder` → `work_orders`）

接口为 PascalCase JSON，解析器 `services/external-orders.ts` 的 `parseOrderInfos(raw)` 规范化为内部蛇形结构：

| 层级 | 接口字段（PascalCase） | 内部字段 | 落点 / 说明 |
|---|---|---|---|
| 订单 | `OrderNumber` | `order_no` | `work_orders.order_no`（主键） |
| 订单 | `CompanyName` | `customer_name` | `work_orders.customer_name` |
| 订单 | `SendDate` | `received_at` | `work_orders.received_at`（取日期部分 YYYY-MM-DD） |
| 订单 | `CompanyAddress` / `TimeRequired` / `TestTimeRequired` / `ReportDeadLine` / `Authorites` / `AuthoritesAddress` / `SaleName` / `JobNo` / `Buyer` / `Status` / `Remark` / `IsChineseReport` / `IsEnglishReport` / `IsPaperReport` / `ReportCount` / `OtherReportCount` / `CompleteWay` | `meta.{company_address,time_required,…}` | `work_orders.payload.meta`（订单级，供报告 binding `source='order'` 拉取） |
| 样品 | `SampleName` | `samples[].name` | 报告侧（接口 1.2）按**样品名**回查关联 |
| 样品 | `BarCode` | `samples[].id` **(= sample_external_id)** | 样品在本单内稳定唯一 id；缺省回退 `s{n}` |
| 样品 | `SampleSortNo` / `Model` | `samples[].{sort_no,model}` | 供 binding `source='sample'` |
| 材料分单 | `ProjectName` | `samples[].test_infos[].name` | `record_data.test_item_name`（同一样品内**唯一**） |
| 材料分单 | `StandardNo` | `test_infos[].standard` | 供 binding `source='test'` |
| 材料分单 | `MainEngineFactory` / `TestMothed` / `TestCondition` / `SamplingMode` / `SamplingRequirement` / `LimitName` / `LimitContent` / `Leader` / `StartDate` / `EndDate` / `SampleDescription` / `TestRemark` / `MaterialUploader` / `Remark` | `test_infos[].{main_engine_factory,test_method,…}` | 供 binding `source='test'`（报告映射可拉取） |

> `samples[].test_infos[]` 的每个元素 = 一个"样品 × 测试项目"格子，正是 `record_data` 的唯一上下文
> `(order_no, sample_external_id, test_item_name)`，也是报告生成最小原子。

### 1.2 容错（解析期）

- **同一样品下重复 `ProjectName`**（文档示例里出现过 3 个同名 `fghfdfg`）：与 `record_data` 唯一键 `test_item_name` 冲突 → **去重保留首条**，返回告警（`warnings[]`）。
- **样品无 `BarCode`**：回退 `s{n}`；id 撞车自动加 `#k` 后缀保证单内唯一。
- **再次推送同一订单**：`upsertOrder` 更新 `customer/received/meta` + 合并 samples，按【样品名 + 项目名】**保留**工程师已设的 `linked_template_ids`；推送中消失的样品/项目返回告警（旧 `record_data` 永不物理删除，留库为孤儿，报告侧标记需手动处理）。

### 1.3 当前实现

- **接收端点**：`routes/external.ts` `POST /api/external/orders`（body = 1.1 PushOrderInfos JSON，单条对象或数组）→ `parseOrderInfos` → `upsertOrder`（`source='external'`）。返回 `{ok, count, results:[{order_no,action,warnings}], warnings}`。
- **解析器**：`services/external-orders.ts` 的 `parseOrderInfo` / `parseOrderInfos`，类型 `ExternalOrder` / `ExternalSample` / `ExternalTestInfo` / `ExternalOrderMeta` 已扩展为接口全字段。`EXAMPLE_PUSH_ORDER` 为文档示例常量，供联调 / mock 触发。
- **拉取接缝保留**：`fetchExternalOrders()` 仍返回 `[]`（占位）；`seed-work-orders.ts` 启动同步逻辑不变（含 `payload.meta`）。
- **两端消费**（都只依赖 `work_orders` 表）：录入 `Lab/TaskList.tsx`、报告 `Report/OrderList.tsx`，均 `GET /api/work-orders`。

### 1.4 真实接入要改什么

- 外部按 1.1 契约 POST 到 `/api/external/orders` 即可（推送模型，无需改我方代码）。
- 若外部是"拉取"模型：把 `fetchExternalOrders()` 换成真实 HTTP（返回 `ExternalOrder[]`），其余不动。
- 演示/联调：把 `EXAMPLE_PUSH_ORDER` 或文档示例 JSON `curl -XPOST /api/external/orders`。

### 1.5 尚未实现

- 真实推送的鉴权 / 防重 / 签名校验。
- 订单**撤销**的同步（当前推送只新增/更新，不删单）。
- 订单状态回写外部（本系统进度 → 外部）。

---

## 2. 身份认证 SSO 🟡 mock

- **当前**：`client/src/auth.tsx` 预置 4 个 mock 用户，axios 拦截器加 `X-Demo-User` / `X-Demo-Role` 头（中文 `encodeURIComponent`）。后端 `services/template-versions.ts` 的 `readActor()` 解析。
- **真实接入**：① `auth.tsx` 改为从 SSO 拿 token + userInfo；② 拦截器换 `Authorization: Bearer`；③ `readActor()` 改为解析 session/JWT。所有写路径已统一走 `readActor()`，共 3 处。
- **未实现**：与真实 SSO 的对接、权限到部门/角色的细粒度映射。

---

## 3. 报告出站回传 / 审批系统对接 🟡 mock（接口 1.4 回传已实现 mock）

- **接口 1.4 AcceptReportFromDiGui（回传 PDF）已实现 mock**：`services/external-report-delivery.ts` 的 `submitReportToDiGui(sys_number, pdfBase64)`（接缝）+ `routes/external.ts` `POST /api/external/requisitions/:id/deliver`（取已生成报告 final_typst → 编译 PDF → Base64 → 回传 → 写 `report_requisitions.delivery_status/delivered_at/delivery_error` 留痕）。前端在报告工作台「取号报告」面板已生成项「回传递归智能」按钮触发。
  - **真实接入**：设环境变量 `DIGUI_ACCEPT_REPORT_URL` 即走真实 HTTP（POST `{SysNumber, file(Base64)}`）；缺省＝mock 返回成功。其余（路由 + 状态留痕 + 前端）不动。
- **仍未实现**：外部接收回执/审批结论回调（→ 触发退回，见第 7 节 P-Flow-2）；防重/鉴权；批量回传。

---

## 4. 仪器自动采集 🔴 未实现

- **当前**：实验员手动录入或 Excel 导入。
- **真实接入**：加适配层把仪器输出映射到 `record_data`，不动主链路。
- **未实现**：全部。

---

## 5. 客户报告查询门户 🔴 未实现（范围外）

完全独立的对外系统，当前不在本项目范围。

---

## 6. 报告生成：报告取号 `PushReportInfos` + 页眉页脚元数据（接口 1.2/1.3/1.4）

> **状态（2026-06）**：录入侧（接口 1.1）+ 报告映射可绑定接口字段 **已实现**（见第 1 节 + 6.0）。
> 报告侧的**取号驱动生成 + 匹配引擎 + 工作台面板（接口 1.2/1.3）已实现**（下文 6.0a–6.0e；表 `report_requisitions` migration 026、
> `services/external-report-info.ts`、`routes/external.ts` 的 `/reports`·`/report-modify`·`/requisitions`·`/requisitions/generate`、
> `Workbench.tsx` 取号报告面板、页眉页脚改用每报告 `buildReportMetaFromReq`——均通过端到端 API + 真实 Typst 编译验证）。
> **仍未做**：接口 1.4（出站回传 PDF，见第 3 节）、真实推送鉴权/防重、英文报告页眉页脚整套文案细化。
> 页眉页脚的**渲染层**（6.1–6.6）已实现；数据源已从 `fetchReportMeta(order_no)`（每单一份 mock）切换为 1.2 推送的"**每报告一份**"（`report_meta` 覆盖）。

### 6.0 已实现：报告映射可绑定委托单接口字段

报告模板映射（`CellBinding`）新增三个来源，生成期从 `work_orders.payload` 拉取（接口 1.1 字段）：

| binding source | 取数 | 解析（`resolveBinding` / `buildReportTypst`） |
|---|---|---|
| `order`（键扩展） | 订单级（`payload.meta`） | `ctx.order_meta`（历史 4 键仍走 `ctx.order`） |
| `sample`（新增） | 当前项目所属样品 | `ctx.sample_info`（按 `record_data.sample_external_id` 回查 `payload.samples`） |
| `test`（新增） | 当前材料分单 | `ctx.test_info`（按 `sample_external_id` + `test_item_name` 回查） |
| `report_meta`（接口 1.2） | 报告取号推送的页眉页脚/抬头（公司/客户/备注/资质 + 中英文 + 报告号/检验码/签发日期） | `ctx.report_meta`（`buildReportMetaFromReq(requisition)` 或 `fetchReportMeta` mock，与页眉页脚同一份 `ReportMeta`，中英文按 `Language` 选中） |

注入点：`buildReportTypst` 载入 `work_orders.payload` → 建 `sampleInfoById/byName` + `testInfoByKey` → 写进每个项目的 `projCtx` 与 `content_doc` 的 ctx 快照（重渲染一致）。UI：两处绑定器均已暴露这三类来源——v2 区块编辑器 `ReportEditor/BindingEditor.tsx` 三个下拉；**v3 模板编辑器（`FieldPropsPanel` 取值绑定）走 `ReportEditor/BindingPickerModal.tsx`，已补「委托单字段(订单 15 键)/委托单·样品(4 键)/委托单·测试项目(16 键)」三个 Tab**（与 BindingEditor 同字典）。校验：`binding-integrity.ts` 视这三类为"不指向原始记录字段，恒有效"。

---

### 6.0a 业务流程变化（P2 设计）

**旧流程**：文员在报告工作台选 `split_mode`（整单/按样品/按项目/自由）+ 系统按订单号 `fetchReportMeta` 自动编号 → 生成多份。
**新流程（取号为准，唯一生成路径）**：

1. 文员在**外部系统**取号（指定哪个样品、哪个项目、出几份报告，一份报告 = 一个 `ReportNumber`）。**报告如何拆分（整单/按样品/按项目）完全由外部取号决定**——本系统不提供拆分选择。
2. 外部按接口 1.2 `PushReportInfos` **推送整单的 `ReportList[]`**——每条 = 一份报告，带 `ReportNumber` + `CheckCode`（检验码）+ 页眉页脚全套（公司/客户/备注/资质/签发日期 + 中英文）+ 该报告覆盖的 `SampleList→TaskList`（=报告范围）。
3. 本系统收到后：**展示所有报告编号的报告**；对每份报告解析 `SampleList/TaskList` → 按**样品名 + 项目名**回查录入侧 `record_data` 与关联报告模板 → **能对应的自动拉取**、对应不上的**标记**让文员手选；最后生成、展示、编辑这些报告。本系统对报告编号内的样品/项目/原始记录**只能补齐/确认**，不能改拆分。
4. **手动拆分已撤除**（2026-06）：`Workbench.tsx` 不再有 `split_mode` Segmented / 手动项目槽 / 手动「生成入库」按钮。后端 `POST /reports/generate-batch` 端点仍在但前端不调（保留兼容，可后续清理）。
5. **取号前看订单信息 + 编辑首页实例**（已实现）：OrderList「操作」→ 报告详情页 `Workbench.tsx`（单列）。左列「订单信息」面板（接口 1.1 `payload.meta` 字段）+ 录入进度 + 只读原始记录 + **「编辑首页」按钮**。
   - **「编辑首页」＝实例级**（非模板）：`POST /reports/cover-draft` 幂等建/取 order 级首页草稿（`is_cover_draft=true` 的 cover-only reports 行，`content_doc.cover` 由 `buildReportTypst` 空项目渲染）→ 进 `InstanceEditor` 改样品信息/结论等填入值、增删改字段、调表格样式。
   - **carry-over**：取号生成时 `/requisitions/generate` 载入草稿 `content_doc.cover.groups` 作 `cover_groups_override`，每份报告首页用草稿的结构/样式/手改值，binding 与结论表按各报告 scope 现算（ctx 每报告重建）。migration 029；端到端验证：override 替换 cover groups、手改字面量进 final_typst。

### 6.0b 关联 / 容错（接口 1.1 ↔ 1.2 对应）

两接口的**样品名 SampleName、项目名 ProjectName 一致**，是关联锚点。匹配引擎对 1.2 每条报告的每个 `(SampleName, ProjectName)` 格子：

| 情况 | 处理 |
|---|---|
| 样品名命中订单样品 + 项目名命中其分单 + 有 `record_data`（优先 reviewed） | ✅ 自动拉取：填 `record_data_id` + 关联报告模板（`linked_record_template_id` 反查），状态 `matched` |
| 样品名命中、项目名命中、但**无 record_data 或未审核** | ⚠️ `needs_record`：文员去录入/选已有记录 |
| 样品名/项目名**对不上**（订单里删了/改名了） | ⚠️ `unmatched`：文员手动选样品/项目/原始记录 |
| 一个项目关联**多个**报告模板（一对多） | 展开成多个项目槽，文员确认取舍 |
| 1.2 报告范围里的项目**多于/少于**录入侧 | 多：标 `unmatched` 待手选；少：只出推送指定的范围（不强出全部） |

> 原则：**能自动拉取就拉取，拉不到只标记不报错**——文员在工作台逐格补齐再生成。

### 6.0c 数据模型（P2，新 migration）

新表 `report_requisitions`（"报告取号单"，1.2 每条一行）：

```sql
CREATE TABLE report_requisitions (
  id SERIAL PRIMARY KEY,
  order_no       TEXT NOT NULL,
  sys_number     TEXT NOT NULL,         -- SysNumber（外部系统编号，幂等键）
  report_number  TEXT NOT NULL,         -- ReportNumber（报告编号，可被 1.3 改号）
  check_code     TEXT,                  -- CheckCode 检验码
  language       TEXT,                  -- Language 语种（中文/英文）
  sample_name    TEXT,                  -- 该报告主样品名
  issue_date     TEXT,                  -- SecondAuditeDate 签发日期
  header_footer  JSONB,                 -- 页眉页脚全套（公司/客户/备注/资质 + 中英文）→ ReportMeta
  scope          JSONB,                 -- 解析后的 SampleList/TaskList（报告范围）
  match_result   JSONB,                 -- 匹配引擎产物：[{sample_name,project_name,status,record_data_id?,project_template_id?}]
  report_id      INTEGER,               -- 生成后回填 reports.id
  status         TEXT DEFAULT 'pending',-- pending(待匹配/待生成) | ready(已匹配可生成) | generated
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(sys_number)
);
```

复用现有 `reports` / `report_batches`（一次推送 = 一个 batch，每条 requisition 生成一行 `reports`，`report_no = ReportNumber`、`scope` 收敛到本报告范围）。页眉页脚不再走 `fetchReportMeta(order_no)`，改用 `report_requisitions.header_footer`（每报告一份，映射见 6.0d）。

### 6.0d 1.2 字段映射（`PushReportInfos` → `ReportMeta` / scope）

| 接口字段 | 去向 |
|---|---|
| `OrderNumber` | `report_requisitions.order_no` |
| `ReportList[].SysNumber` | `sys_number`（幂等键，1.3 按它改号） |
| `ReportList[].ReportNumber` | `report_number` → `reports.report_no` + 页眉 `report_no/cover_report_no` |
| `CheckCode` | `check_code` → 页眉 `verify_code`（检验码） |
| `Language` | `language`（中文/英文 → 选中英文页眉页脚文案） |
| `SecondAuditeDate` | `issue_date` → 页脚签发日期 |
| `CompanyName/CompanyNameEN` | `header_footer.company_name(_en)` |
| `CompanyAddress/CompanyAddress2/…EN` | `header_footer.company_address*` |
| `CompanyFax/CompanyTel/…EN` | `header_footer.fax/phone(_en)`（含网址） |
| `ReportRemark/ReportRemarkEN` | `header_footer.report_note(_en)` |
| `QualificationRemark/…EN` | `header_footer.qualification_note(_en)` |
| `CustomerName/CustomerAddress` | `header_footer.customer_*` |
| `SampleList[]→TaskList[]` | `scope`（解析复用 1.1 的 `parseOrderInfo` 样品/分单逻辑）|

> `ReportMeta` 需补中英文字段（按 `Language` 选用），主题 `record-theme` 页眉页脚已支持的键不变。

### 6.0e 实现要点 / 影响文件（P2）

| 改动 | 文件 |
|---|---|
| `report_requisitions` 表 | 新 migration |
| 1.2 接收 + 解析（复用样品/分单解析）+ 匹配引擎 | `routes/external.ts` POST `/reports`（去掉 501）+ 新 `services/external-report-info.ts`（parse + match） |
| 1.3 改号 | `routes/external.ts` POST `/report-modify`：按 `sys_number` 更新 `report_number`；已生成则标 stale / 重渲染页眉 |
| 1.4 回传 PDF（出站）| 新 `routes/external-approval.ts`（并入第 3 节）|
| 页眉页脚数据源切换（每报告一份 + 中英文） | `reports.ts` `buildReportTypst`（按 requisition 注入 `header_footer` 而非 `fetchReportMeta`）+ `ReportMeta` 加中英文字段 |
| 工作台：列出取号报告 + 匹配状态 + 手动补齐 + 生成 | `client/pages/Report/OrderList.tsx` / `Workbench.tsx`（取号优先、手动拆分兜底）|

---

### 页眉页脚【渲染层】（已实现；数据源 P2 由接口 1.2 取代 mock）

> **实现进度（2026-06）**：mock 数据源 `services/external-report-meta.ts` + 主题 `record-theme` 页眉页脚渲染（首页/其余页报告编号区分、页码/总页数）+ `buildReportTypst` 注入 + 快照进 `content_doc` **均已实现并通过 Typst 编译验证**。剩"真实外部 HTTP 接口（1.2）"与"模板期版式配置 UI / 实例期备注覆盖"（见 6.6）。下文为完整设计，括注已落地项。

**业务含义**：所有报告最终都要在**页眉/页脚**印上一组"机构级元数据"。这组数据由外部系统**按订单号生成并回传**，我们填进 PDF。

### 6.1 接口契约

```ts
// 请求：本系统 → 外部（按订单号要元数据）
ReportMetaRequest { order_no: string }

// 响应：外部 → 本系统（生成检验码等并回传）
ReportMeta {
  verify_code:    string   // 检验码（外部生成，防伪/核验用）
  report_no:      string   // 报告编号（页眉/正文用）
  cover_report_no:string   // 首页报告编号（首页与其余页可不同）
  issue_date:     string   // 签发日期 YYYY-MM-DD
  company_name:   string   // 公司名称
  report_note:    string   // 报告备注
  qualification_note: string // 资质备注（CMA/CNAS 等）
  company_address:string   // 公司地址
  fax:            string   // 传真
  phone:          string   // 电话
}
```

- **接缝**：`server/src/services/external-report-meta.ts`，`fetchReportMeta(order_no): Promise<ReportMeta>`。
  - 现在：mock 返回写死的一组（与 `external-orders.ts` 同套路，便于全流程测试）。
  - 真实接入：换成对外部系统的 HTTP 调用（请求 order_no，拿回 ReportMeta）。其余不动。
- **调用时机**：报告生成时（`buildReportTypst`）按 `order_no` 调一次，把 ReportMeta 注入到主题的页眉/页脚配置；并快照进 `reports.content_doc`（与正文一起冻结，保证重渲染一致、可留痕）。

### 6.2 Typst 能不能做页眉页脚？—— 能

Typst 原生支持运行页眉/页脚（每页重复）、首页与其余页不同、页码/总页数：

```typ
set page(
  paper: "a4",
  margin: (top: 3cm, bottom: 2.8cm, left: 2cm, right: 2cm), // 留出页眉页脚空间
  header: context {
    let first = counter(page).get().first() == 1
    grid(columns: (1fr, 1fr),
      align(left + horizon)[#company_name],
      align(right + horizon)[报告编号：#(if first { cover_report_no } else { report_no })　检验码：#verify_code],
    )
    line(length: 100%, stroke: 0.5pt)
  },
  footer: context {
    line(length: 100%, stroke: 0.5pt)
    grid(columns: (1fr, auto, 1fr),
      align(left)[电话：#phone　传真：#fax],
      align(center)[第 #counter(page).display() 页 / 共 #counter(page).final().first() 页],
      align(right)[#company_address],
    )
    text(size: 0.8em, fill: gray)[#qualification_note　签发：#issue_date]
  },
)
```

要点：
- **首页/其余页不同**：用 `counter(page).get().first() == 1` 判断（如首页报告编号 vs 其余页报告编号）。
- **总页数**：`counter(page).final().first()`。
- **页码**：`counter(page).display()`。
- 当前主题 `record-theme`（`typst-packages/local/record-theme/0.1.0/lib.typ`）的 `set page` 只设了 paper+margin、**没有 header/footer**，标题只是首页顶部一个居中块——本方案在 `set page` 里补 `header:`/`footer:`，并把元数据从 `config` 传进来。

### 6.3 数据怎么流进 PDF

1. `buildReportTypst` 按 `order_no` 调 `fetchReportMeta` 拿 `ReportMeta`。
2. 把 `ReportMeta` 放进**首页**那张伪 `RecordTemplate` 的 `layout_options.header_footer`（整篇报告是 cover+projects 拼成一个 typst 文档、共用一个 `set page`，所以页眉页脚在首页主题里设一次即可覆盖全文）。
3. `generateTypst` 渲染 `#show: record-theme.with(config: (..., header_footer: (...)))`，主题据此 emit 上面的 `set page(header, footer)`。
4. 同时把 `ReportMeta` 快照进 `content_doc`，重渲染（实例编辑后）走同一份，保证一致。

### 6.4 这些页眉页脚怎么编辑

分三类，各归其位（沿用第 7 节"报告映射"的三层职责思路）：

| 类别 | 字段 | 谁/何时/在哪改 |
|---|---|---|
| **外部权威值** | verify_code / report_no / issue_date 等 | 外部系统生成，本系统**只填不改**；如需覆盖，走报告实例编辑器（P2）改 `content_doc`，P3 留痕 + 偏离告警 |
| **版式/布局** | 哪个字段放页眉哪个放页脚、字号、是否显示页码 | 工程师在**报告模板**设计期配（`report_templates.layout_options.header_footer` 的布局部分），或留一套公司级默认 |
| **每报告可调备注** | report_note / qualification_note | 文员在报告实例编辑器里改（留痕）|

> 即"**值**大多来自外部、**版式**由模板定、**个别备注**可在报告编辑期微调"。

### 6.5 实现要点 / 影响文件

| 改动 | 文件 |
|---|---|
| 新建 mock 外部元数据接口 `fetchReportMeta(order_no)` | `server/src/services/external-report-meta.ts`（新） |
| `buildReportTypst` 按 order_no 取元数据 + 注入首页主题 config + 快照进 content_doc | `server/src/routes/reports.ts` |
| 主题 `set page` 增加 `header`/`footer`，从 `config.header_footer` 取值 | `typst-packages/local/record-theme/0.1.0/lib.typ` |
| `generateTypst` 把 `layout_options.header_footer` 透传进 `record-theme.with(config)` | `shared/typst-generator.ts` |
| 报告模板编辑器加"页眉页脚版式"配置面板（哪个字段放哪、页码开关） | `client/components/FieldEditor/DocumentStylePanel.tsx` |
| 报告实例编辑器允许覆盖 report_note / 资质备注等（留痕） | `client/pages/Report/InstanceEditor.tsx` |

**兼容**：无 `header_footer` 配置时主题行为不变（不出页眉页脚），存量模板不受影响。

### 6.6 尚未实现

- 真实外部接口（HTTP 请求 order_no → ReportMeta）——当前 `fetchReportMeta` 为 mock。
- ~~主题页眉页脚 + 首页/其余页差异渲染~~ ✅ 已实现（`record-theme/0.1.0/lib.typ` `_render-header`/`_render-footer`，Typst 编译验证首页 `cover_report_no`、其余页 `report_no`、页码/总页数）。
- ~~`buildReportTypst` 注入 + 快照进 `content_doc`~~ ✅ 已实现（`buildHeaderFooterConfig` + cover `layout_options.header_footer`）。
- 模板期版式配置 UI：✅ **最小版已实现**——`CoverEditor.tsx` 顶栏「页眉页脚=标准/无」+「页码开关」，存进 `layout_options.header_footer.{enabled, show_page_number}`，Typst 编译验证三种组合（开+页码 / 开+无页码 / 全关）。**仍未做**：字段放页眉/页脚的细粒度布局、字号等，及实例期备注覆盖（report_note / 资质备注）。
- DOCX 导出端页眉页脚保真（PDF→DOCX 反转会丢页眉装饰，见 README 第九节已知保真度损失）。

---

## 6.7 检测结论模型重构：声明迁移到项目报告模板 ✅ 已实现（2026-06-19）

> **实现状态（2026-06-19）**：按下方设计落地。`shared/types.ts` 加 `ProjectConclusionDecl`、删 `conclusion_field`（FieldDefinition + MatrixSummaryRowDef）；`reports.ts buildReportTypst` 改读 `projTpl.layout_options.conclusions[]` 逐条 `resolveBinding` 收结论、项目名优先 `projTpl.layout_options.project_name`、删旧扫描与 result_table 回退；`binding-integrity.validateReportBindings` 加第三参 `conclusions` 校验（抽 `checkRecordBinding` 复用，server 三处调用 + ProjectEditor 同步）；`ProjectEditor.tsx` 加「本项目检测结论」面板（项目名 + 子项目结论列表，复用 `BindingPickerModal`）；移除原始记录的「作为检测结论」开关（`FieldPropsPanel` / `HeaderConfigCard` showConclusion / `MatrixCanvas` 结论 Tag）；迁移脚本 `scripts/migrate-conclusions-to-project.ts` 已跑（7 个带标记原始记录 → 迁 1 个有关联项目模板）。验证：client/server `tsc --noEmit` 0 错；smoke（conclusions→resolveBinding→project_summary→renderConclusionTableTypst→typst→PNG）实测——子项目"抗拉强度/断裂伸长率"各一行、结论"符合/不符合"、序号与标准 rowspan 合并均正确。**仍待**：buildReportTypst 的取号端到端（需 work_orders + record_data 实数据）真实出报告回归。

> **背景**：当前检测结论从**原始记录**抓取——扫描原始记录模板里标了 `FieldDefinition.conclusion_field` / `MatrixSummaryRowDef.conclusion_field` 的字段（P-Map-6/8），值＝展平后的录入/计算判定。问题：①"项目名 / 分几个子项目 / 哪个值算结论"是**报告侧语义**，却塞在原始记录上（原始记录只该管实验数据）；②子项目↔结论框靠 `sub_name` + 扫描，声明分散、不够显式；③项目名现读自原始记录的 `layout_options.project_name`，归属错位。
>
> **决策（2026-06-19 评审拍板）**：
> 1. 结论**声明**（项目名 / 子项目名 / 每个结论的来源）放**项目报告模板**；结论**值**仍在生成时从原始记录按绑定解析（值本质是数据，只能来自原始记录）。
> 2. 承载形式＝项目模板编辑器的**专用「项目结论」面板**（非字段、非结果表列）。
> 3. **彻底移除** `conclusion_field`（原始记录不再标"结论"标签；含数据迁移）。

### 6.7.1 一句话方案

**项目报告模板**声明：`项目名称` + `结论列表 conclusions[]`，每条＝一个"结论框"＝`{ sub_name?, binding }`；`binding` 用现有 `CellBinding`（`record_field`/`record_summary`/`record_cell`/`record_formula`）指向关联原始记录里**该（子）项目的判定值**。生成时逐条 `resolveBinding` 出值，拼成首页检测结论汇总表的行。子项目与结论框**一对一显式映射**（每条声明自带名字+绑定，不靠猜）。

### 6.7.2 数据模型（项目报告模板，`ReportTemplate.layout_options`）

```ts
// 项目报告模板（kind='project'）的 layout_options 上：
layout_options.project_name?: string;        // 本项目在报告里的显示名（移到项目模板自身，优先级最高）
layout_options.conclusions?: Array<{
  id: string;                                // 稳定 id（uniqueCode 生成）
  sub_name?: string;                         // 子项目名；空＝项目级单结论（显示用 project_name）
  binding: CellBinding;                      // 结论值来源（指向关联原始记录字段/矩阵汇总行/单元格/公式）
}>;
```

- 单结论项目：`conclusions=[{ binding }]`（sub_name 空）。
- 多子项目：`conclusions=[{ sub_name:'抗拉强度', binding:… }, { sub_name:'断裂伸长率', binding:… }, …]`。
- 绑定作用域＝该项目模板 `linked_record_template_id` 指向的原始记录（与结果表 cell binding 同一套来源/校验）。

### 6.7.3 子项目 ↔ 结论框映射（回答"怎么标注哪个子项目对应哪个结论框"）

每个子项目就是 `conclusions[]` 里的一条，**自带 `sub_name` + `binding`**：
- `sub_name` = 这一行在结论表里显示的子项目名；
- `binding` = 这一行的值从原始记录哪里取（如 `record_summary` 指矩阵"力学"汇总行"抗拉判定"，或 `record_field` 指"伸长率判定"字段）。
- 失效引用（原始记录改名/删字段）由 `shared/binding-integrity.ts` 自动校验标红（新增对 `conclusions[].binding` 的遍历）。

### 6.7.4 取号 → 模板 → 原始记录 → 生成（完整流程，★＝本次改动）

```
1. 接口 1.2 取号  POST /api/external/reports → parseReportInfos → scope{样品名×项目名}
2. matchRequisitionScope：每个(样品名,项目名)→record_data(优先reviewed)
   →反查 report_templates(kind=project) WHERE linked_record_template_id=record_data.template_id
   →assignments[{record_data_id, record_template_id, project_template_id}]   （已有）
3. POST /api/external/requisitions/generate → generateAndStoreReport(assignments)  （已有）
4. buildReportTypst：逐 assignment（项目模板 + 一份原始记录数据）
   a. 锁版本载入原始记录模板+数据，展平+算公式 → flat                          （已有）
   b. 用 flat 填项目模板结果表/设备表/图片表 binding                          （已有）
   c. ★ 读项目模板 layout_options.conclusions[]，逐条 resolveBinding(binding, 原始记录ctx)
        → { name=项目模板 project_name, sub_name, value }
   d. 收进 projectRecords                                                      （改：来源换成 c）
   e. 排序展平 → project_summary（行结构不变：name/sub_name/conclusion/sample_no/standard）（已有）
5. 首页 report_conclusion_table 渲染 project_summary                           （已有，不改）
6. 编译 + content_doc 快照入库                                                  （已有）
```

**关键**：每份报告的样品/项目范围在第 2 步（取号匹配）已定，项目模板靠 `linked_record_template_id` 已绑死其原始记录——"拉哪份原始记录填哪个项目模板"无需新增逻辑；**唯一改动是 4c**：结论收集从"扫原始记录标签"换成"按项目模板声明解析"。因此每份报告的结论表天然按其 scope 不同（本就如此）。

### 6.7.5 移除 `conclusion_field`（决策＝彻底移除）

- **类型**：删 `FieldDefinition.conclusion_field`、`MatrixSummaryRowDef.conclusion_field`。
- **生成端**：`buildReportTypst` 删 P-Map-6/8 扫描 + result_table `is_conclusion` 回退分支，改读项目模板 `conclusions[]`。
- **编辑端**：原始记录 `FieldPropsPanel`/`HeaderConfigCard`（汇总行）去掉「作为检测结论」开关。
- **数据迁移**：新 migration/脚本扫描存量原始记录模板里标了 `conclusion_field` 的字段/汇总行，按其 `linked_record_template_id` 反查项目模板，生成对应 `conclusions[{sub_name, binding: record_field/record_summary 指向该字段}]` 写入项目模板当前版本（非破坏性，幂等）。无法自动归属的留报告 + 提示人工补。
- **风险**：存量报告 `content_doc` 已是快照、不受影响；只影响"重新生成"。迁移脚本须覆盖 P-Map-6（字段）与 P-Map-8（矩阵汇总行）两类来源。

### 6.7.6 编辑端 UI（专用「项目结论」面板）

- `pages/ReportTemplate/ProjectEditor.tsx`：顶部或侧栏加一块「本项目检测结论」：
  - 「项目名称」输入 → `layout_options.project_name`；
  - 「子项目结论」列表：每行 = `子项目名` 输入 + 「选择数据来源」按钮（复用 `ReportEditor/BindingPickerModal`，来源限 `record_*`）+ 删除；「+ 添加子项目结论」；
  - 单结论时留一行、子项目名空即可。
- 失效绑定复用现有 `binding_warnings` Alert 实时标红。

### 6.7.7 影响文件

`shared/types.ts`（删 conclusion_field、可选给 layout_options.conclusions 一个类型别名）、`server/routes/reports.ts`（buildReportTypst 4c/4d）、`shared/binding-integrity.ts`（校验 conclusions[].binding）、`client/pages/ReportTemplate/ProjectEditor.tsx`（结论面板）、`client/components/FieldEditor/{FieldPropsPanel,MatrixEditor/HeaderConfigCard}.tsx`（去结论开关）、迁移脚本 `scripts/migrate-conclusions-to-project.ts`、文档（本节 + README 第五节"改报告结论判定逻辑"行 + typst-generator `renderConclusionTableTypst` 注释）。

### 6.7.8 分期建议

- **P1 模型+生成**：types 加 conclusions / project_name 优先级；buildReportTypst 读 conclusions（**先与旧 conclusion_field 回退并存**，验证生成正确）。
- **P2 编辑端**：ProjectEditor 结论面板 + binding-integrity 校验。
- **P3 迁移+清除**：迁移脚本跑通存量 → 删 conclusion_field 类型/扫描/UI。
- 每期走 modify-system 五阶段 + generator→typst→PDF 验证。

> **状态**：🔵 设计待评审通过 → 按 6.7.8 分期实现，完成各期回标 ✅。

### 6.7.9 项目明细段「N) 项目名」编号标题 ✅ 已实现（2026-06-19）

> 依据参考样张（`ref_files/files/测试报告 C202512086592-01…doc`、`C202613045990_合格…doc`）：每个项目明细段以 **`N) 项目名`** 编号小标题开头（左对齐加粗，非居中大标题），序号 `N` 与首页检测结论汇总表的序号**完全一致**；一个项目多个子项目时，明细段标题合并显示（如「3) 弯曲强度&弯曲模量」），结论表里序号跨行（rowspan）、每子项目一行。

**实现**：
- `server/routes/reports.ts buildReportTypst`：在生成 `projectSummary` 的排序循环里给每个项目段挂 `projects[oi].seq = runningIdx`（runningIdx 即结论表序号），再 `projects = order.map(oi => projects[oi])` 让明细段按与结论表相同顺序输出（序号才连续一致）；`projects.map` 进 content_doc 时带上 `seq`。`projects` 由 `const` 改 `let`。
- `shared/types.ts`：`ReportContentDoc.projects[]` 加 `seq?: number`。
- `shared/typst-generator.ts renderContentDoc`：每个项目段前注入 `#block(below:0.6em)[#text(weight:700, size:1.05em)[N) 项目名]]`（项目名取 `title||name`＝displayName，空名不出标题）。
- 项目名来源沿用 6.7 的 `displayName` 优先级（项目模板 `layout_options.project_name` 优先）。**所以"是否需要为每个项目设标题"＝需要**：填项目模板的 `project_name`，序号由生成端自动编、不存模板。

**验证（2026-06-19）**：client/server tsc 0 错；renderContentDoc smoke→typst→PNG 实测——结论表序号 1/2/3（弯曲 rowspan）与明细段「1) 密度」「2) 拉伸强度」「3) 弯曲强度&弯曲模量」逐一对应、加粗左对齐。**仍待**：真实取号端到端出报告回归（需实数据）。

### 6.7.10 「项目结论」面板移到左侧 + 必填阻断提交 ✅ 已实现（2026-06-19）

> 回应：① 结论面板原占编辑器上方整宽、遮挡右侧 PDF 预览；② 结论必填、未填不许提交审核。

- **面板移位**：`ProjectEditor.tsx` 把「本项目检测结论」从 `EditorSplit` 上方整宽 `<details>` 移到 **left 列顶部**（FieldEditor 之上，flex column；right 的 PDF 预览不再被压）；每条结论改成紧凑卡片（子项目名一行 + 来源/选择一行）。标题旁加 `QuestionCircleOutlined` 悬停说明：项目名＝报告章节标题、子项目名＝结论表显示（单项目可空·多子项目必填）、数据来源＝原始记录判定值。
- **必填校验**：新增 `shared/binding-integrity.validateProjectConclusions(project_name, conclusions)`（项目名必填 + ≥1 条结论且各有数据来源[空 literal 视为未选] + 多条时每条须填 sub_name）。面板内红字 + `status=error` 实时标注。
- **阻断提交审核**：`TemplateVersionPanel` 加 `submitGuard?: ()=>string|null`（`handleSubmit` 先跑，返回非空则 `message.error` 并不开提交弹窗），`ProjectEditor` 传入按 `conclErrors` 拦；**服务端兜底**——`report-templates POST /:id/submit` 对 `template_kind='project'` 载入版本 `layout_options` 跑同一校验，不过则 400（防客户端绕过）。
- **验证**：client/server tsc 0 错、vite build 通过；validateProjectConclusions 单测 6 例（空/有名无结论/空来源/迁移态单结论/多子项目缺名/齐全）输出符合预期；迁移后的项目模板 #10（密度 + 1 条 record_summary 结论）校验通过。

---

## 7. 退回 / 返工 + 全链路溯源 🟡 P-Flow-1 已实现（场景1 + 时间线）

**目标**：把三类"发现错误 → 退回上游修改"的流程统一管理，且每次"谁在哪个阶段、因为什么、改了哪里"都可溯源。

> **实现进度（2026-06）**：**P-Flow-1 已实现并通过端到端 API 验证**——`rework_tickets` 表（migration 020）+ 场景1（报告→录入退回）+ 全订单时间线 + 数据重审通过后 `reports.stale` + 自动关单。文件：`server/routes/rework.ts`、`record-data.ts` review 分支、`components/ReadonlyRecordViewer.tsx`（报告端「退回原始记录」按钮）、`pages/Report/Workbench.tsx`（订单时间线 Drawer）。P-Flow-2（外部反馈/场景3）、P-Flow-3（场景2并入）仍未实现。

### 7.1 三个场景

| # | 触发 | 从 → 到 | 现状 |
|---|---|---|---|
| 1 | 报告生成时文员发现原始记录有错 | 报告生成 → 数据录入(主检) | 🟢 **已实现（P-Flow-1）**：报告端「退回原始记录」建工单 + 置 rejected + 时间线 |
| 2 | 数据审核时审核员发现错 | 数据审核 → 数据录入(主检) | 🟢 已实现（`record_data` reject + 备注 + 审计；未并入统一工单 = P-Flow-3）|
| 3 | PDF 发外部系统后，外部要求修改 | 外部 → 报告生成(文员)，必要时再 → 录入(主检) | 🟡 **已实现（P-Flow-2）**：1.4 回传 + `/report-feedback` 回执 + `reports.external_status` 状态机 + 外部返工工单 + 文员分级处理（改/升级/驳回）|

### 7.2 统一模型：返工工单（rework_ticket）

三类退回本质相同——"在某阶段发现问题，退回某阶段，带原因，被处理，留痕"。用**一张工单表**统一，而不是三套机制：

```sql
CREATE TABLE rework_tickets (
  id SERIAL PRIMARY KEY,
  order_no       TEXT NOT NULL,
  scope          TEXT NOT NULL,        -- 'record' | 'report'
  record_data_id INTEGER,              -- scope=record 时
  report_id      INTEGER,              -- scope=report 时
  origin_stage   TEXT NOT NULL,        -- 'report_gen' | 'data_review' | 'external'
  target_stage   TEXT NOT NULL,        -- 'data_entry' | 'report_gen'
  raised_by_name TEXT, raised_by_role TEXT,
  reason         TEXT,                 -- 退回原因
  suggestion     TEXT,                 -- 修改建议（外部回执也放这）
  external_ref   TEXT,                 -- 场景3：外部系统的回执/建议 id
  parent_ticket_id INTEGER,            -- 链式：外部→文员工单，文员升级→录入工单(挂父)
  status         TEXT DEFAULT 'open',  -- 'open' | 'in_progress' | 'resolved'
  resolved_by_name TEXT, resolved_at TIMESTAMPTZ, resolution_note TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW()
);
```

每个工单只表达"退到哪、为什么"；"具体改了什么"复用已有的 `record_audit_log` / `report_audit_log`（字段级 diff + actor）。两者按 `order_no` 关联，拼成完整溯源。

### 7.3 三场景如何落到统一模型

- **场景 1**：文员在报告工作台点「退回原始记录」→ 建工单 `{scope:record, origin:report_gen, target:data_entry, record_data_id, reason}`，并把对应 `record_data.audit_status` 置 `rejected`（复用现有退回态）。主检在 /lab 看到待返工 → 改 → 重提 → 重审 → 文员重新生成报告。
- **场景 2**：保持现有 `record_data` reject 流；同时落一条工单 `{scope:record, origin:data_review, target:data_entry}` 以并入统一时间线（也可只读现有审计，不强制建工单）。
- **场景 3**：外部反馈"需修改" → 建工单 `{scope:report, origin:external, target:report_gen, report_id, suggestion, external_ref}` → 通知文员。文员处理：
  - **报告呈现问题** → 实例编辑器改 → resolve 工单 → 重新发外部（P2/P3 留痕）。
  - **判定是数据问题** → 文员「升级到录入」→ 建**子工单** `{scope:record, target:data_entry, parent_ticket_id}` → 主检改数据 → 重审 → 文员重生成 → resolve 父工单 → 重发外部。

### 7.4 外部反馈接口设计（场景 3）

```ts
// 出站：本系统 → 外部（发报告 PDF，复用第 3 节"审批系统对接"）
SubmitReport { order_no, report_id, report_no, pdf }

// 入站：外部 → 本系统（回执；可回调或我方轮询）
ExternalReportFeedback {
  order_no: string
  report_id?: number
  decision: 'approved' | 'needs_revision'
  suggestions?: { field?: string; text: string }[]   // 修改建议
  external_ref: string                                // 回执 id（防重、可追溯）
}
```

- **接缝**：`server/src/services/external-report-feedback.ts`（mock：提供一个手动触发端点模拟外部回执，便于演示）。
- `approved` → 报告状态置 `external_approved`（签发完成）。
- `needs_revision` → 建报告级工单 + 通知文员。
- 报告新增状态机：`generated → submitted_external → (external_approved | external_revision) → 重编辑/重生成 → submitted_external …`（在 `reports` 加 `external_status` 列）。

#### 取舍：外部退回时"分级处理" vs "同时通知文员和工程师"

**建议分级（文员先收，工程师可见但不抢动），理由**：

| | 分级（推荐） | 同时双通知 |
|---|---|---|
| 责任归属 | 每步单一 owner，清晰 | 两人都觉得"该谁弄"，易扯皮 |
| 合规链 | 数据改动**必经审核**（文员判定是数据问题才升级到主检走审核）| 工程师可能绕过报告上下文直接改数据 |
| 冲突 | 不会"工程师改数据的同时文员在改报告"——数据一变报告就失效 | 易并行冲突、重复返工 |
| 信息 | 外部多数是呈现层问题，文员能直接处理，无需惊动工程师 | 工程师被无关报告问题打扰 |

**但保留"共享可见"**：工单对文员和主检都可见（主检能看到"订单 X 有外部返工进行中"），只是**动作所有权分级**——外部回执先落到文员，文员三选一：自己改、升级到主检、驳回外部建议（带说明）。这样既单一责任、又信息透明。

### 7.5 可溯源：全订单时间线

新增 `GET /api/orders/:order_no/timeline`，按时间合并：
- `rework_tickets`（谁在哪阶段因为什么退回、被谁处理）
- `record_audit_log`（录入/审核/退回 + 数据快照 diff + actor）
- `report_audit_log`（生成/编辑 + 字段 diff + actor）
- （可选）模板版本事件

→ 一条"自始至终谁改了哪里、为什么"的完整链，前端在订单详情/报告页用 Timeline 展示。这就是"可溯源留痕"的统一出口。

### 7.6 配套：数据改动让旧报告失效

退回修数据后，已生成报告引用的是**旧锁定版本**，会过期。`record_data` 重审通过(新版本)时，把引用它的 `reports` 标 `stale=true`，报告列表提示"源数据已更新，请重新生成"。保证退回闭环的一致性。

### 7.7 实现要点 / 影响文件

| 改动 | 文件 |
|---|---|
| `rework_tickets` 表 + `reports.external_status` / `reports.stale` | 新 migration |
| 退回/工单 CRUD + 升级(建子工单) + resolve | 新 `server/routes/rework.ts` |
| 外部反馈接口（mock 触发 + 回执处理） | 新 `server/services/external-report-feedback.ts` + 路由 |
| 全订单时间线聚合 | 新 `GET /api/orders/:order_no/timeline` |
| 报告工作台「退回原始记录」按钮 + 待返工角标 | `client/pages/Report/{OrderList,Workbench}.tsx` |
| 录入页「待返工」列表（主检看 target=data_entry 的 open 工单）| `client/pages/Lab/TaskList.tsx` |
| 文员「外部返工」处理面板（改/升级/驳回） | `client/pages/Report/InstanceEditor.tsx` 或新组件 |
| 通知：按 `target_stage` ↔ 角色 派生待办角标（演示阶段不建独立通知表）| 顶部导航/各列表 |

### 7.8 分期

1. ✅ **P-Flow-1（已实现）**：`rework_tickets` 表 + 场景 1（报告→录入退回）+ 全订单时间线 + 数据重审 stale + 自动关单。落地最常用的退回 + 统一溯源。
2. ✅ **P-Flow-2（已实现，mock 外部）**：1.4 回传（`/requisitions/:id/deliver`）+ 外部回执 `/report-feedback`（mock 可手动触发）+ 报告 `external_status` 状态机（generated→submitted_external→external_approved\|external_revision）+ 场景 3 分级处理（文员「外部返工」面板：改报告 / 升级到录入(建 scope=record 子工单，复用 P-Flow-1) / 驳回留痕）。
3. **P-Flow-3**：场景 2 并入统一时间线（现有 reject 落一条工单）+ 待办通知角标。

### 7.9 尚未实现

- ✅ P-Flow-1（场景1 + 工单表 + 时间线 + stale + 自动关单）**已实现**。
- ✅ P-Flow-2（场景3：1.4 回传 + 回执接口 + `external_status` 状态机 + 分级处理）**已实现（外部为 mock）**。仍待：真实外部回执的**回调鉴权 / 轮询 / 防重**。
- P-Flow-3：场景2（现有 record_data reject）并入统一工单/时间线 + 角色级通知（演示先用角标派生，生产可接消息中心）。

---

## 8. 模板变更管理：母子联动 / 历史影响 / 映射一致性 🟡 部分已实现

> 这一节回答三个高频问题：**改一个模板能否连带改它的母 / 子模板？模板改了历史数据 / 报告会不会乱？原始记录模板改了报告映射要不要重配？**
> 其中"历史按版本锁定回放"**已实现**；"变更传播"和"映射引用完整性校验"**未实现**（后者已列在 `OPTIMIZATION.md` 🟠）。

### 8.1 现状与缺口

| 能力 | 现状 | 位置 |
|---|---|---|
| 派生 = 按值复制 + 血缘指针（不共享内容） | 🟢 已实现 | `parent_template_id` / `parent_version_id`（`db/migrations/014_template_versions.sql:46-60`）；`forkTemplate()` / `getLineage()`（`server/src/services/template-versions.ts:242-328`）|
| 版本锁定回放（历史数据 / 报告不被后续改模板影响） | 🟢 已实现 | `record_data.template_version_id`（`db/migrations/016_record_data_version_lock.sql:6`），录入时锁定（`server/src/routes/record-data.ts:90-104`），出报告按锁定版本渲染（`server/src/routes/reports.ts:227-279`）|
| 母子模板"变更传播"（一处改、多处同步） | 🔴 未实现 | 见 8.2 |
| 报告映射引用完整性校验（保存/审核期标红+告警） | 🟢 已实现（P-Tpl-1） | `shared/binding-integrity.ts` `validateReportBindings`；`GET /api/report-templates/:id/binding-check` + PUT/approve 响应带 `binding_warnings`；`ProjectEditor.tsx` 实时标红 |
| 改原始记录模板时的反向影响提醒（删/改编码前列出受影响报告模板） | 🔴 未实现 | 见 8.4 第 2 点（P-Tpl-2）|

### 8.2 问题一：母子模板"变更传播"（opt-in，未实现）

**默认不联动**是合规底线——派生按值复制、各自独立演进，`parent_*` 指针只表达血缘、不共享内容。**不做"共享字段 / 自动级联覆盖"**，原因：① 破坏"改一个模板绝不影响其他模板"；② 绕过每个模板各自的审核闸门；③ 与版本锁定相悖（会在子模板上凭空生出未经审核的新版本）。

需要"母版改了下游一起改"时，做成**受控传播 = 把一次字段级 diff 作为"建议草稿"批量落到选定目标，各自走审核生效**：

1. 工程师在模板编辑器改完字段、提交本版后，可选"推送本次改动到派生模板"，用 `getLineage()` 列出可选目标（默认母版 → 后代，也支持反向 / 指定子集）。
2. 对每个目标模板调用现有 `createDraft()`，把**同一处字段级改动**（依 `field_code` 定位）应用到该模板当前版本的 `field_definitions` 副本上，生成一个**新草稿**——不直接生效。
3. 每个草稿各自走 `submitForReview → reviewVersion`，由各模板审核人把关；留痕、`diff_from_prev`、版本号照常。
4. **冲突检测**：若目标模板该 `field_code` 已与源不同（已独立演进），标冲突 → 人工选择"覆盖 / 跳过 / 手动合并"，不盲目覆盖。

**接口草案**：`POST /api/record-templates/:id/propagate { from_version_id, field_codes[], target_template_ids[] }` → 返回 `[{ target_template_id, draft_version_id, conflict? }]`。报告模板同理走 `report-templates` 对应路由。复用 `forkTemplate`/`createDraft` 基建，不新增表。

### 8.3 问题二：历史数据 / 报告不受模板改动影响（已实现，记其边界）

- **已实现**：见 8.1 表。改模板 = 产生新版本，`current_version_id` 翻到新版；**只对"改之后"新录入的数据、新生成的报告生效**。存量 `record_data` 仍按其 `template_version_id` 锁定版本回放，已生成报告是 `content_doc` 快照，均不变、不乱。
- **不自动迁移历史**：把老数据迁到新格式属显式的人工"迁移 / 重录"决定，不自动批量改写。
- **与 7.6 的 `stale` 区分**：`stale` 是"**原始数据**被改"（退回重录、新版本）时给引用旧数据的报告打的失效标记；"**改模板**"不触发 `stale`——历史报告是按其锁定版本正确生成的快照，无需失效。**实现注意**：不要把模板版本翻新误接到 `stale` 标记逻辑上。

### 8.4 问题三：报告映射引用完整性校验 + 反向影响（未实现）

映射靠字符串编码指向原始记录字段（见 8.1）。**改原始记录模板时**：

- **不改编码**（增字段 / 改标签 / 改单位 / 改公式）→ 老映射照常命中，报告模板**无需重配**。
- **改名 / 删除编码** → 引用它的报告 binding 静默取空（`resolveBinding` 落到 `'—'`），那格变空且无告警 ← 要治理的缺口。

设计三件套：

1. **保存 / 审核期校验**：✅ **已实现（P-Tpl-1）**。`shared/binding-integrity.ts` 提供 `collectReportBindings()` + `buildRecordFieldIndex()` + `validateReportBindings(reportGroups, recordGroups)`：取 `report_templates.linked_record_template_id` 当前版本字段集（含矩阵 `param_code`、汇总 `row_id`），遍历报告所有 binding（`FieldDefinition.binding`、`result_table.cells/summary_rows/summary_cols[].binding`），指不到的返回失效项。接入点：`server/routes/report-templates.ts` 的 `GET /:id/binding-check` + PUT/approve 响应 `binding_warnings`；`client/pages/ReportTemplate/ProjectEditor.tsx` 实时标红 Alert。默认只告警不阻断保存。
2. **反向影响分析**：工程师改 / 删原始记录模板某编码、提交版本前，扫描所有 `linked_record_template_id` 指向本模板的报告模板，比对"将消失的编码"，返回"N 个报告模板 M 处映射将失效"+ 跳转链接，确认后再提交。
3. **受控重命名传播**：把字段编码当稳定标识；重命名时可选"自动更新所有引用它的报告映射"——对受影响报告模板各 `createDraft()` 把 binding 的旧编码改新编码、走审核（复用 8.2 传播机制），或拦下要求人工修。
4. **历史报告不受影响**：报告是快照、按数据锁定版本解释；校验只针对"以后用新版原始记录模板再出的报告"。

### 8.5 实现要点 / 影响文件

| 改动 | 文件 |
|---|---|
| 变更传播：按 `field_code` 把字段级 diff 套到目标模板、生成草稿 + 冲突检测 | `server/src/services/template-versions.ts`（扩展，复用 `createDraft`/`getLineage`）|
| 变更传播路由 `POST /api/record-templates/:id/propagate`（报告模板同理） | `server/src/routes/{record-templates,report-templates}.ts` |
| 传播目标选择 + 冲突解决 UI（基于族谱树勾选） | `client/src/components/.../TemplateVersionPanel.tsx`（或新组件）|
| binding 引用收集 + 字段集收集 + 校验函数 | `shared/typst-generator.ts`（或新 `shared/binding-integrity.ts`）|
| 报告模板保存 / 审核时跑校验、返回失效清单 | `server/src/routes/report-templates.ts` |
| 改原始记录模板提交前反向影响扫描 | `server/src/routes/record-templates.ts` |
| 编辑器标红失效 binding + 审核告警展示 | `client/src/components/ReportEditor/BindingEditor.tsx`、`FieldEditor/FieldPropsPanel.tsx` |

**兼容**：变更传播是新增的可选动作，不开启时模板行为完全不变；完整性校验默认只告警不阻断（可配置为审核硬卡），存量模板不受影响。

### 8.6 分期

1. ✅ **P-Tpl-1（已实现）**：报告映射引用完整性校验（保存 / 审核期标红 + 告警）——成本低、收益直接，先堵"映射静默取空"。
2. **P-Tpl-2**：改原始记录模板时的反向影响提醒（删 / 改编码前列出受影响报告模板）。
3. **P-Tpl-3**：母子模板变更传播（建议草稿 + 冲突检测 + 各自审核）+ 受控重命名传播。

### 8.7 尚未实现

- 变更传播（8.2）、反向影响提醒（8.4 第 2 点）、受控重命名传播（8.4 第 3 点）未实现。
- 已实现：版本锁定回放 + 派生血缘（8.1）、**报告映射引用完整性校验（P-Tpl-1，8.4 第 1 点）**。
- 编码重命名的自动映射更新依赖 8.2 的传播基建，需先落地传播。
