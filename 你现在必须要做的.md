# 你需要做的：

1. 了解这个系统的功能，最好能自己编造几个订单，完整走一遍从：创建原始记录模板，创建报告首页模板和绑定映射，创建报告项目模板和绑定映射，在数据录入时关联原始记录模板并且录入数据，最后生成报告时选择模板以及进行最后编辑。最后报告生成的时候，按照和业务系统对接的数据接口获取总共要出多少份报告和报告对应的样品以及项目（对应关系是：一份报告可能包含多个样品的检测，一个样品要检测多个项目，每个项目里的试样可能有多份，试样也就对应原始记录模板表格中的那种1/2/3的序号）。在过程中继续测试有没有什么bug，然后修改。给你参考文件在```/files/```目录下，里面有：1）已经填好数值的不同项目的原始记录模板，都是excel文件；2）有文件夹的三个原始记录是pdf，虽然已经填了数据，但是试验数据表的数据是从excel来的，为了方便你测试创建原始记录模板和数据录入的时候的excel导入功能是否正常。3）这些项目对应能生成的一份报告例子：```files/报告 C202512086592-01 C202513070222-G1_不合格_1#.doc```，你要保证最后生成的报告，如果是总共生成一份的话，要和这个一样，字体固定是仿宋GB2312；对于原始记录的话格式要求不严格，能看就行。在github上的代码里，你部署后应该有我设置的模板和订单，如果没有的话你自己创建一些虚拟的用于测试。

2. 一定要严格检查原始记录模板到报告项目模板的映射功能，其中最大的难点是，在设置原始记录模板的时候，表格试验数据里可能预先设置了试样数量，但是在实际实验中试样数量会变化，导致表格结构出错（因为他们的表格一般都很复杂，给你的例子是最简单的，经常有合并格子的出现，动态的试样数量会导致表格最终对齐出错）。试样数量变化也随之带来一个问题，在数据录入时怎么知道哪部分是可变区域，在项目模板绑定映射时怎么知道哪部分是可变区域？我现在的做法是，在原始记录模板编辑里面，设置一个试样区域，这个试样区域在数据录入的时候可以新增试样数量，在报告项目模板里面绑定映射的时候直接整条绑定。但是这个实现方法很丑陋，中间有各种各样的bug，你可以想想有没有更好的实现方式，主要是映射绑定的问题，项目模板不知道哪块是动态变化要自动读取和调整的。这部分一定要多做测试和改进。

3. 需要测试的功能点1: 正常来说一个样品下每个测试项目只会对应一份原始记录，但是他们可能会出现的情况是：一个测试项目会使用多种不同的测试标准或者测试方法。现在是把每种测试标准或者测试方法单独放在一个原始记录模板里面，就会导致数据录入的时候一个项目下绑定多个原始记录模板（我在模板管理中加入“项目组”功能其实也是为了方便管理这些同一个项目下不通测试方法的原始记录模板）。在这种情况下，他们的需求时，在数据录入的时候很多信息（基本就是原始记录里的“基本信息”这个区域）都是相同的，他们希望可以不重复录入。我现在的解决方法是，不同测试方法原始记录模板里面“基本信息”这个分区的字段全都设置成“允许被拉取”，在数据录入的时候，在同一个项目下如果绑定了多个测试方法的原始记录，只要录入了一份原始记录，录入其他原始记录的时候就可以从录入过的原始记录里拉取这些公共字段的值。希望你重点测试这个功能是否正常。

4. 需要测试的功能点2: 对于报告模板，包括首页和项目模板，会出现一个情况就是，可能是同样的模板（意味着对应同样的原始记录），但是因为主机厂（也就是供应的厂商不同），可能会有不同要求，比如这个厂商的多几个字段，另一个厂商的表格样式不一样。这就会出现多个报告/项目模板会对应同一份原始记录的情况。你首先要测试在报告模板部分关联和管理主机厂的功能是否正确；其次，在生成报告的时候，他们的逻辑是，在数据录入没有做完的时候，文员可以先编辑报告首页的信息，核对有没有问题，上传原始样品照片之类的操作，所以要先选首页模板进行编辑，之后所有数据录入都做完之后，就可以生成对应报告了，在首页模板编辑点击应用到已生成报告，就可以把这个编辑过的首页直接应用到下面对应的生成的每份报告。如果已经选择的首页模板有关联主机厂的话，最后下面生成报告使用的模板也默认可以自己带出对应主机厂的项目模板，但是允许文员调整使用其他模板。在这个场景下，你需要测试的第一个功能就是：这个主机厂带出的功能是否正常。第二个需要测试的功能是：每份报告是可以自由调整包括哪些样品的哪些项目的，你需要检查调整后是否出的报告正常。第三个需要测试的功能是：在每份报告的编辑里，被应用的首页模板是否正确，是否还能二次调整，生成的每个项目有没有什么问题，比如有没有映射配置不对导致的问题，或者是有没有图片拉取不正确之类的问题；测试能否比较自由地进行格式编辑，自由添加表格/图片分区的功能是否正确？尤其需要重点关注的就是每份报告编辑的舒适度，因为他们很可能做很多微调，尽量改进得像word一样，观察有没有什么bug，比如按上下键能不能移动光标位置，能不能换行，能不能调整字体字号间距，能不能换页等等功能。

5. 测试完之后，你需要打包成docker镜像进部署。你一定要注意的事情是，你在本地的数据库存的内容（也就是所有模板和订单），一般来说是不会迁移到docker里面的，我建议你保留几个比较好的模板作为给用户的例子，一起迁移到docker里，或者你访问内网重新构建几个，不能让用户没有基础例子。而且你打包docker的时候，一定要注意代码和数据库分离，保证你之后更新docker不会导致用户数据库里的内容消失。这点千万千万要注意。特别是用户在订单里上传的图片/附件，我现在好像是放在代码根目录下，你一定要自己修改位置，部署服务器的时候放在服务器其他位置，不然你换一次代码数据都会丢一次。在本机使用测试数据的时候默认和外部业务系统的接口是关闭的，你部署到服务器之前一定要切换成对应的接口配置。你可以使用

   ```
   id:admin
   pwd:demo
   ```

   或者：

   ```
   id:GDJL06567
   pwd:justin0425
   ```

   登陆系统，模拟环境和内网环境都可以。第二个是朱伟业的业务系统账号，是直接通过他们oa接口登陆的，第一个是我自己设置的账号，绕开了他们oa。

6. 关于系统传输包的大小，你可以等测试完mtu之后自己进行相应修改。

7. 写一份详细的使用手册。我已经生成了很基础的文字版，在```docs/递归智能报告系统_客户使用指南.md```，但是你一定要熟悉系统之后，自己截图添加扩充成图文版，不然无法交付，他们看不懂，也不知道系统怎么用。有时间的话最好每部分功能录制对应视频。这个过程能帮助你快速熟悉这个系统的功能和弱点在哪里，哪里还必须要改进。之后可能经常需要你去培训化学部的文员和实验室工程师怎么使用，所以你一定要熟悉。

8. 化学部对于系统自己记录的修改意见在```修改意见/化学报告系统（LIMS改造）修改意见登记表.xlsx```里面，你可以查看还有没有什么需要继续修改的。我自己记录的一些问题点零零散散整理在```修改意见/系统修改意见（本地记录）.md```，你也可以看看。

9. 把使用指南，系统背景说明，系统实现说明，所有修改意见记录，这些文档整理完之后放到中枢上。

# 对于你需要写的使用指南大概内容

说明这个系统的所有功能。主要流程是：创建原始记录模板，可以从其他模板派生子模板（之后就会存在母子关系）或者直接复制其他模板（这部分需要把所有选区功能说清楚，所有字段功能说清楚，所有排版细节说清楚，包括如何使用自由表格进行编辑，特别是设置试样区域，设置格子角色/类型/单位等，设置公式等功能，以及如果使用excel导入，需要填写导入文件的sheet名称，并且表格内的表头参数需要和sheet中对应否则导入时无法识别。对于公共字段，尤其重点的是可以举一个例子，创建一个测试组，组里是不同测试方法，基本信息字段默认设置为公共字段，在数据录入时会出现一个项目关联多个原始记录，也就是说一个项目会有多个测试方法的情况，只要填写了一个原始记录，其他原始记录的公共字段可以使用拉取获得，而不是重复填写）。

接下来的流程是，进行数据录入，在数据录入时可以修改排版，表格可以从excel直接复制，如果使用excel导入的话，识别不正确可以在导入卡片进行调整，还不正确直接excel复制，对于图片可以上传可调整尺寸/旋转，使用手机/平板可以直接拍照上传，电脑端则没有这个功能。最底下可以直接上传任何形式的附件，也可以下载和删除。

对于报告首页模板和项目模板，都可以添加对应的主机厂，并且可以建立项目组进行管理，例如同一个主机厂的首页和报告模板可以放进一个项目组中，但只是方便管理，并不一定需要项目组。首页模板和项目模板都需要设置映射。项目模板因为关联了原始记录，可以直接从原始记录拉取，此时映射已经配置好，后续修改可以在这个基础上进行修改。

最后生成报告，首先可以查看对应的原始记录是否正确，不正确可以在此处退回。可以编辑报告首页模板，并且首页模板会应用到每份报告中，但是在每份报告中可以继续调整首页。如果选择了对应主机厂的首页模板，对应每个报告编号生成的项目模板也会默认使用对应该主机厂的项目模板，但是仍然可以用户自行调整。每份报告可以自行调整样品/项目，在报告中可以查看对应的原始记录，从原始记录中直接复制表格/图片内容到生成报告中。可以自行再添加表格/图片。需要你整理出如何操作，例如如何进行编辑字体字号行距，编辑表格，签发区域位置等信息。


# 仍可能有bug需要修复的点：

1. 模板管理。因为数字化部和化学部对于模板管理的要求太多，包括从母模板派生子模板，一个模板修改某个字段之后其他模板同步修改，修改版本的修改痕迹保留等，现在模板管理变得比较复杂，后续这部分在使用时最可能出错，尤其是一个模板修改某个字段之后其他模板同步修改。
2. 在模板编辑中的各种格式设置例如设置行距/段前后距/字体字号等，可能会出现问题。组件卡片太多，可能出现卡片高度/宽度溢出或者嵌套鼠标滑动无效的bug。
3. 审核/撤回流程。在所有部分包括原始记录模板/报告首页和项目模板/数据录入/生成报告，都有送审/撤回/审核/删除的功能，每个部分的规则有细微差别，可能出现错误。之前工程师反馈希望增加批量审核的功能，但是这样会导致他们流程出问题，我已经拦截这个需求，后续如果有其他问题或者修改需要视情况进行调整。尤其是最后生成报告的送审和撤回，和外部业务系统接口对接，由于对方接口字段设置得极其粗糙，导致系统报告/订单的状态修改极易出错，并且这部分的接口测试不足，极易出错，需要后续重点调试和关注。
4. 在报告首页/项目模板设置映射时，映射值可能出现问题。
5. 最后生成报告时，如果用户自己调整每份报告对应的样品/项目，有可能出错。
6. 信息不对称。例如数据录入时已经关联了原始记录模板并且进行数据录入了，但是这份数据没有提交审核，此时修改原始记录模板，有可能出现问题。或者生成报告未提交，但是报告模板进行了修改。这类问题尤其难解决，因为涉及到机制问题。我已经尽量修复和避免，但可能还是会出现bug。
7. 在原始记录模板中，表格里的试样在数据录入时是可变的，在设置项目模板映射时很可能出现问题，最后拉取生成报告时可能表格结构也会出错。我已经测试了大部分情况，但是他们的情况太多了，很可能还是会有问题。可以想想有没有更加方便的方法，我现在的实现未必好。
8. 现在每个编辑器都设置了30s自动保存（他们要求的），但是经常会出现自动保存的时候状态更新，导致需要重新点击“开始编辑”才能继续。数据录入那部分更有问题，自动保存之后如果你继续编辑，再点“结束编辑”，会出现提示说冲突，有其他人已经保存。我已经修复过一次，但是有时还是会出现这个bug，你要重点修复。




# 第二期改进的点（实现优先级：第一级最高，第二级次之，以此类推）：

1. 所有模板数据填充后都是typst源码，后续ai应用会很容易读取和分析，比如ai自动审核等。（ai自动审核是化学部工程师最想做的）（实现优先级：第二级）
2. 目前只支持中文报告，后续要做英文报告，可以引入大模型来做，同时模板管理会存在一定的问题，例如中英文的模板如何关联，设置映射时如何保证中英文的模板映射字段一致（只设置一次映射但是两类模板都生效）。（实现优先级：第二级）
3. 报告首页模板工程师反映会有添加logo这种比较自由的操作，目前暂时不支持，可以考虑如何改进。（实现优先级：第二级）
4. 报告首页/项目模板也可以做成像最后生成报告编辑器一样，类似word的直接编辑方式。具体实现路径：自由编辑，如果是要配置映射的字段仍然使用添加字段的路径，只是直接添加到编辑画布中，字段名称/字段值位置可以随意修改，双击字段名/字段值进入编辑。这个改进本身不难做，只是需要一定工程量，改进之后报告的格式编辑会更加简单直观，但是集成度没有现在高，所以我没有贸然改进。可以在后续上线使用后根据工程师反馈继续改进，如果他们对于报告首页/项目模板的格式编辑确实很需要可以改成这样。修改之后第三条的logo添加也可以解决，实现路径可以是允许用户在自由编辑器中添加图片自由拖动。原始记录模板也可以做类似修改，但是从走访中来看原始记录模板的格式似乎无人在意，统一口径是能看就行，所以可能原始记录模板编辑这部分保证可用好用即可。（实现优先级：第一级）
5. 可以添加一个ai小助手回答问题比如怎么使用这个系统，化学实验室设置的限值是否正确等，不需要太大的模型，可以使用外部模型（这点待定，只是我觉得这部分不涉及机密，所以用外部模型应该没问题，但是要找李辉旺申请外网白名单）。（实现优先级：第三级）
6. 手机移动端和平板端的适配。根据数字化部和化学部共同要求，现在数据录入上传图片时，在移动端和平板端是可以唤起相机拍照上传的，但是界面没有做适配，在平板端横屏的还好，在手机端竖屏会出现排版错乱的情况。（实现优先级：第一级）
7. 批量上传图片，批量审核/退回。
8. 接口标准化平台，把系统的所有接口集成到平台，方便其他事业部调用。
9. 实验室的设备数据直接接进来，做一个设备对接管理平台，之后数据录入时不需要手动填写或者excel导入。
10. 如果大幅度推广之后，要做数据隔离，权限管理（避免不同事业部的数据混乱）。


