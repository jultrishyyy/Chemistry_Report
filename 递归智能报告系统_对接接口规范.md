# 化学报告系统 — 接口文档

接口按提供方各自实现，协议如下：

- **1.1 / 1.2 / 1.3（REST）**：委托单、报告信息、报告修改信息推送 —— 报告系统提供 REST 端点，业务系统以 HTTP POST + JSON 调用。
- **1.4 / 1.5 / 1.6（SOAP WebService）**：报告 PDF 回传、撤销报告送审、材料任务状态更新 —— 业务系统提供 WCF SOAP 端点，报告系统调用。
- **登录（REST）**：用户登录认证 —— 原认证系统提供。

---

## 一、接口清单

| 编号 | 接口（方法 / 路径） | 协议 | 调用方 → 提供方（提供方＝实现服务端） | 用途 |
|---|---|---|---|---|
| 1.1 | `POST /api/external/orders` | REST | 业务系统 → 报告系统 | 推送委托单（样品、检测项目） |
| 1.2 | `POST /api/external/reports` | REST | 业务系统 → 报告系统 | 推送报告信息（编号、检验码、抬头、报告范围） |
| 1.3 | `POST /api/external/report-modify` | REST | 业务系统 → 报告系统 | 推送报告修改信息（改号 / 退回修改环节） |
| 1.4 | `AcceptReportFromDiGui`（SOAP 方法） | SOAP | 报告系统 → 业务系统 | 回传报告 PDF（参数 sysNumber + file + jobNo） |
| 1.5 | `CancelFlowFromDiGui`（SOAP 方法） | SOAP | 报告系统 → 业务系统 | 撤销已回传报告的外部送审流程 |
| 1.6 | `UpdateMaterialTaskState`（SOAP 方法） | SOAP | 报告系统 → 业务系统 | 原始记录审核完工后更新材料任务状态 |
| 5.1| `GET /commonLogin/login` | REST | 报告系统 → 原认证系统 | 用户登录认证 |

**实现分工：**
- **1.1 / 1.2 / 1.3**：报告系统提供 **REST 端点（HTTP POST + JSON）**，业务系统以 HTTP 客户端调用。无 WSDL。
- **1.4 / 1.5 / 1.6**：业务系统提供同一个 **WCF SOAP 端点**（当前配置示例为 `http://172.18.0.97:8003/Lab/Chemistry`），报告系统作 SOAP 客户端调用。部署前必须以实际 WSDL 核对端点、命名空间、SOAPAction、参数名大小写和 SOAP 版本。
- **5.1**：原认证系统提供 REST 接口，报告系统服务端调用。

---

## 二、通用约定

### 新服务器部署地址（2026-09-16）

- 报告系统 IP：`172.19.10.12`。按仓库 `pnpm serve:server` 默认端口 5173，REST Base URL 为 `http://172.19.10.12:5173`；若生产使用反向代理或其他端口，以实际入口为准。
- 对方需更新三个入站地址：`http://172.19.10.12:5173/api/external/orders`、`http://172.19.10.12:5173/api/external/reports`、`http://172.19.10.12:5173/api/external/report-modify`。
- 对方 SOAP 地址保持 `http://172.18.0.97:8003/Lab/Chemistry`，认证地址保持原配置。不能把对方地址替换为本系统新 IP。
- 对方防火墙/白名单需允许新服务器 `172.19.10.12` 出站访问 SOAP；本系统需允许对方访问上述 REST 端口。
- `config/interfaces.server.json` 中 `public_base_url` 同步改为实际访问入口；示例已更新为 `http://172.19.10.12:5173`。实际文件为部署私有配置，不由仓库修改。

**全部接口通用：**

| 项 | 约定 |
|---|---|
| 传输 | HTTP 或 HTTPS，以部署环境实际地址为准；生产推荐 HTTPS。当前业务系统示例端点为内网 HTTP |
| 鉴权 | 当前接口文档未定义应用层鉴权；安全由内网 / 专线 / IP 白名单保障。若生产启用 HTTPS 或额外鉴权，双方另行确认 |
| 字符编码 | UTF-8 |
| 时间格式 | ISO8601，如 `2026-06-15T10:06:13` |
| 幂等键 | 委托单以 `OrderNumber`、报告以 `SysNumber`、材料任务以 `TaskId` 为唯一键；同一键重复调用须可安全重放 |
| 关联锚点 | 报告（1.2）与委托单（1.1）通过 **样品名 `SampleName` + 检测项目名 `ProjectName`** 关联 |
| 空值 | 缺省字段可不传或传 `null` |

**REST 接口（1.1 / 1.2 / 1.3 + 登录）：**

| 项 | 约定 |
|---|---|
| 方法 | 1.1/1.2/1.3：`POST`；登录：`GET` |
| 请求体 | POST：`Content-Type: application/json`，body 为业务 JSON（即各接口字段表，**直接作为请求体，不再包成字符串**）；登录 GET：`application/x-www-form-urlencoded`，参数走 query |
| 响应 | `Content-Type: application/json`，body 为 JSON（见各接口「返回」） |
| 成功/失败 | HTTP 2xx = 成功；响应体 `ok` 字段亦标识成败 |

**错误响应（REST 1.1 / 1.2 / 1.3 统一约定）：**

所有 REST 接口的错误响应均为 **JSON**，形态统一为 `{ "ok": false, "error": "<原因>" }`（404 另附 `path`），**绝不返回 HTML**。按 HTTP 状态码区分责任方：

| HTTP | 含义 | 典型触发 | 响应体示例 |
|---|---|---|---|
| 200 | 处理完成 | 正常；**批量接口即使部分条目失败也回 200**，逐条成败在 `results[].ok` / `results[].error` 标识 | `{ "ok": true, "results": [ … ] }` |
| 400 | 请求非法（**调用方**问题） | 请求体不是合法 JSON；缺必填（如 `OrderNumber` / `SysNumber`）；无可解析数据 | `{ "ok": false, "error": "请求体非法：…" }` |
| 404 | 资源不存在 | 路径不存在；或定位不到目标（如回传时取号单不存在） | `{ "ok": false, "error": "Not Found", "path": "…" }` |
| 500 | 服务器内部错误（**提供方**问题） | 报告系统内部异常（数据库等）。**不泄露内部细节**，完整堆栈见报告系统服务端日志 | `{ "ok": false, "error": "服务器内部错误：…" }` |
| 502 | 下游失败 | 1.4 / 1.5：调用业务系统 SOAP 失败 | `{ "ok": false, "error": "回传失败：…" }` |

> **调用方处理建议**：① 先看 **HTTP 状态码**判断责任方——`4xx`=请求侧问题（改正后重发），`5xx`=报告系统侧问题（可稍后重试或联系对接人）；② 批量接口（1.1 / 1.2 / 1.3）**务必遍历 `results[]` 逐条核对 `ok`**，不要仅凭顶层 HTTP 200 / `ok:true` 认为每条都成功。

**SOAP 接口（1.4 / 1.5 / 1.6）：**

| 项 | 约定 |
|---|---|
| 协议 | SOAP over HTTPS；版本由业务系统的 WSDL 确定（SOAP 1.1：`text/xml` + `SOAPAction`；或 SOAP 1.2：`application/soap+xml`） |
| 方法签名 | 见 §4.4～§4.6；均为 WCF 方法、平铺参数，SOAP 方法返回 `string`，字符串内容为 JSON |
| WCF 命名空间 | 当前按 `http://tempuri.org/`；SOAPAction 当前按 `http://tempuri.org/IChemistryService/{方法名}`，最终必须以部署环境 WSDL 为准 |
| 报文大小 | 报告 PDF 以 Base64 内联在 `file` 中，单份可达数 MB，服务端消息大小上限建议 ≥ 20 MB |
| Base64 | `file` 为 PDF 原始字节的标准 Base64（无 `data:` 前缀、无换行） |
| jobNo | 1.4 / 1.5 取实际发起操作的当前登录账号工号（HTTP 头 `X-User-Job`），业务系统据此审计 |
| 成功判定 | 1.5 必须同时满足 `Msg=OK`、`RecordState=草稿`；1.6 必须满足 `Msg=OK`；仅 HTTP 2xx 不代表业务成功 |

---

## 三、服务地址

| 接口 | 提供方 | 类型 | 地址（测试环境） | 地址（生产环境） |
|---|---|---|---|---|
| 1.1 / 1.2 / 1.3 | 报告系统 | REST Base URL | （部署后填写） | （部署后填写） |
| 1.4 / 1.5 / 1.6 | 业务系统 | SOAP WSDL / 端点 | （部署后填写） | （部署后填写） |
| 5.1 | 原认证系统 | REST Base URL | （填写） | （填写） |

> REST 接口的完整地址 = 上方 Base URL + 各接口路径（见接口清单 / §四）。

---

## 四、报告数据接口

### 4.1 推送委托单 — `POST /api/external/orders`（REST）

- **调用方 → 提供方**：业务系统 → 报告系统
- **请求**：`POST {REST Base URL}/api/external/orders`，`Content-Type: application/json`
- **幂等键**：`OrderNumber`

**请求体 — 订单级字段（JSON）：**

| 参数名 | 变量名 | 类型 | 说明 |
|---|---|---|---|
| 委托单号 | OrderNumber | String | 唯一键 |
| 委托单位 | CompanyName | String | |
| 委托单位地址 | CompanyAddress | String | |
| 送检日期 | SendDate | Datetime | |
| 客户要求期限 | TimeRequired | Datetime | |
| 检测要求期限 | TestTimeRequired | Datetime | |
| 报告期限 | ReportDeadLine | Datetime | |
| 证书单位 | Authorites | String | |
| 英文证书单位 | EnglishAuthorites | String | |
| 证书单位地址 | AuthoritesAddress | String | |
| 英文证书单位地址 | EnglishAuthoritesAddress | String | |
| 备注 | Remark | String | |
| 业务员 | SaleName | String | |
| 业务员工号 | JobNo | String | |
| 买家 | Buyer | String | |
| 委托单状态 | Status | String | |
| 是否出中文报告 | IsChineseReport | Bool | |
| 是否出英文报告 | IsEnglishReport | Bool | |
| 是否出纸质报告 | IsPaperReport | Bool | |
| 报告数量 | ReportCount | String | |
| 其他报告数量 | OtherReportCount | String | `ReportCount` 为「其他」时有值 |
| 完工方式 | CompleteWay | String | |
| 样品及检测项目 | SampleList | Array | 见下 |

**`SampleList[]` — 每个样品：**

| 参数名 | 变量名 | 类型 | 说明 |
|---|---|---|---|
| 样品名称 | SampleName | String | |
| 样品条码 | BarCode | String | |
| 序号 | SampleSortNo | String | 如 `1#` |
| 型号 | Model | String | |
| 材料分单 | TaskList | Array | 见下 |

**`TaskList[]` — 每个材料分单（检测项目）：**

| 参数名 | 变量名 | 类型 | 说明 |
|---|---|---|---|
| 任务 ID | TaskId | String | 材料任务唯一标识；报告系统在 1.6 中原样回传，必须稳定且非空 |
| 检测项目 | ProjectName | String | |
| 主机厂 | MainEngineFactory | String | |
| 标准号 | StandardNo | String | |
| 测试方法 | TestMothed | String | |
| 测试条件 | TestCondition | String | |
| 制样方式 | SamplingMode | String | |
| 制样要求 | SamplingRequirement | String | |
| 限值名称（牌号） | LimitName | String | |
| 限值内容 | LimitContent | String | |
| 分单负责人 | Leader | String | |
| 开始测试日期 | StartDate | Datetime | |
| 结束测试日期 | EndDate | Datetime | |
| 制样备注 | Remark | String | |
| 样品描述 | SampleDescription | String | |
| 检测备注 | TestRemark | String | |
| 分单人 | MaterialUploader | String | |

**请求体示例：**
```json
{
  "OrderNumber": "C202307190320",
  "CompanyName": "浙江万盾制冷股份有限公司",
  "SendDate": "2026-06-15T10:06:13",
  "SampleList": [
    {
      "SampleName": "化学样品1", "SampleSortNo": "0#", "BarCode": "C202307190320-111", "Model": null,
      "TaskList": [
        { "TaskId": "687321", "ProjectName": "拉伸性能", "StandardNo": "GB/T 1040.1-2025", "TestMothed": "...", "Leader": "...", "MaterialUploader": "..." }
      ]
    }
  ]
}
```

**返回（JSON）：**
```json
{ "ok": true, "count": 1, "results": [ { "order_no": "C202307190320", "action": "inserted", "warnings": [] } ], "warnings": [] }
```
- `action`：`inserted`（新建）/ `updated`（更新已有委托单）。

**错误示例：**
```jsonc
// 400 缺委托单号（请求非法）
{ "ok": false, "error": "请求体非法：OrderNumber（委托单号）不能为空" }
// 400 请求体非合法 JSON
{ "ok": false, "error": "请求体 JSON 解析失败" }
// 400 没有可解析的委托单（空数组）
{ "ok": false, "error": "没有可解析的委托单" }
// 500 报告系统内部异常（不泄露内部细节，详情见服务端日志）
{ "ok": false, "error": "服务器内部错误：委托单接收失败" }
```

---

### 4.2 推送报告信息 — `POST /api/external/reports`（REST）

- **调用方 → 提供方**：业务系统 → 报告系统
- **请求**：`POST {REST Base URL}/api/external/reports`，`Content-Type: application/json`
- **幂等键**：`SysNumber`（每份报告一个）
- **请求体结构**：`{ "OrderNumber": "...", "ReportList": [ 每份报告 ] }`

**`ReportList[]` — 每份报告字段：**

| 参数名 | 变量名 | 类型 | 说明 |
|---|---|---|---|
| 系统编号 | SysNumber | String | 报告唯一键（1.3 / 1.4 以此对账） |
| 报告编号 | ReportNumber | String | 可被 1.3 变更 |
| 检验码 | CheckCode | String | |
| 语种 | Language | String | 中文 / 英文 |
| 样品名称 | SampleName | String | 该报告主样品 |
| 签发日期 | SecondAuditeDate | Datetime | |
| 客户名称 | CustomerName | String | |
| 客户地址 | CustomerAddress | String | |
| 报告备注 | ReportRemark | String | |
| 报告备注（英文） | ReportRemarkEN | String | |
| 资质备注 | QualificationRemark | String | CMA / CNAS 等 |
| 资质备注（英文） | QualificationRemarkEN | String | |
| 公司名称 | CompanyName | String | 出具机构抬头 |
| 公司名称（英文） | CompanyNameEN | String | |
| 公司地址 | CompanyAddress | String | |
| 公司地址2 | CompanyAddress2 | String | |
| 公司地址（英文） | CompanyAddressEN | String | |
| 公司地址2（英文） | CompanyAddress2EN | String | |
| 传真 | CompanyFax | String | |
| 传真（英文） | CompanyFaxEN | String | |
| 电话 | CompanyTel | String | |
| 电话（英文） | CompanyTelEN | String | |
| 样品及检测项目 | SampleList | Array | 结构同 1.1 的 `SampleList` / `TaskList`，用于界定本报告覆盖范围 |

**返回（JSON）：**
```json
{ "ok": true, "count": 2, "results": [ { "sys_number": "C202307190320-2", "report_number": "C202307190320-2-G1", "matched": 3, "unresolved": 0, "auto_generated": true, "report_id": 42 } ], "warnings": [] }
```
- `matched`：已匹配到检测数据的「样品 × 项目」数；`unresolved`：尚未匹配数。
- `auto_generated`：兼容字段。当前流程需在报告工作台确认范围和项目模板后生成，不应依赖接收 1.2 自动完成报告；`report_id` 以实际生成结果为准。

**错误示例：**
```jsonc
// 400 缺委托单号（请求非法）
{ "ok": false, "error": "请求体非法：OrderNumber（委托单号）不能为空" }
// 400 ReportList 为空
{ "ok": false, "error": "请求体非法：ReportList 为空（没有报告取号信息）" }
// 400 请求体非合法 JSON
{ "ok": false, "error": "请求体 JSON 解析失败" }
// 500 报告系统内部异常（不泄露内部细节，详情见服务端日志）
{ "ok": false, "error": "服务器内部错误：报告取号接收失败" }
```

---

### 4.3 推送报告修改信息（改号 / 退回修改环节）— `POST /api/external/report-modify`（REST）

- **调用方 → 提供方**：业务系统 → 报告系统
- **请求**：`POST {REST Base URL}/api/external/report-modify`，`Content-Type: application/json`
- **幂等键**：`SysNumber`
- **作用**：一次调用可同时完成 ① 改号 / 更新报告元数据（签发时间、审核状态、备注），② 按 `ModifyType` 触发退回环节。
- **退回仅对「已生成报告」有效**；若该取号单尚未生成报告，则只记录改号/元数据、退回环节被忽略并在结果里返回 `warning`。
- **支持单条对象或数组**：数组逐条处理、逐条返回结果，互不影响（单条失败不影响其它条）。

**请求体字段（《递归智能接口文档》1.3 节，共 6 个，字段顺序与官方一致）：**

| 参数名 | 变量名 | 必填 | 类型 | 说明 |
|---|---|:--:|---|---|
| 系统编号 | SysNumber | 是 | String | 定位报告（幂等键） |
| 报告编号 | ReportNumber | 否 | String | 改号：**给了才更新**为新编号；为空不动 |
| 审核状态 | RecordState | 否 | String | 外部审核状态：`草稿` / `审核中` / `审核通过` / `审核不通过`，原样记录到 `record_state` |
| 签发时间 | SecondAuditDate | 否 | Datetime | 报告签发/复核时间，本系统截取为日期 `YYYY-MM-DD` 存入 `issue_date` |
| 修改类型 | ModifyType | 否 | String | 退回环节，三选一：**缺省/空**＝仅改号或更新元数据、不退回；`report_edit`＝退回给文员修改报告；`data_entry`＝退回给实验室重录检测数据（语义见下表） |
| 修改备注 | Remark | 否 | String | 退回原因 / 修改说明；落入退回工单与数据审核的 `reject_note` |

**`ModifyType` 语义：**

| 取值 | 行为 | 对接方后续 |
|---|---|---|
| 缺省 / 空 | 改号 / 更新元数据；实际改号才置 `stale`，外部审核状态按下述规则处理 | —— |
| `report_edit` | 报告置「退回修改」态 + 建**报告级**退回工单 → **文员**在工作台编辑该报告 | 文员改完重新送审 → 1.4 回传新 PDF |
| `data_entry` | 把该报告引用的**全部检测数据**置「退回」态、逐条建数据录入退回工单 → **实验室工程师**重录；此期间该报告在文员侧被**锁定**（不能重新生成/送审），须等数据重审通过后自动解锁、再重新生成 | 数据重审通过 → 报告重新生成 → 1.4 回传新 PDF |

**请求体示例（数组，可单条；取自官方文档 1.3 节示例）：**

补充状态规则：`RecordState` 去除首尾空格后按“草稿 / 审核中 / 审核通过 / 审核不通过”处理，非法状态或非空非法 `ModifyType` 返回逐条失败。未传 `RecordState` 不会重复执行历史状态。无 `ModifyType` 时，已送审报告收到“审核不通过”或“草稿”进入报告退回修改；收到“审核通过”进入签发终态。没有本地送审依据的报告不会被仅靠状态回包锁定。明确 `ModifyType` 优先于状态文字。

仅状态/备注回执不自动设置 `stale`；实际改号或退回才需要重出。`regenerate_needed` 是兼容提示字段，页面以报告实际状态为准。
```json
[
  {
    "SysNumber": "C202502130848-1",
    "ReportNumber": "C202502130848-1-G1",
    "RecordState": "审核通过",
    "SecondAuditDate": "2023-07-26T23:59:59",
    "ModifyType": "data_entry",
    "Remark": "加测Solvents (Migration)溶剂迁移项目"
  }
]
```

**返回（JSON）：**
```json
{
  "ok": true,
  "results": [
    { "sys_number": "C202502130848-1", "ok": true, "report_number": "C202502130848-1-G1", "issue_date": "2023-07-26", "record_state": "审核通过", "modify_type": "data_entry", "regenerate_needed": true, "rejected_records": 3 }
  ]
}
```

**返回字段（逐条）：**

| 字段 | 说明 |
|---|---|
| sys_number / ok | 该条的系统编号 + 是否成功 |
| report_number / issue_date / record_state | 更新后的报告编号 / 签发日期 / 审核状态 |
| modify_type | 实际触发的退回环节（`data_entry` / `report_edit`；纯改号时不返回此字段） |
| regenerate_needed | `true`＝该报告需重新生成 / 重出（已生成报告改号或被退回后） |
| rejected_records | 仅 `data_entry`：被退回的检测数据条数 |
| warning | 提示信息，如「该取号单尚未生成报告，仅记录改号/元数据，退回环节忽略」 |
| error | 该条失败时的错误信息（如 `SysNumber 必填` / `未找到该取号单`） |

**错误示例：**
> ⚠️ 1.3 是**批量逐条**接口：**单条失败不影响整体**，HTTP 仍为 **200**，失败信息在对应 `results[].ok=false` + `error` 里。调用方务必逐条核对，勿仅凭顶层 `ok:true` 认为全部成功。
```jsonc
// 200 逐条结果：第 1 条成功、第 2 条定位不到、第 3 条缺 SysNumber
{
  "ok": true,
  "results": [
    { "sys_number": "C202502130848-1", "ok": true, "report_number": "C202502130848-1-G1", "modify_type": "report_edit", "regenerate_needed": true },
    { "sys_number": "NOPE-999", "ok": false, "error": "未找到该取号单" },
    { "sys_number": "", "ok": false, "error": "SysNumber 必填" }
  ]
}
// 200 已退回但报告尚未生成（退回环节被忽略，仅记录元数据）
{ "ok": true, "results": [ { "sys_number": "C20250213-9", "ok": true, "regenerate_needed": false, "warning": "该取号单尚未生成报告，仅记录改号/元数据，退回环节忽略" } ] }
// 400 请求体非合法 JSON
{ "ok": false, "error": "请求体 JSON 解析失败" }
// 500 整体性服务器内部异常（如取数据库连接失败；单条异常不会走到这里）
{ "ok": false, "error": "服务器内部错误：报告修改/退回失败" }
```

---

### 4.4 接收报告（回传 PDF）— `AcceptReportFromDiGui`（SOAP）

- **调用方 → 提供方**：报告系统 → 业务系统
- **方法签名**：`string AcceptReportFromDiGui(string sysNumber, string file, string jobNo)` —— **平铺三个字符串参数**
  - 方法名为 **`AcceptReportFromDiGui`**，当前配置 SOAPAction 为 `http://tempuri.org/IChemistryService/AcceptReportFromDiGui`。本次未访问对方实时 WSDL，部署时仍须核对。
  - ⚠️ 三个参数缺一不可，**少 `jobNo` 业务系统会拒收**。
- **幂等键**：`sysNumber`
- **调用时机**：报告生成完成、报告改号或退回重生成后

| 参数名 | 变量名 | 必填 | 类型 | 说明 |
|---|---|:--:|---|---|
| 系统编号 | sysNumber | 是 | String | 来自 1.2 的 `SysNumber` |
| 报告文件 | file | 是 | String | 报告 PDF 的 Base64 字符串（无前缀、无换行） |
| 操作人工号 | jobNo | 是 | String | 当前登录并点击送审的操作人工号，与撤回一致；不是委托单业务员的替代工号 |

**返回（`string`）：** 必须包含非空的 SOAP 方法 Result，可返回回执号（如 `{ "ref": "..." }`）或 `{ "Msg": "OK" }`。SOAP Fault、非 2xx、缺少 Result，以及 JSON 中明确的失败标志均不算成功。不能只凭 HTTP 200 判定接收成功。

**SOAP 信封示例（SOAP 1.1，最终以业务系统 WSDL 为准）：**
```xml
POST {1.4 端点 URL}
Content-Type: text/xml; charset=utf-8
SOAPAction: "http://tempuri.org/IChemistryService/AcceptReportFromDiGui"

<?xml version="1.0" encoding="utf-8"?>
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <AcceptReportFromDiGui xmlns="{targetNamespace}">
      <sysNumber>C202502130848-1</sysNumber>
      <file>JVBERi0xLjcK…（PDF 的 Base64 字符串）</file>
      <jobNo>GDJL02859</jobNo>
    </AcceptReportFromDiGui>
  </soap:Body>
</soap:Envelope>
```

---

### 4.5 撤销报告送审 — `CancelFlowFromDiGui`（SOAP）

- **调用方 → 提供方**：报告系统 → 业务系统
- **用途**：报告系统已经通过 4.4 回传 PDF、但外部尚未审核通过时，撤销业务系统中的送审流程，使该报告恢复为可编辑、可重新送审状态。
- **方法签名**：`string CancelFlowFromDiGui(string sysNumber, string jobNo)` —— 平铺两个字符串参数。
- **传输协议**：SOAP over HTTP(S)，与 4.4 使用同一个业务系统 WCF 服务端点；本文档中“HTTP”指承载 SOAP 的 HTTP，不是 JSON REST。
- **SOAPAction**：`http://tempuri.org/IChemistryService/CancelFlowFromDiGui`（最终以业务系统 WSDL 为准）。
- **幂等键**：`sysNumber`。网络超时后报告系统可能重试；若该报告已经成功撤回，业务系统应仍返回成功状态，不能将其视为错误。

**调用前置条件：**

1. 报告已通过 4.4 成功回传（报告系统状态为“已送审 / 外部审核中”）；
2. 外部审核尚未通过；审核通过是终态，不允许撤回；
3. `jobNo` 必须为实际点击撤回用户的业务员工号。

| 参数名 | 变量名 | 必填 | 类型 | 说明 |
|---|---|:--:|---|---|
| 系统编号 | `sysNumber` | 是 | String | 来自 1.2 的 `SysNumber`，用于定位唯一报告 |
| 当前操作人工号 | `jobNo` | 是 | String | 实际发起撤回的业务员工号，用于业务系统审计 |

**SOAP 信封示例（SOAP 1.1）：**

```xml
POST {业务系统 WCF 端点}
Content-Type: text/xml; charset=utf-8
SOAPAction: "http://tempuri.org/IChemistryService/CancelFlowFromDiGui"

<?xml version="1.0" encoding="utf-8"?>
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <CancelFlowFromDiGui xmlns="http://tempuri.org/">
      <sysNumber>C202502130848-1</sysNumber>
      <jobNo>GDJL02859</jobNo>
    </CancelFlowFromDiGui>
  </soap:Body>
</soap:Envelope>
```

**返回值（SOAP 方法的字符串结果，内容为 JSON）：**

```json
{ "Msg": "OK", "RecordState": "草稿" }
```

报告系统仅在同时满足以下两项时，才确认撤回成功并更新本地状态：

1. `Msg`（大小写不敏感）等于 `OK`；
2. `RecordState` 等于 `草稿`。

确认成功后，报告系统将该报告恢复为“待送审 / 可编辑”，并记录外部原始回执用于对账。HTTP/SOAP 调用失败、`Msg` 非 `OK`、缺少状态、或返回状态不是 `草稿` 时，报告系统必须**保留原“已送审”状态**，仅记录错误并允许用户重试，不能单方面撤回。

**失败回包建议：**

```json
{ "Msg": "FAILED", "RecordState": "审核通过", "Error": "报告已审核通过，不可撤回" }
```

后续外部审核通过、审核不通过、改号或退回修改，仍通过 4.3 `POST /api/external/report-modify` 同步；4.5 只负责“撤回命令”及其同步确认。

---

### 4.6 更改单项完工状态 — `UpdateMaterialTaskState`（SOAP）

- **调用方 → 提供方**：报告系统 → 业务系统
- **用途**：某个材料任务的数据录入已全部审核通过后，通知业务系统更新该任务的完工状态。
- **方法签名**：`string UpdateMaterialTaskState(string taskId, int testState)` —— 平铺两个参数。
- **传输协议**：SOAP over HTTP(S)，与 4.4、4.5 使用同一个业务系统 WCF 服务端点。原接口文档所写“通讯协议 HTTP、输入 text”是 SOAP 的 HTTP 承载及方法字符串返回值，**不是 JSON REST 接口**。
- **SOAPAction**：`http://tempuri.org/IChemistryService/UpdateMaterialTaskState`（最终以业务系统 WSDL 为准）。
- **幂等键**：`taskId + testState`。调用可能因网络超时或后台重试而重复到达，业务系统必须支持幂等处理。

| 参数名 | 变量名 | 必填 | 类型 | 说明 |
|---|---|:--:|---|---|
| 任务 ID | `taskId` | 是 | String | 原样取自 1.1 `SampleList[].TaskList[].TaskId` |
| 状态 | `testState` | 是 | Int32 | `0`＝未完工，`1`＝完工；当前报告系统在审核完工时发送 `1`，客户端实现同时支持 `0` |

**报告系统的触发规则：**

1. 单条原始记录审核通过，或多方法录入批次中的记录审核通过；
2. 按 `(OrderNumber, BarCode/样品内部 ID, ProjectName)` 找到同一材料任务；
3. 该任务下所有**未取消**的原始记录均为“审核通过”后，才发送 `testState=1`。批次只通过部分测试方法时不提前发送；
4. 通过 1.1 保存的 `TaskId` 调用接口；旧订单或上游未传 `TaskId` 时记录明确错误，不使用项目名冒充任务 ID；
5. `(taskId, testState)` 在本地唯一，成功后不重复发送；失败写入持久化队列，首次 5 分钟后按指数退避重试，最长间隔 6 小时；服务重启后继续处理；
6. 外部系统不可用不回滚已经完成的本地审核，审核响应中的 `task_state_updates` 会标识 `sent` / `mock` / `queued` / `failed` / `not_complete` / `missing_task_id` / `already_sent`。`mock` 仅表示演示调用，通知仍保留待发，不能视为外部已更新。

**SOAP 信封示例（SOAP 1.1）：**

```xml
POST {业务系统 WCF 端点}
Content-Type: text/xml; charset=utf-8
SOAPAction: "http://tempuri.org/IChemistryService/UpdateMaterialTaskState"

<?xml version="1.0" encoding="utf-8"?>
<soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/">
  <soap:Body>
    <UpdateMaterialTaskState xmlns="http://tempuri.org/">
      <taskId>687321</taskId>
      <testState>1</testState>
    </UpdateMaterialTaskState>
  </soap:Body>
</soap:Envelope>
```

**返回值（SOAP 方法的字符串结果，内容为 JSON）：**

```json
{ "Msg": "OK" }
```

只有 `Msg`（大小写不敏感）等于 `OK` 才确认成功。SOAP Fault、非 2xx、空结果、非 JSON 或 `Msg` 非 `OK` 均记录为失败并进入重试。

### 4.7 SOAP 部署配置与联调检查

1. 复制 `config/interfaces.server.json.example` 为 `config/interfaces.server.json`，使用 `INTEGRATIONS_PROFILE=server` 启动。`interfaces.server.json` 被忽略，不提交真实环境地址或凭据。
2. 1.4、1.5、1.6 共用 `report_delivery.soap_endpoint`、`target_namespace`、`soap_version`、`timeout_ms`，各自的方法名、参数名和 SOAPAction 独立配置：

```json
{
  "report_delivery": {
    "soap_endpoint": "http://172.18.0.97:8003/Lab/Chemistry",
    "target_namespace": "http://tempuri.org/",
    "soap_version": "1.1",
    "method": "AcceptReportFromDiGui",
    "soap_action": "http://tempuri.org/IChemistryService/AcceptReportFromDiGui",
    "cancel_method": "CancelFlowFromDiGui",
    "cancel_soap_action": "http://tempuri.org/IChemistryService/CancelFlowFromDiGui",
    "task_state_method": "UpdateMaterialTaskState",
    "task_state_param_id_name": "taskId",
    "task_state_param_state_name": "testState",
    "task_state_soap_action": "http://tempuri.org/IChemistryService/UpdateMaterialTaskState"
  }
}
```

3. 部署前执行数据库迁移 `pnpm migrate`，创建 `external_task_state_deliveries` 通知与重试表；未迁移时 1.6 无法持久化发送。
4. 启动日志必须显示 `业务系统 SOAP(1.4/1.5/1.6)=SOAP(实际地址)`。server 模式缺少 SOAP 地址时拒绝启动；即使在 demo 模式，缺少地址的送审/撤回也返回明确失败，不再把模拟结果记为外部成功。1.6 演示通知仍保留为 `pending`，配置真实地址后由后台发送。
5. 必须从**实际部署服务器**确认能访问业务系统端口，并向对方取得/打开 WSDL；浏览器或开发电脑能访问不代表服务器能访问。若 WSDL 声明的 endpoint、namespace、SOAP 版本或 action 与上述示例不同，应按 WSDL 修改配置后重启。
6. 联调时分别用测试 `sysNumber`、`jobNo`、`taskId` 验证 1.5 和 1.6，并由业务系统人员确认状态确实落库；仅看到 HTTP 200 不足以确认业务成功，还须核对 SOAP Result 中的 JSON。

> 当前仓库只能验证报文构造、回包解析、状态机和重试逻辑。是否能从生产服务器连通 `172.18.0.97:8003`，必须在部署网络中测试，不能由本地代码静态检查保证。

### 4.8 实现边界与验收（2026-09-16）

- 1.1/1.2/1.3 均有入站路由；1.4/1.5/1.6 均有 SOAP 客户端。1.6 已接入单条/批次审核和持久化重试，发送前再次确认任务未取消记录均已审核，避免退回后把旧队列通知发成“完工”。
- 1.6 当前自动流程只通知完工 `1`；`0` 的报文发送能力存在，但外部退回后的“未完工”自动通知闭环尚未接入，不能声称完整支持双向状态循环。已成功通知任务再次完工的循环幂等策略需双方明确后扩展。
- 网络超时存在“对方已处理但本地没收到”的可能；撤回保留原状态并允许重试，对方需保证幂等。本次不自动改写历史状态。
- 自测：`node --import tsx scripts/test-external-interface-regressions.cjs`（模拟 HTTP/SOAP）；`node --import tsx scripts/test-external-modify-regressions.cjs`（模拟数据库及外部撤回）；不调用真实业务系统。
- 部署验收：用双方约定的测试报告验证“送审→对方可见→撤回→双方草稿→再次送审→外部 report_edit / 审核不通过→本系统退回修改”；再验证 `data_entry` 数据锁与重新审核，以及多方法最后一条审核通过才发完工通知。
- 对方必须逐条检查 1.3 `results[].ok`，不能只看 HTTP 200。发生问题时提供请求时间、SysNumber、脱敏请求/回执和本系统日志进行关联排查。
- 原始 DOCX 没有本系统部署 IP，无需改写客户原件；部署地址、SOAP 细节及上述实现边界以本 Markdown 补充为准。

---

## 五、登录认证接口（REST）

### 5.1 `commonLogin/login` — 用户登录

- **调用方 → 提供方**：报告系统（服务端）→ 原认证系统
- **协议 / 方法**：HTTPS `GET {BaseURL}/commonLogin/login`
- **请求格式**：`application/x-www-form-urlencoded`（参数走 query）
- **响应格式**：JSON

**请求参数（query，均必填）：**

| 参数名 | 类型 | 说明 |
|---|---|---|
| `loginName` | String | 登录名（工号 / 账号） |
| `pwd` | String | 密码 |
| `appId` | String | 应用标识（由原认证系统为接入方分配） |

**响应（`ResponseModel«登录返回信息»`）：**

| 参数名 | 类型 | 说明 |
|---|---|---|
| `code` | String | `0010`=成功；`002X`=参数异常；`003X`=token 异常；`004X`=访问控制异常；`005X`=系统内部异常 |
| `success` | Boolean | 是否成功 |
| `fail` | Boolean | 是否失败 |
| `message` | String | 响应消息说明 |
| `ticks` | String | 服务器响应标识 |
| `response` | Object | 登录返回信息（见下） |
| `response.jobNo` | String | 工号 |
| `response.userName` | String | 姓名 |
| `response.departName` | String | 部门 |
| `response.token` | String | jwt Token |
| `response.loginToken` | String | .net Token |
| `response.modifyPwdTips` | String | 非空表示密码超期未更新，需提示用户修改密码 |
| `response.id` | String | 用户标识（无效，请勿使用） |

**响应示例：**
```json
{
  "code": "0010",
  "fail": false,
  "success": true,
  "message": "",
  "ticks": "638...",
  "response": {
    "departName": "化学检测中心",
    "id": "",
    "jobNo": "GDJL02859",
    "loginToken": "<.net token>",
    "modifyPwdTips": "",
    "token": "<jwt token>",
    "userName": "王树雪"
  }
}
```

**HTTP 状态码：** `200` OK / `401` Unauthorized / `403` Forbidden / `404` Not Found。
