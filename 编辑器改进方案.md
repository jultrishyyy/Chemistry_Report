# 统一编辑器改进方案（Unified Editor Design）

> **本文件定位**：化学部检测报告系统"模板/报告编辑器"的整体重构设计，供评审后分期实现。
>
> 与 `待实现内容.md` / `OPTIMIZATION.md` 一样，这是 README 第六节「不要再开 markdown 文档」约定的**特批例外**——
> 编辑器是跨三个场景的核心交互层，方案较大、需独立评审，单列一份。系统"现状"仍写 README；
> 本文件只记"编辑器要做成什么样、怎么从现状演进过去"。`OPTIMIZATION.md` 的 P-Editor 一节指向本文件。
>
> **状态**：🟡 设计待评审 → 评审通过后按第 8 节路线分期实现。

---

## 0. 一句话方案

**一棵结构化文档树**（把"数据/语义"与"样式/版式"分层）＋ **一个编辑器组件、三种模式**（用能力开关区分，而非三套代码）＋ **层叠样式模型**（文档→区块→字段逐级 override，像 CSS，渲染成 Typst set-rules）＋ **直接操作手柄**（拖列宽/拖 logo/调间距）。数据录入＝"填 + 模板授权范围内的有限结构微调"；报告＝"可组合的部件 + 映射 + 页眉页脚/logo"。Typst 始终是底层出片引擎，不替换。

---

## 1. 现状与三个使用场景

编辑器底层是同一个组件（现 `client/src/components/FieldEditor`，靠 `editorMode` 区分），在三处复用：

| 场景 | 入口 | 现状 | 核心诉求 |
|---|---|---|---|
| **编辑原始记录模板** | `pages/RecordTemplate/Editor.tsx`，`editorMode='record'` | 结构化拖字段 + 属性面板 + 右侧 PDF 预览 | 快速建各类字段、配公式、**Word 式排版（字体/加粗/行距/字段间距/页边距/居中），且不同部分可不同**、换行、表宽拖拽、图片表样式 |
| **编辑报告模板** | `pages/ReportTemplate/{CoverEditor,ProjectEditor}.tsx`，`editorMode='report-cover'/'report-project'` | 同上 + 取值映射（8 种来源）+ 刚加的页眉页脚开关 | 上述全部 + **报告封面（放 logo）** + 页眉页脚样式/应用范围 + 映射（订单/样品/关联记录/测试项目名等） |
| **生成报告时编辑报告** | `pages/Report/{Workbench,InstanceEditor}.tsx`，实例文档 `content_doc` | 结构化改值/增删字段/增删结果表行、重渲染、不回写 `record_data` | **拉出报告全部字段**、删字段、自由排版（表宽/换行/字体/logo）、改值加字段、**组合报告由哪些部件构成、选样品/项目** |

**当前实现的关键事实（改进的地基）**：
- 模型：`FieldGroup[] / FieldDefinition`（`shared/types.ts`）；样式只有文档级 `layout_options.theme_config`（字体/字号/边距等少量键）。
- 渲染：`shared/typst-generator.ts`（`generateTypst` / `injectReportFieldsIntoTypst` / `renderContentDoc` / `resolveBinding`）→ Typst → `record-theme/0.1.0/lib.typ` → 后端 `typst-compiler` 出 PDF。
- 录入与出报告**共用同一套公式与渲染口径**（合规要求逐位一致）。
- 报告实例＝生成时冻结的 `content_doc` 快照，编辑只改它、永不回写 `record_data`。
- 已有的"局部能力但没接 UID/UI"：`result_table.columns[].width`（有字段、没拖拽）、`image_table_style`（有、UI 弱）、矩阵的 `allow_add_remove_samples` 等录入开关。

**痛点本质**：① 结构化模型上"直接操作/样式覆盖"的 UI 没建够；② 预览是"死 PDF"无法在上面操作；③ 样式只有文档级、不能按区块/字段分别设；④ 报告部件化/封面/logo/页眉页脚版式/映射来源还不完整。

---

## 2. 核心设计思想（四条）

1. **一棵结构化文档树，三层分离**：每个节点（部件 / 区块 / 字段 / 单元格）拆成
   - **数据·语义**：类型、编码、必填、选项、**公式**、**取值绑定**——它"是什么、值从哪来"。
   - **样式·版式**：字体/字重/字号/对齐/行距/段距/页边距/列宽/颜色/表样式/图片表样式——它"长什么样"，**可在任意层级 override**。
   - **录入策略**（仅原始记录模板）：录入时允许改什么（加减样品行、改行名、图片张数…）。
2. **一个编辑器、三种模式**：`UnifiedEditor({ mode, doc, dataCtx? })`，`mode ∈ {record-template, report-template, report-instance}`。三者**共享一套核心**，差异用**能力开关**表达，不写三套编辑器（见第 4 节能力矩阵）。
3. **样式＝层叠模型 → Typst set-rules**：文档默认 → 部件 → 区块 → 字段 → 单元格，逐级覆盖（像 CSS / 像 Typst 的 `set`/`show` 作用域）。这天然满足"**不同部分设置不同**"。
4. **直接操作 + 属性面板双轨**：能拖的就拖（列宽、图片大小、间距、logo 位置），拖动写回结构化属性；同时保留属性/格式面板填精确值。普通工程师用拖拽和预设，不碰 Typst。

---

## 3. 统一文档模型（数据结构）

在现有 `FieldGroup / FieldDefinition` 上**增量加可选字段，向后兼容**（存量模板无这些字段 → 走默认，渲染行为不变）。

### 3.1 节点通用形状

```ts
// 样式覆盖块：所有键可选，缺省＝继承上层
interface StyleOverride {
  font?: string; size?: string; weight?: 'regular' | 'bold';
  italic?: boolean; color?: string;
  align?: 'left' | 'center' | 'right' | 'justify';
  line_height?: string;          // 行距
  tracking?: string;             // ✅ 字距（字与字间距）→ #set text(tracking:…)，封面"检 验 报 告"那种拉开
  space_before?: string; space_after?: string;  // ✅ 段/块间距 → #v(段前/段后)（已渲染）
  vertical_align?: 'top' | 'center' | 'bottom';  // ✅ 区块级整页垂直分布 → 页流层 #v(1fr)（封面标题居中/机构钉底）
  margin?: { top?: string; bottom?: string; left?: string; right?: string }; // 仅部件/页级（渲染待后续）
  width?: string;                // 字段/列宽（fr 或 cm，渲染待后续）
}

// 在 FieldGroup 上加：
interface FieldGroup {
  /* …现有… */
  style?: StyleOverride;         // 区块级样式覆盖
  layout?: 'vertical' | 'inline' | 'grid' | 'two-col';  // 已部分有
  break_before?: boolean;        // 已有 page_break_before，统一语义
}

// 在 FieldDefinition 上加：
interface FieldDefinition {
  /* …现有（type/code/binding/formula/matrix/…）… */
  style?: StyleOverride;         // 字段级样式覆盖
  new_line_before?: boolean;     // 布局换行：强制本字段另起一行
}

// result_table 列、矩阵列、单元格同理可挂 style?/width?
```

### 3.2 层叠顺序（render 时合并）

```
文档默认(layout_options.theme_config)  →  部件 style  →  区块 style  →  字段 style  →  单元格 style
                                  （后者覆盖前者，未设的键继承）
```

### 3.3 报告部件模型（report 模式新增）

报告从"首页模板 + 项目模板"扩展为**可组合部件**：

```ts
type ReportPartKind = 'cover' | 'front' | 'project';  // 封面 / 首页(结论汇总) / 项目报告
interface ReportPart {
  id: string; kind: ReportPartKind; title?: string;
  groups: FieldGroup[];          // 复用同一结构
  style?: StyleOverride;
  header_footer?: HeaderFooterConfig;  // 可按部件覆盖（见 5.3）
  enabled?: boolean;             // 生成时可勾选要不要
  logos?: LogoElement[];         // 封面/页面上的 logo（见 5.3）
}
```

> 兼容：现有"cover 模板 / project 模板"映射为 `kind='front'`（首页）与 `kind='project'`；新增独立 `kind='cover'` 封面。

---

## 4. 一个编辑器，三种模式（能力矩阵）

`UnifiedEditor` 共享核心：**大纲/树导航 · 中间画布(可直接操作) · 右侧"数据/格式/策略"三面板 · 顶部插入字段面板 · 直接操作手柄**。差异＝下表开关：

| 能力 | record-template | report-template | report-instance |
|---|:--:|:--:|:--:|
| 插入字段（文本/数值/日期/选择/审核/日期/图片/数据表格…） | ✅ | ✅ | ➖(增字段) |
| 公式配置 | ✅ | ✅(报告级公式) | ➖ |
| **录入策略面板**（加减样品/图片张数…授权） | ✅ | ❌ | ❌ |
| 取值映射/绑定（8+ 来源） | ❌ | ✅ | ➖(改值=literal) |
| **格式面板**（字体/加粗/行距/段距/边距/居中，层叠 override） | ✅ | ✅ | ✅ |
| 换行（文本内换行 + 布局换行） | ✅ | ✅ | ✅ |
| 表格列宽拖拽 | ✅ | ✅ | ✅ |
| 图片表样式 | ✅ | ✅ | ✅ |
| 报告部件（封面/首页/项目）组合 | ❌ | ✅(设计) | ✅(勾选启用) |
| 页眉页脚（样式/应用范围） | ❌ | ✅ | ✅(微调) |
| logo 上传 + 定位 | ❌ | ✅ | ✅ |
| 数据绑定的**真实值预览** | mock | mock | ✅(快照) |
| 删除字段 / 自由增字段 | 设计期 | 设计期 | ✅ |
| 选样品 / 选项目 / 选部件出报告 | ❌ | ❌ | ✅ |

➖＝有限/变体；✅＝完整；❌＝不开放。

---

## 5. 各场景细化

### 5.1 原始记录模板 & 报告模板（结构 + 公式 + Word 式样式）

- **快速建各类字段**：保留并强化字段面板 + **分区预设**（`section-presets.ts` 已有 7 种）。常用字段一键插入；矩阵/审核/日期/图片各有专用属性面板（现 `FieldPropsPanel` + `MatrixEditor`）。
- **公式**：沿用 `FormulaEditor`（12 预设）+ 矩阵列/单元格/汇总公式 + 自定义表达式沙箱。**不动公式引擎**，只在新结构里照常挂。
- **Word 式样式（核心新增）**：右侧加**「格式」面板**——选中区块/字段后，可设字体/字重/字号/对齐(含居中)/行距/段距/页边距(部件级)/颜色；面板顶部有「应用到：本字段 / 本区块 / 整篇」三档，写进对应层级的 `style`。层叠合并后渲染。→ 满足"**不同部分设置不同**"。
- **换行**：① 文本内换行——文本域已能敲回车，**渲染端把 `\n`→`linebreak()`、空行→`parbreak()`**（小改 `__cell`/主题 `#field`，见 6.2），立刻得到 Typora 手感；② 布局换行——字段 `new_line_before` 强制另起一行。富文本（加粗/列表/小标题）作为**可选的「富文本」字段类型**，仅用于 literal 内容（见 10 取舍）。
- **表宽拖拽**：结果表/矩阵列边做成**可拖把手**（HTML 表格原生），写回 `column.width`（fr/cm），渲染拼进 `#table(columns: …)`。
  ✅ 2026-06 补全：**数据矩阵**画布也有列宽拖拽（参数列 + 试样列），并新增**行高拖拽**（行头下缘拖"最小行高"，零宽 box 实现——行至少这么高、内容多仍自动撑开），双击复位。
- **图片表样式**：`image_table_style`（边框/内边距）已有数据，补一个直观面板（边框粗细/内边距/每行张数/图大小滑块）。

### 5.2 数据录入开放多大权限？（回答你的问题）

**原则：录入＝"填数据 + 模板授权范围内的有限结构微调"，绝不＝"改表单设计"。** 编辑权限**按字段、由模板作者预先授权**（不是给录入端一个全局大开关）。一切录入仍走**审核 + 版本锁定 + 审计**。

| 录入时**可以**（模板授权时） | 录入时**不可以**（永远） |
|---|---|
| 数据矩阵**加/减样品行**（`allow_add_remove_samples`） | 改字段**编码 code**、增删字段、改字段类型 |
| 改**样品行名/参数列显示名**（`allow_edit_*_labels_at_entry`，不改 code） | 改**公式**、改固定单位、改阈值/判定规则 |
| **表头备注可选项**（✅ 2026-06，统一表头模型）：四类表头（参数列/样品行/汇总行/汇总列）的括号备注可固定（如单位）或从模板配置的选项中选（如「判定要求 (客户要求/标准要求)」），所选值存 `DataMatrixValue.*_overrides` 并印进 PDF | 自行发明模板没授权的备注（无 `*_allow_custom` 时） |
| 图片**上传多张/删张**（`allow_multiple`） | 改版式/样式/页眉页脚/映射 |
| 选填可选字段、填"其他(自定义)"选项 | 把计算值/签字值手改进 `raw_data` |
| Excel 导入批量填充 | 跳过审核直接生效 |

- **落地**：把现有零散开关收敛成字段级 `entry_policy`（`{ add_remove_rows?, edit_labels?, image_count?: 'fixed'|'multi', optional? }`），录入端 `FormRenderer` 据此放开/锁定对应交互。语义字段（主检/审核/日期）永远只读、自动注入。
- **为什么这样**：样品数、图片数这类**确实因每次实验而异**，应放开；但放开"改结构/公式/编码"会破坏可复现、污染 `raw_data`、绕过审核——这些必须锁死。**自由度给在"数据多少"，不给在"表单长什么样"。**

### 5.3 报告模板：部件 + 页眉页脚 + logo + 映射

- **部件化（封面/首页/项目）**：报告 = 选若干 `ReportPart` 组合。现有"首页模板"= `front`、"项目模板"= `project`；**新增独立「封面」`cover`**——专门放标题、logo、机构信息的整页。
- **logo / 图片自由定位**：`LogoElement { asset_url, anchor: 'top-left'|…, dx, dy, width, height }`，上传走现有 `routes/images.ts`，渲染用 Typst `place(anchor, dx, dy, image(...))`。封面给一块**拖拽画布**：拖 logo 改 `dx/dy`、拽角改大小（直接操作）。放页眉里则是页眉 grid 的一格（简单情形）。
- **页眉页脚（在已实现基础上扩展）**：现已有"标准/无 + 页码开关"（`CoverEditor` + 主题 `_render-header/_render-footer`）。扩展为：
  - **槽位**：页眉/页脚各分 左/中/右，把字段（公司名/报告编号/检验码/页码/logo）拖进槽；
  - **样式**：字体/字重/字号/居中/分隔线（挂 `StyleOverride`）；
  - **应用范围**：全部页 / 仅首页 / 除首页 / 指定页（主题已能用 `counter(page)` 区分首页与其余，扩成可配）。
- **映射来源扩展**：现 8 种来源（`CellBinding`）已含 `order(order_no/customer_name/sample_name/received_at)`、`record_*`、`record_meta`、`system`。补齐你提到的：**关联原始记录信息、测试项目名称、样品在单内 id** 等——给 `order`/新增 `order_sample`/`test_item` 来源类型；保存时用已实现的 **binding 完整性校验**（P-Tpl-1）防止指空。

### 5.4 生成报告时编辑（report-instance）

这是**权限最大**的模式（改的是 `content_doc` 快照，不碰 `record_data`），但仍在同一结构/样式模型上：

- **拉出报告全部字段**：实例编辑器把 `content_doc` 的 cover+各 project 的所有 groups/fields 平铺到大纲，每个字段显示**解析后的真实值**（绑定已 resolve），可逐个：改值（= 把 binding 改成 `literal`）、删字段、增字段、增删结果表行。
- **自由排版**：表宽拖拽、换行、字体/加粗/对齐、logo 调整、图片大小——全部走第 3 节的 `style` 层叠（实例级 override）。每次改写 `content_doc` 重渲染，**偏离原始数据处标红**（P3 已有基线对比）。
- **组合报告由哪些部件构成**：勾选 封面/首页/各项目报告 要不要；项目报告里勾选**要哪些样品 / 哪些项目**。
- **出报告方式前置**：**"按样品 / 按项目 / 整单 / 自由"应在进入实例编辑之前就选好**（现 `Workbench` 已有四模式 + 批次）——先定拆分粒度 → 生成各份实例 → 再进编辑器精修。实例内的"选样品/项目"是对**本份** scope 的微调，不是重新拆分。
- **红线**：实例编辑**永不回写 `record_data`**（现状已是）；改值只在快照层、全程留痕。

---

## 6. 渲染映射到 Typst（保持两端一致）

- **样式层叠 → set/show 作用域**：文档级 → 主题 `set text/par/page`；部件/区块级 → 包一层 `{ set text(...); set par(...); …内容… }`（Typst set 在块作用域内生效）；字段/单元格级 → 行内 `text(font:…, weight:…)[…]` 覆盖。
- **换行**：值渲染处把 `\n`→`linebreak()`、`\n\n`→`parbreak()`（改 `__cell` 与主题 `#field`）。
- **列宽**：`#table(columns: (w1, w2, …))` 用各列 `width`（缺省 `1fr`）。
- **logo**：`place(anchor, dx: …, dy: …, image(path, width: …))`。
- **页眉页脚**：`set page(header: …, footer: …)`（已实现），扩展槽位/样式/应用范围。
- **一致性铁律**：录入端预览、出报告、实例重渲染**共用同一渲染函数与样式合并逻辑**（现已是原则）——样式层加进来后，三端合并顺序必须同一份代码，避免"编辑器看着对、出片不一样"。

---

## 7. 北极星：SVG 预览 + 叠加直接操作（可选，最后做）

PDF 预览（iframe）改 **Typst `--format svg` 内联渲染** + 用 Typst `query`/metadata 拿元素包围盒坐标 → 前端叠 HTML 拖拽手柄，**在真实预览上**拖列边/拽 logo/拉图片大小，写回结构化属性、防抖重编译。一处投入、通解所有"在成品上直接调"的需求。工程量最大、放路线末尾；P0/P1 先用 HTML 原生交互验证模式。

> ✅ **首切已实现（2026-06）——"定位"半边落地，"操作"半边未做**：
> - 预览从 iframe 换 **pdfjs 画布渲染**（`PdfPreview.tsx`）：双缓冲换页、**重编译后滚动位置保持原位**（解决"改一个字段 PDF 回滚到顶部"）；
> - **编辑器 ⇄ PDF 双向跳转**（Overleaf SyncTeX 式）：`generateTypst` 在分区+字段块前注入零尺寸 metadata 标记 `<__fepos__>`（实测零排版影响），服务端 `POST /api/typst/query` 用 `typst query` 取回每个标记的 page+y(pt)——点左侧字段/大纲 → PDF 平滑滚到对应位置并高亮页框；**Ctrl/⌘+点击 PDF** → 选中点击点上方最近的字段并打开属性面板。三个模板编辑器（record/cover/project）均已接线。
> - 仍属北极星未做：SVG 内联 + 包围盒 + 叠加拖拽手柄（在预览上直接拖列宽/间距）。

---

## 8. 分期路线（每期可独立交付）

> 地基优先：先做**渲染/模型**的小而稳的能力，UI 再叠上去。每期都按 modify-system 五阶段走（改完同步 README + 自审）。

| 期 | 目标 | 主要做法 | 影响文件 | 工作量 | 风险 |
|---|---|---|---|---|---|
| ✅ **P0 快速赢（已实现 2026-06）** | 文本换行 + 列宽拖拽 + 图片表样式 UI | `\n`→linebreak/parbreak（主题 `multiline` + 生成器 `__cell`）；HTML 列边拖把手写 `column.width`（`colWidthSpec`→`#table(columns)`）；图片表样式 UI **本就已存在**（`FieldPropsPanel` image 段：阶段/多图/尺寸/边框/内边距） | `typst-generator.ts`、`record-theme/lib.typ`、`MatrixEditor/ReportResultTableCanvas.tsx`、`FieldPropsPanel.tsx`(已有) | 小 | 低 |
| 🟡 **P1 样式层叠（首切已实现 2026-06）** | 节点级 `style` override + 「格式」面板 | ✅ `StyleOverride` 类型 + `FieldGroup.style`/`FieldDefinition.style`；`styleSetRules`/`applyBlockStyle` 渲染（区块 set 规则级联 + 字段 #field 行内）；`FormatPanel` 接区块头 Popover + 字段属性面板（字体/字号/加粗/斜体/对齐/行距/颜色/**段前段后间距**）。段距已渲染（`#v()`，封面"调位置"靠它）。✅ 2026-06 补：`margin`（`#pad`，left 用于横向缩进定位）+ 字距 `tracking` + 垂直分布 `vertical_align`（见 P1c）。**待补**：`width`（字段宽）渲染、内联/矩阵/结果表单元格的字段级样式 | `shared/types.ts`、`typst-generator.ts`、新 `FieldEditor/FormatPanel.tsx`、`FieldEditor/index.tsx`、`FieldPropsPanel.tsx` | 中 | 中 |
| ✅ **P1c 封面排版三件套（已实现 2026-06，回应"封面标题在整页正中/分区间留白"）** | 整页垂直分布 + 字距 + 可插入「间隔」块 | **① 整页垂直居中/钉底**：`StyleOverride.vertical_align`（top/center/bottom），渲染在**页流层**注入 `#v(1fr)`（不能埋进 `#block`，块内 1fr 不撑开）——只对顶级分区有效；区块头「格式」面板新增「垂直」三档（仅区块级显示，`FormatPanel block` 开关）。**② 字距**：`StyleOverride.tracking` → `#set text(tracking:…)`，格式面板新增「字距」旋钮（封面"检 验 报 告"那种拉开，替代塞全角空格）。**③ 间隔块**：新增 `FieldType='spacer'`（`spacer_height`，固定 cm/pt/em）→ 渲染 `#v(高度)`，作为「字段类别·间隔（空白）」可插入，对应 Word 敲回车留白；纯版式无数据（不进 `#let data`/raw_data/mock）。顺手修掉 types.ts"间距渲染待 P1b"的过期注释（其实早已渲染）。**实测**：generateTypst → typst 编译出片，封面标题整页居中 + 字距 + 间隔均正确（PNG 验证） | `shared/types.ts`、`typst-generator.ts`、`matrix-flatten.ts`、`mock-data.ts`、`FieldEditor/{FormatPanel,field-types,FieldPropsPanel,index}.tsx`、`FormRenderer/index.tsx` | 中 | 低 |
| ✅ **P2 录入策略（已实现 2026-06，决策＝开放权限）** | 录入端默认放开"加减样品行 / 多图"，模板可显式锁定 | `FormRenderer` 样品增删改为 `!== false`（默认开放）；图片多图本就 `!== false`；矩阵编辑器「增删行」开关显示默认开放、可锁定（固定样品数） | `FormRenderer/index.tsx`、`MatrixEditor/index.tsx` | 小 | 低 |
| 🟡 **P3 报告部件化（进行中）** | 独立封面 + parts 组合（决策：Word 式、不拖拽、logo 不做） | **P3.1 后端✅**：`ReportTemplateKind+='cover_page'` + `ReportContentDoc.front_cover?` + `renderContentDoc` 前置封面页（首页 #show 统管页眉页脚）+ `buildReportTypst`/`generate*` 接 `cover_page_template_id`，Typst 编译验证。**P3.2 前端✅**：报告模板列表「新建封面」+ 封面分组（复用 CoverEditor）+ 工作台可选封面选择器（传 `cover_page_template_id`）+ `InstanceEditor` 整份深拷贝自动保留 `front_cover`，端到端实测 preview 200/PDF。**仍待**：实例期直接编辑封面段 + front_cover 留痕 diff + 封面不出页眉的 first-page 细化 | `types.ts`、`typst-generator.ts`、`reports.ts`、`ReportTemplate/*`、`Report/{Workbench,InstanceEditor}.tsx` | 大 | 中 |
| 🟡 **P4 生成端编辑增强（大部已实现）** | 拉出全字段/删增字段/选部件·样品·项目 + 报告内排版 | **已有**：实例编辑器平铺全字段、改值（→literal）、增删字段、增删结果表行（P2）；部件组合＝封面可选（P3）+ 项目启用/停用 + 四种拆分（按样品/项目/整单/自由）；**P4 新增✅**：实例编辑器接入 `FormatPanel`——区块级 + 字段级**格式**（字体/字号/加粗/对齐/行距/间距），改动落 content_doc.style、重渲染。**仍待**：实例期结果表列宽拖拽（模板期已有）、`CellBinding` 来源扩展、样式改动纳入留痕 diff | `Report/InstanceEditor.tsx`（接 FormatPanel）| 中 | 低 |
| **P5 北极星（可选）** | SVG 预览 + 叠加手柄 | `typst-compiler` 加 svg；`TypstViewer` 渲染 + 坐标叠加层 | `typst-compiler.ts`、`TypstViewer/*`、生成器 metadata | 大 | 中高 |

**建议**：先 **P0 → P1**（地基 + 立竿见影），再按业务优先级取 P2/P3/P4，P5 视效果决定。

---

## 9. 数据模型与兼容性

- **全部新字段可选**：存量模板无 `style`/`entry_policy`/`parts`/`logos` → 走默认，**渲染与现有完全一致**（不破坏存量）。
- **走版本流**：样式/结构/部件改动经 PUT → 草稿 → 审核 → 生效（与现有约定一致，受控）。
- **录入数据按版本锁定回放不变**：样式是模板/报告侧的事，历史 `record_data` 仍按其锁定版本呈现。
- **实例编辑只落 `content_doc`**：报告侧所有自由排版改动不回写 `record_data`。

---

## 10. 非目标 / 取舍（明确不做）

- ❌ **自由文本框 / 绝对定位任意数据内容**：破坏数据绑定与可复现。自由度给"样式覆盖 + 有限结构"，不给"随便摆数据"。
- ❌ **替换 Typst 出片引擎**：它承载数据绑定/版本锁定/可复现，是合规地基。
- ❌ **富文本满功能**：富文本只给 **literal 内容**（备注/结论/声明），安全子集（换行/加粗/斜体/列表/小标题）→ Typst；不给数据绑定单元格。
- 取舍原则：**对非排版专业用户，"预设 + 旋钮 + 拖把手 + 吸附 + 一键回预设"比一张白纸更好用也更稳**；`PreviewEditor`（直接改 Typst）保留为极少数高手的逃生口。

---

## 11. 待决策清单（评审时拍板）

> 决策进展（2026-06）：

1. **先做哪几期**？→ ✅ 已做 **P0**（换行/列宽/图片表样式）+ **P1**（样式层叠 + 格式面板，含段距）；接下来 **P2 + P3**。
2. **富文本范围**：⏳ 未定——目前"换行+分段"已够（P0）；加粗/列表/小标题等富文本字段类型暂缓。
3. **logo / 封面定位**：✅ **logo 不需要我们做**（CMA/CNAS/GRGTEST/印章等由外部/页眉处理）。封面只需调各文字块的**字体/字号/粗细/对齐/位置**，**采用 Word 式（结构化流 + 选块调格式 + 段距/对齐），不做自由拖拽定位**——封面是居中竖排版面，"位置"＝纵向间距 + 对齐。✅ **已落地（P1c，2026-06）**：标题"在整页正中"＝区块级 `vertical_align: center`（页流层 `#v(1fr)`）；分区/字段间留白＝「间隔」spacer 块或段前段后间距；字距拉开＝`tracking`。封面排版诉求至此闭环。
4. **封面独立部件 `cover`**：✅ **是**，作为区别于"首页"的独立部件。
5. **P5（SVG 叠加）**：⏳ 未定，暂不纳入；保持"属性面板 + 拖把手 + PDF 预览确认"。
6. **录入策略默认值**：✅ **开放权限**——默认放开"加减样品行 / 多图上传"（P2 据此实现）。

---

## 12. 报告精修"软编辑"（让生成端编辑没那么死板）

报告实例编辑器（`InstanceEditor`）"太死板"的改进，分快赢 / 大招两档：

- ✅ **快赢·首切（已实现 2026-06）**：「插入内容」下拉（段落 / 小标题 / 说明字段）+ 字段上移/下移重排；段落/小标题用 `hide_label` 渲染成纯文本块（无「标签：」），配合 P1 字段级样式（小标题=加粗大字）+ P0.1 换行。文员可较自由组排正文。文件：`shared/typst-generator.ts`（`generateFieldBlock` 的 hide_label 分支）、`Report/InstanceEditor.tsx`。
- ✅ **快赢·内联富文本（已实现 2026-06）**：字段 `rich: true` → 值按 Markdown 子集（`**粗**`/`*斜*`/`-`·`1.` 列表/换行/空行分段）渲染。`shared/typst-generator.ts` 的 `richTextToTypst()` 先转义再套白名单标记（**无 eval、注入中和**，已用 `#table()`/`$`/`[]`/`@` 验证）；走锚点 `// __RICH__` 在两条渲染路径替换。`InstanceEditor` 的「段落」即富文本，配 B/I/• 工具条。Typst 编译 + 在线 preview-content-doc 200 验证。
- ✅ **快赢·浮动格式条（已实现 2026-06）**：`InstanceEditor` 左栏顶部 sticky 工具条，点中某字段后直接 B/I/字号±/左中右对齐/清除（写 `field.style`，复用 P1 渲染）；省去逐字段开 Popover。聚焦用行级 `onFocusCapture` 追踪。
- ⏳ **大招**：预览内就地编辑（edit-in-place，content_doc 渲染成可编辑 HTML、点字段就地改、绑定/手改/偏离用色区分），与 P5 SVG 叠加同北极星。**取舍**：不做"自由 Word"——检测报告是受控输出，每改必留痕、偏离必标红、值的来源（数据/手改）须清楚、最终由 Typst 出片。

---

## 13. 编辑器 UI/UX 整洁化（✅ 已实现 2026-06）

> 与第 1–12 节的"能力"维度互补：这一节解决的是**操作效率与视觉整洁**——
> 用户反馈"编辑非常难操作、字段编辑界面简陋"。纯 client 侧，不动 `shared/types.ts` 与渲染层，PDF 输出零变化。

三个包一次落地（A 视觉 / B 交互 / C 属性面板），全部在 `FieldEditor/`（record / report-cover / report-project 三模式共享生效）：

- **A. 视觉整洁化**：
  - 删除七彩 `GROUP_PALETTE`（橙/蓝/绿/粉/紫/黄/青便利贴墙）→ 统一白底灰边卡 + `#f8fafc` 头部；
  - 字段行重构：类别图标芯片 + 无边框标签输入为主体，彩色 Tag 收敛为一条灰色 meta 文本（`类别 · 多选 · 单位 · 必填`），操作按钮（公式/↑/↓/删）**hover 才浮现**；计算字段保留紫色公式 Tag（可点开公式编辑）；
  - 分组头收敛：名称 + 字段数 + 状态徽标（标题隐藏/前分页），「隐藏标题/分页/排版/格式」变 icon-only 工具条，上移/下移/删除 hover 浮现；
  - 「标题与副标题」「文档样式」卡同步中性化（去紫/黄底色）；视觉类名统一收在 `client/src/index.css` 的 `.fe-*` 段。
- **B. 操作效率**：
  - **真拖拽排序**（react-dnd，已有依赖首次启用）：分组重排 + 字段重排（**支持跨分区拖**），落点指示线（上/下半判定）；分组尾部/空分组是投放区；旧 `HolderOutlined` 假把手成真。↑↓ 按钮保留为无障碍后备；
  - **大纲导航**：左侧可折叠树（分组+字段），点击平滑滚动定位 + 选中（`data-fe-node` 锚点），折叠态持久化 `localStorage.feOutlineOpen`；
  - **点字段行整行即选中并打开属性面板**，选中行高亮；选中/公式目标改用 `field.id` 定位（替代 `{groupIdx, fieldIdx}`），拖拽/增删后不指错；顺带修了 `genId` 从 f100 盲数会撞已加载模板 id 的隐患（现扫描现有 id 防撞）。
- **C. 字段属性面板**（`FieldPropsPanel.tsx`）：
  - 一长条 570 行滚动表单 → **Tabs：基础 / 类型配置 / 格式 / 取值绑定**（无类型配置的类别与非报告模式自动隐藏对应页；切字段/切类别回到「基础」）；
  - 字段类别从纯下拉 → **带图标的卡片网格**，且按 editorMode 过滤（修了 record 模式下拉里能看到报告类别的隐患）；
  - 选项列表：**拖拽排序 + 「批量粘贴」**（一行一个、整体替换）；
  - 面板开着（mask=false）点其他字段行**直接切换内容**；删除了死代码 `ResultTableEditor`（早被 `ReportResultTableCanvas` 取代）。

**验证**（2026-06-11）：Playwright headless 实测原始记录模板编辑器（id=9）——大纲 19 项/4 分组/15 字段行渲染、点行开面板、选择字段出「类型配置」页含批量粘贴，console 零错误、零失败请求；`/api/typst/compile` 200（headless 截图右侧空白系 chromium-headless-shell 无 PDF 插件，真实浏览器正常）。`vite build` 通过；`tsc` 净增错误 0（存量 14 个 `tsc -b` 错误见 OPTIMIZATION.md）。

**追加（2026-06-19）：编辑器顶栏标题 + 单行工具栏**。① 顶栏标题只显示模板名，去掉" — 首页编辑器/项目编辑器"后缀（`CoverEditor`/`ProjectEditor`；记录编辑器本就只显模板名）；标题 `flex-shrink + ellipsis`，过长截断不挤按钮。② 顶栏工具栏原 `flexWrap:'wrap'` 会在内容变化（点击后版本面板/标签增减）时折成两行、把「保存为草稿」挤到第二行——改 `flexWrap:'nowrap' + overflowX:auto`，并把顶栏按钮（返回/试录测试/受控登记/保存/回到当前版本）统一 `size="small"`、`gap 12→8`，无论怎样都保持一行（极窄时横向滚动而非换行）。三个编辑器页一致。纯页面外壳层，不动 `FieldEditor`/渲染。

**未做（后续可选）**：FieldRow 拖拽改 hover 实时换位动画（现为落点指示线 + drop 生效）；大纲树拖拽重排；属性面板内嵌为第三栏（现仍是左弹 Drawer）。

---

## 14. 字段属性面板「按字段构成重组」（✅ 14.2+14.4 已实现 2026-06，回应"太复杂太凌乱"）

> **实现状态（2026-06）**：14.2（数据模型 `label_style`/`value_style`）+ 14.4（面板对象化重组 + 迷你预览）**已落地**；其余 14.5 的 ③继承可视化 / ④三模式深度统一 暂缓。
> - `shared/types.ts`：`FieldDefinition` 加 `label_style?`/`value_style?:StyleOverride`，`label_bold` 标 `@deprecated`（label_style 未设时回退读它，存量零改）。
> - 渲染：主题 `#field` 加 `label_args`/`value_args` 两 dict（splat 进 `text()`，缺省空 ⇒ 不包，向后兼容）；`typst-generator` 新 `fieldPartTextDict()`，`generateFieldBlockInner` 的 #field 路径与 hide_label 路径分别传 label/value 样式；label 一侧 weight 仍走 `label_bold` 通道复用 faux-bold。
> - UI：`FormatPanel` 加 `variant: text/layout/full`（默认 full 不破坏既有调用）；`FieldPropsPanel` 对**简单值类型**（text/number/date/choice）按字段物理构成分**顶层 Tab**：**基础**（类别/必填/单位/行数/说明）｜**字段名**（显示开关+文字+label_style）｜**字段值**（手填/抓取来源+默认值/绑定+value_style）｜**版式**（field.style 对齐/段前后 + 行距间距去分区/文档级的指针）｜choice 另加**类型配置**。复杂类型（矩阵/图片/报告自动表）保留「基础+类型配置+格式+取值绑定」原结构。
> - 交互定稿（2026-06）：迷你预览/Segmented 试过后**按用户要求撤掉**——"纯 Tab + 不要迷你预览"（右侧 PDF 实时预览已能看到改动效果，迷你预览多余）。最终＝顶层 Tab 分区，回到与原版一致的"上方分区"手感，只是分区轴从技术维度（基础/格式/绑定）换成字段构成（字段名/字段值/版式）。
> - 验证（2026-06）：tsc 净增错误 0；generator→typst 编译→PDF/PNG 实测，检测要求标签蓝+粗+大、结论值粗体+整行居中、单位名称保持默认，旧 `#field`/`label_bold` 调用向后兼容编译通过。
> - **行距/间距归属**（14.3）维持原判：字段级面板不放，引导去「分区头·格式」「文档样式」（全篇一致）。



> 用户反馈：报告模板编辑器的**单字段配置**显得复杂凌乱。根因不是缺能力，而是 **UI 按"技术维度"切 Tab（基础 / 类型配置 / 格式 / 取值绑定），而非按用户心智里的"字段物理构成"切**。用户的自然模型是「一个字段 = 字段名(标签) + 字段值，各自独立配」，现有面板把这两者的设置打散在 4 个 Tab 里。本节把 P1/P13 已有能力**重新归类**，并补一处真缺口（字段名只能加粗）。

### 14.1 现状对照（能力多已具备，问题在归类）

| 用户诉求 | 现状 | 现在在哪 | 缺口 |
|---|---|---|---|
| 字段名**是否显示** | ✅ `hide_label` | 基础 Tab「显示字段名 / 字段呈现方式」 | — |
| 字段名**加粗** | 🟡 仅加粗 `label_bold:boolean` | **格式 Tab** | ❌ 不能单独设字号/斜体/颜色/字体 |
| 字段值**手填 or 抓取** | ✅ 呈现方式预设 + `binding` | 基础 Tab +「取值绑定」Tab | 同一件事拆两个 Tab |
| 字段值**默认显示值** | ✅ `default_value` / binding | 基础 Tab | — |
| 字段值**加粗/字号/斜体** | 🟡 `field.style` 罩**整个字段块**(名+值) | 格式 Tab | ❌ 无"只调值、不动名"的独立样式 |

**三个真问题**：① 同一字段的设置散在 4 Tab、来回跳；② 字段名只能加粗（`label_bold` 是布尔）；③ `field.style` 语义含糊——同时罩名和值，没有"名一套、值一套"。

### 14.2 数据模型（增量、向后兼容）

```ts
// FieldDefinition 上：
label_style?: StyleOverride;   // 字段名：字体/字号/加粗/斜体/颜色（取代 label_bold）
value_style?: StyleOverride;   // 字段值：上述 + 对齐
// 迁移：旧 label_bold → label_style.weight；旧 style(混用) → value_style（保留 style 读取做兼容回退）
```

渲染端 `#field(label, value, …)` 已分别接 label 与 value 两参（见 `typst-generator.ts` `generateFieldBlockInner` + 主题 `#field` 的 `label_bold` 入参），扩成各吃一套 `StyleOverride` 行内规则即可，**不动公式/绑定/数据流**。存量模板无新键 → 走默认、出片零变化（符合第 9 节）。

### 14.3 行距 / 间距归属（回答"应该在哪配"）

原则：**按"段内 vs 段间"分层，不塞进单字段**——

- **行距 (line spacing)** = 文字自身属性，应全篇统一 → **「文档样式」设默认 +「分区格式」可覆盖**（如密集数据表那段收紧）；**单字段不放**。
- **字段间距 / 段距 (两字段之间的留白)** = 布局/容器属性，不属于任一字段 → **主力在「分区(区块)格式」**（`block_spacing`，`#set block(spacing)` 级联整分区）；**单字段只留「段前/段后」做个别例外**（`space_before/after`）。

这套分层**代码里已实现**（`block_spacing` 区块级、`space_before/after` 字段级、`line_height` 在 FormatPanel）——当前痛点纯是**可发现性**：它们混在字段格式 Tab、与文档级无视觉区分。**对策：上提到「分区头」「文档样式」并做视觉分层；字段面板里只留一行指针「想统一调行距/间距 → 去分区/文档级」。**

**✅ 命名统一 + 层叠可见/可收回（2026-06，回应"文档与分组重复设行距/字段间距会冲突、相互不生效"）**：根因＝**命名碰撞**——文档样式把 `line_gap`（其实是*字段间距*，theme `#field` 的默认 gap）叫「行距（字段之间）」，分组把 `line_height`（*行间距*，`#set par(leading)`）也叫「行距」；两个互不相干的旋钮同名 → 用户两边都调觉得"打架"。而分组 `block_spacing` 一旦设了就 emit 作用域限于该组的 `#set block(spacing)`，**屏蔽**文档默认，且界面无任何"正在覆盖/如何收回"的提示 → "分组设过后文档调了不生效"。改三处（纯前端、不动渲染/类型）：
> ① **统一词汇**：`DocumentStylePanel` 把「行距（字段之间）」→**「字段间距」**(`line_gap`，全局默认)、「段间距（分组之间）」→**「分组间距」**(`paragraph_gap`)；`FormatPanel` 分组里「行距」→**「行间距」**(`line_height`，并 tooltip 说明≠字段间距)。文档与分组现在**共用「字段间距」**一词（文档=默认、分组=覆盖该组），层叠关系可预期。
> ② **文档级"应用到全部分组"（收回覆盖）**：文档「字段间距」旁，当有分组单独设过 `block_spacing` 时出现「应用到全部分组」入口，一键清掉各组 `block_spacing` 覆盖（复用 `setLabelWeight()` 的"统一动作=清下级覆盖"语义，见 §15 line 398）。解决"分组设过后文档收不回"。
> ③ **分组级可见继承 + 一键恢复**：分组「字段间距」placeholder 显示当前继承的文档值（如"继承 0.6em"，经 `FormatPanel` 新增 `inheritedFieldGap` prop，由 `FieldEditor/index.tsx` 从 `theme_config.line_gap` 透传）；「行间距」「字段间距」被本级覆盖时各加「↩恢复继承」按钮，只清该一项（区别于底部「清除本级样式」的全清）。
> 文件：`DocumentStylePanel.tsx`、`FormatPanel.tsx`、`FieldEditor/index.tsx`。不新增 theme_config 键、不改 `typst-generator`/`record-theme`（`line_gap` 本就是 `#field` 默认 gap、`block_spacing` 经 `gap:` 覆盖，见 §16 line 425）。

### 14.4 重构后的面板布局（4 Tab → 1 面板 3 段）

```
┌ 字段类别：[文本▾]   必填 ⃝            ← 顶部，整字段级
│ ┌ 实时预览：单位名称： 奇瑞汽车股份…  ┐ ← 按当前样式渲染的迷你预览
│ └────────────────────────────────────┘
├▸ 字段名（标签）   显示 ◉开 ○关 · 文字[…] · 样式 字体/字号/加粗/斜体/颜色  (label_style)
├▸ 字段值          来源 ◉手填 ○抓取(绑定…) · 默认值/绑定来源 · 样式 …/对齐  (value_style)
└▸ 这个字段在版面上（可折叠，给"个别例外"）  段前/段后 · 换行
   ── 想统一调行距/字段间距？→「分区格式」「文档样式」↗
```

把"呈现方式(基础)"+"取值绑定(格式旁)"合进**字段值·来源**一处；行距/间距移出，只留指针。

### 14.5 配套 UX 提升（按性价比排序）

1. **字段迷你实时预览**（最高收益）：面板内即时显示「字段名：值」按当前样式渲染，无需来回看右侧整页 PDF。
2. **字段名补齐完整样式**（`label_style`）：直接满足"字段名也能设字号/斜体/颜色"。
3. **继承可视化**：字号/字体 placeholder 现仅"继承"，改为淡显继承到的**具体值**（「继承自分区：宋体 11pt」），用户才知在覆盖什么。
4. **三模式统一"值来源"概念**：现仅报告模式有手填/抓取；统一成同一"字段值·来源"区（记录模式来源固定=录入），三编辑器心智一致。
5. **分区/文档级「格式」更显眼**：行距、字段间距、页边距收进「文档样式」「分区格式」并视觉分层，配合字段面板指针，解决"全局排版在哪调"。

### 14.6 落地与风险

- 属 P1（样式层叠）/ P13（UI 整洁化）的延伸；纯 client + 渲染端 label/value 分样式，**不动公式/绑定/数据流/raw_data**。
- 风险**中**：主要在 `label_bold`/`style` → `label_style`/`value_style` 的**数据迁移与渲染等价**（须保证存量模板出片逐位不变，按一致性铁律走同一份合并逻辑）。
- 影响文件（预估）：`shared/types.ts`、`shared/typst-generator.ts`（`#field` 行内样式拆名/值）、`record-theme/lib.typ`（`#field` 接 value 样式）、`FieldEditor/{FieldPropsPanel,FormatPanel}.tsx`（面板重组 + 迷你预览）。
- **状态**：🟡 设计待评审 → 通过后按第 8 节并入 P1/P13 延伸，走 modify-system 五阶段实现，回本节标「✅ 已实现」。

---

## 15. 分区排版方式「收窄 + 分流」（✅ 已实现 2026-06，回应"排版方式漏洞多、难统一"）

> 用户反馈：分区头的「字段排版方式」(竖排/横排/表格/两列/网格 五档) 是个**漏抽象**——对数据矩阵/图片/签名分区根本没用（layout 被渲染端忽略，UI 却照显），表格的表头还写死"项目|值"和字段对不上。

**根因**：layout 把两件事混为一谈——① 分区**装什么内容**（自由字段 / 矩阵 / 图片 / 签名，由内容决定）② 自由字段**怎么排**（单列 / 多列 / 表格）。一个开关套在所有分区类型上，自然漏洞多。

**对策＝不强行统一，而是按分区性质分流（已实现）**：
1. **排版下拉只对"纯字段分区"显示**：`sectionLayoutInfo()` 判定——含 data_matrix / images 角色 / 签名字段且无自由字段的分区，**不显示下拉**，改一行灰字「📊 排版自动」。漏洞①消失。
2. **纯字段分区只留三档**：竖排 / **多列**（配「列数 2-4」`grid_columns`）/ **表格**。存量 `inline`(横排)/`two-col`(两列)/`grid`(网格) 渲染路径保留、向后兼容，UI 归一显示为"多列"（`displayLayout()`）。五档冗余收成三档。
3. **表格表头可配**：`table_header='none'`(无表头，缺省) / `'custom'`(自定义两列标题 `columns`)。`generateTable` 改为直出 `#table`（不再走主题写死的"项目|值"），且表格单元格也吃 label_style/value_style/换行。漏洞②消失。

**数据模型**（全可选、存量零改）：`FieldGroup.grid_columns?`、`table_header?: 'none'|'custom'`、复用 `columns?`（自定义表头标题）。
**影响文件**：`shared/types.ts`、`shared/typst-generator.ts`(`generateGrid`/`generateTable`)、`FieldEditor/index.tsx`(LAYOUTS 收窄 + `sectionLayoutInfo`/`displayLayout`/`TableHeaderConfig`)。
**验证**（2026-06）：tsc client 0 / server 无新错；三档（竖排/多列3列/表格无表头/表格自定义表头）generator→typst→PNG 实测全部正确，表格不再硬塞"项目|值"。
**多列两处修复（2026-06）**：① **列对齐**——`generateGrid` 原用整数 `columns: N`（Typst 当 auto 宽度→各列起点参差），改 `(1fr × N)` 等宽列，列与列起点对齐。② **spacer 不再把多列切碎**——含 spacer 的竖排分区（如首页"基本信息"，字段间穿插「空行」）切到多列时，原 chunking 在每个 spacer 处断开，导致被 spacer 单独隔开的字段（检测要求/结果/结论）各成单字段块、看似仍竖排；改为**多列布局忽略 spacer**（让字段连续流入列，列间距用 row-gutter），data_matrix 仍独占整行。③ **多列间距可调**——多列的 `row-gutter` 原写死文档行距、分区「格式·字段间距」(`block_spacing`) 对它无效（block_spacing 走 `#set block(spacing)`，对 grid 的 gutter 无作用）；改为 `block_spacing` 直接驱动 grid 的 `row-gutter`，竖排/多列共用同一个间距旋钮（Word 式：间距是内容属性，单列/多列一致可调）。三者均 generator→typst→PNG 实测。
**多列对齐 break-out（2026-06）**：多列分区里字段本被关在 `1fr` 列内，设中/右对齐只在该列(半页)生效——用户反馈"对齐只在左半部分"。改为 `renderMultiCol`：普通字段流入等宽列，**中/右对齐的字段从网格里"拎出来"成整行独立块 `#align()`、跨整页对齐**（字段级优先、否则继承分区级 `group.style.align`，故"分区设居中"＝该区字段都整页居中；左对齐＝留在列内）。不用 `grid.cell(colspan)`（落在行中会报错），改"闭合网格→独立块→再开网格"。PNG 实测：两列分区里 检测结论 整页居中、签发日期 整页靠右、其余字段仍在两列。
**两处对齐/加粗 bug（2026-06）**：① **取消加粗失效**——FormatPanel 的加粗原是 2 态 Switch（bold⟷继承），当文档默认就是加粗（首页 `label_weight:"bold"`）时关掉只是"继承"仍加粗、无法强制取消。改 **3 态 Radio：跟随/加粗/正常**，`正常` 走 `#field(label_bold:false)` / `#set text(weight:"regular")` 真正取消加粗。② **hide_title 分区对齐只在左半**——`renderGroupEntry` 给隐藏标题分区包的 `#block[...]` 缺省收缩到内容宽度，内部 `#align(right/center)` 无可对齐空间（首页"基本信息"标题隐藏，故右对齐推不到页边）。改 `#block(width: 100%)[...]` 撑满整页，左/中/右对齐恢复跨整页。两者均 PNG 实测（默认加粗标签 vs 强制正常标签对比、隐藏标题分区左/中/右对齐到位）。
**文档级"字段标签"统一应用不到全部字段（2026-06）**：文档样式的「字段标签」加粗设置被**字段级 `label_bold` 覆盖**——首页模板的字段在复刻时带了 `label_bold:false`，所以文档设加粗时这些标签不变（"应用不到全部"）。改 `DocumentStylePanel.setLabelWeight()` 为**三态 默认/加粗/正常**：加粗/正常＝强制统一，遍历清掉所有字段的 `label_bold`/`label_style.weight` 再设 `theme_config.label_weight`，让设置真正落到每个字段；默认＝主题默认 + 保留字段级。实测：含 `label_bold:false` 的字段，清后渲染从 `#field(…, label_bold:false)`（不加粗）变 `#field(…)`（跟随文档加粗）。**同类排查**：文档级 字体/字号/行距 也会被字段级 `style.font/size/line_height` 覆盖，但那些是**有意的逐字段定制**（如首页小字备注 6pt），不应被文档统一动作清掉——故只对"字段标签加粗"做清覆盖（它是无意被复刻脚本批量写死的）。`label_width`/页边距/标题倍率等文档键无字段级对应，不存在该问题。
**未做（后续可选）**：表格"字段当列"形态（每字段一列、一行值，如 长|宽|高）——等无表头/自定义表头用一阵看是否真有需要。

---

## 16. 项目编辑器对齐首页编辑器 + 图/表版式（✅ 已实现 2026-06，回应"项目/首页差别大、图表不可调"）

> 用户反馈四点：① 项目模板不该有标题/副标题及「显示自动标题副标题」开关（画蛇添足）；② 图/表标签无法控制是否显示；③ 添加分区/字段时混入了原始记录的类型；④ 项目编辑无法像首页一样逐字段调版式，且所有编辑器里图/表的标签/间距/版式都不可调。
> **根因**：首页与项目编辑器本就是同一个 `FieldEditor`（仅 `editorMode` 不同），差异全在于**复杂报告类型（图/表）从未获得普通字段的版式能力**——所以围绕表格的项目编辑器显得"死板"。

**① 去掉项目的标题开关**：`DocumentStylePanel` 接 `editorMode`，`report-project` 隐藏「只渲染字段（自动标题/副标题）」整行（项目段本就被 `renderContentDoc` 剥大标题）。`TitleStylePanel` 早已 record-only。
**①补（2026-06，回应"项目模板还是会自己显示标题"）**：彻底移除"项目名作为章节大标题"的自动注入——`renderContentDoc` 删掉每项目段前的 `#heading(level:1)[项目名]`，`ProjectEditor.tsx` 预览删掉对应 `injectTitle`。现项目报告"有什么分区/字段才显示什么"，要项目标题就加分区标题/文本字段；项目间仍靠 `#pagebreak()` 分隔。PNG 实测：项目预览顶部不再有居中大标题，只剩定义的分区与字段。

**② 图/表标签显隐**：三种报告自动表（result/equipment/image_gallery）此前**根本不渲染 field.label**，故截图里没有显隐开关。现新增「版式」Tab 带显隐开关 + 标题样式；渲染端 `wrapFigure` 按 `hide_label` 决定是否出加粗标题。matrix/image 原有的 hide_label 开关旁补了 `label_style`。
**修正（2026-06，回应"不要自己显示标题"）**：报告自动表标题**缺省＝不显示**（`wrapFigure` 仅 `hide_label === false` 时出）——因报告表通常已在带标题的分区里（如"检测结果"分区含"检测结果"结果表字段），自动出标题会与分区标题重复。开关语义改 `checked={hide_label===false}`/`v?false:undefined`。matrix/image 仍缺省显示（存量零变）。

**「显示为标题」开关移到「基础」Tab（2026-06，回应"是否显示标题按钮放到基础的标签部分"）**：三种报告自动表（result/equipment/image_gallery）的标题显隐开关从「版式」Tab 移到「基础」Tab、紧贴「标签（显示名称）」下方——因标题文字就是该标签，"填名字 + 要不要显示成标题"放一起更顺；「版式」Tab 只保留标题**样式**（字体/字号/加粗）+ 标题与图表距离，并在标题关闭时显示指向「基础」的提示。`hide_label` 数据语义不变，三种表一致。文件：`FieldPropsPanel.tsx`（basicTab 加开关、figureLayoutTab 去开关留样式）。
**追加（回应"标签和图表的距离无法修改"）**：`FieldDefinition.label_gap`(pt) → wrapFigure 与 matrix 的标题块 `below`（缺省 0.4em / 0.55em）；UI「标题与图/表的距离」在版式 Tab（报告表）/类型配置（matrix）。PNG 实测：matrix 标题↔表距离 24pt 明显拉开；报告表缺省无标题、开后 below 生效（2pt 贴住 / 40pt 拉开）。
**再追加（回应"项目模板编辑器图表标签和图表距离仍无法修改"）**：根因＝报告表用「检测结果表」分区预设时，表上方显示的"检测结果"其实是**分区标题**（`#section`），而 `#section` 里标题↔内容是**写死的 `#v(0.4em)`**，没有入口。新增 `FieldGroup.title_gap`(pt)：主题 `#section(title_gap:…)` 取代写死值（子分区走标题块 `below`），生成器透传，UI 在区块头「格式」Popover 左栏「标题与内容间距」。两个间距分清：分区标题↔内容＝`title_gap`，图/表字段自身标题↔表＝`label_gap`。PNG 实测：分区标题↔表 4pt 贴住 / 40pt 拉开。
**三追加（回应"备注离图表的距离也无法调整"）**：`captionTypst` 原写死 `above: 0.35em`。新增 `FieldDefinition.caption_gap`(pt) → `captionTypst(caption, caption_gap, side)` 把图/表一侧间距（备注在下方→`above`、在上方→`below`）改为可调；五个调用点（matrix/wrapFigure/loose+compact image）全部透传；UI 在基础段备注框下方「备注与图/表的距离」（填了备注才显示）。PNG 实测：备注↔表 2pt 贴住 / 30pt 拉开；缺省 0.35em 出片不变。**控件归位（用户要求）**：报告表的「备注与图/表的距离」移到「版式」Tab「备注（题注）位置」下方；matrix/image 留基础段。
**四追加（回应"图表版式加更多编辑：字体/表头加粗/内容加粗/备注加粗字号"，参考原始记录数据表格）**：新增 `FieldDefinition.table_style={font,font_size,header_bold,body_bold}`（仅 report_result_table/equipment_table）+ `caption_style`(StyleOverride，备注文字)。渲染：`tableStyleWrap()` 把整表包 `#text(font,size)`，`tableCellBold()` 逐格按 header_bold(缺省真)/body_bold(缺省假) 加粗（faux-bold 感知）；静态 + 动态(`renderMatrixResultTableTypst` 加 `ts` 参数)两路径都吃；`captionTypst` 加 `style` 参数套备注样式。UI 在「版式」Tab 加「整表文字（字体/字号/表头·内容加粗）」+「备注样式」。缺省＝表头加粗(faux-bold 感知)/内容常规/备注 9pt——⚠️ 表头从旧 `[*label*]` markup 改为 `#text(weight:"bold")`，对**真粗体字体（宋体等）逐像素一致**，但对**无粗体字体（仿宋/楷体，项目模板默认仿宋）表头现在会真正加粗**（旧 `*` markup 在仿宋不显示粗，属修正）。PNG 实测：楷体 12pt 整表 + 表头常规 + 内容 faux-bold + 备注粗 11pt 均正确。

**③ 类型分流（什么编辑器显示什么）**：`FieldCategoryEntry.mode?:'report'` → **`editors: EditorMode[]` 白名单**（`categoriesForEditor()`）；`SectionPreset.mode` → `editors`，并修掉 `'report'===‘report-cover’` 永不相等导致签字栏在首页消失的 bug。映射：**原始记录**=文本/数字/日期/选择/图片/数据表格/测试设备/间隔｜**首页**=文本/数字/日期/选择/图片/间隔/结论汇总表｜**项目**=文本/数字/日期/选择/间隔/检测结果表/设备表/图片表。新增首页预设（结论汇总/样品照片）+ 项目预设（检测结果表/设备表/图片记录/说明段落）。

**④ 图/表版式可调（统一 `wrapFigure`）**：报告自动表此前直接 `return 表 + captionTypst()`、**不经 `applyBlockStyle`**，故 `field.style` 不渲染。改为 `return wrapFigure(field, body)`——标题(label_style,sticky) → 题注(按 `caption_position` 上/下) → `applyBlockStyle`(对齐 + 段前/段后)。`FieldPropsPanel` 给三类报告表加「版式」Tab（标题显隐+样式 / 题注位置 / 对齐 / 段前后）。新增类型字段 `caption_position?:'above'|'below'`。所有图/表标题（matrix/image/三报告表）统一改用 `titleTextArgs(label_style,'1em')`，缺省加粗、存量零变。

**影响文件**：`shared/types.ts`（+`caption_position`）、`shared/typst-generator.ts`（+`wrapFigure`，3 个 `render*Typst` 收口，matrix/image 标题吃 label_style）、`FieldEditor/{field-types,section-presets,index,DocumentStylePanel,FieldPropsPanel}.tsx`。
**验证（2026-06）**：client `tsc --noEmit` 0 错；server 仅存量 `TS6059 rootDir` 噪声；generator→typst→PDF/PNG 实测——项目结果表标题红/14pt/加粗、整表右对齐、题注置于表上方均正确，`hide_label` 关闭后标题块消失。
**追加（2026-06）：项目结果表行高拖拽**。列宽拖拽（`column.width` fr + 双击复位）模板期本就有；补齐**行高拖拽**——`result_table.rows[].height`(cm)，画布行头下缘拖把手 + 双击复位（`ReportResultTableCanvas` 的 `rowDrag`），渲染端 `renderReportResultTableTypst` 在本行第一个出格单元格内放零宽 `#box(width:0pt,height:…)`（与数据矩阵 `sample_row_heights` 同口径，行至少这么高、内容多仍撑开）。PNG 实测：设 1.5cm 的行明显变高、未设的行自动。
**bug 修复（2026-06）：分区「字段间距」只动图/表、不动普通字段**。根因＝主题 `#field` 把字段 block 写死 `above/below=line_gap`，**覆盖**了分区 `#set block(spacing: block_spacing)`，所以只有无显式 above/below 的图/表块响应、普通字段不动。修法＝`#field` 加 `gap:` 参数（优先于 `line_gap`），生成器 `generateGroupContent` 把 `block_spacing` 作为 `fieldGap` 透传给竖排 `generateVertical→generateFieldBlock→#field(…, gap:)`。现竖排字段/多列(row-gutter)/图表(set block) 三类间距由同一个 `block_spacing` 旋钮统一控制。PNG 实测：3 字段 + 矩阵同区，block_spacing 0→30pt 时字段与矩阵间距一起拉开；未设时 0 个 gap 参数、出片与改动前逐像素一致（向后兼容）。

**bug 修复（2026-06）：文档级「字段间距」对图/表不生效（尤其图↔图）**。上一条修了【分区级】block_spacing 对图表生效，但【文档级】「字段间距」(`theme_config.line_gap`) 仍只喂 `#field` 的 gap（文字字段）+ grid row-gutter，**不 emit `#set block(spacing)`** → 裸 `#block`/`#table` 的图/表走 Typst 默认块距、调文档字段间距纹丝不动；文字↔图表因前一个 `#field` 的 below=line_gap 还能动，但**图↔图（如项目报告里矩阵紧挨结果表）完全不动**。`typst query <__fepos__>` 量证：line_gap 0.6em→2em 时文字↔文字、文字↔矩阵 13.2→27.2pt 响应，**矩阵↔结果表恒 58.8pt**。修法＝`generateGroupContent` 末尾给每分区补 `#set block(spacing: <分区 block_spacing ?? 文档 line_gap>)`（分区级已设时由 `applyBlockStyle` 负责、此处只补"未设"档），让图/表与文字统一吃文档级/分区级字段间距。复测：矩阵↔结果表 52.8→66.8pt（间距分量随 line_gap 同步），文字侧不变。

**bug 修复（2026-06）：调了分区「字段间距」后，图/表的标题/备注离图的距离调不动了**（上一条"图表响应字段间距"的副作用）。`wrapFigure` 把标题(`#block(below:label_gap)`)、图、备注(`captionTypst` 的 above/below=caption_gap) 直接放在分区内，分区级 `#set block(spacing:字段间距)` 成了图/标题/备注块的默认 above/below，Typst 相邻块间距取较大值 → 字段间距(大)盖住 `label_gap`/`caption_gap`(小)，调小无效（实测 label_gap 2pt→40pt 仅变 20pt、被钳在字段间距 20pt 上）。修法＝`wrapFigure` 把"标题+图+备注"包进 `#block[#set block(spacing:0pt) …]` 隔离内部间距，使两段距离完全由 `label_gap`/`caption_gap` 决定；外层块仍继承分区「字段间距」与相邻分区等距。实测修后 label_gap/caption_gap 2pt→40pt 均变 38pt（完全可调），且表↔表分区间距仍随字段间距(Δ24pt)。影响文件：`shared/typst-generator.ts`（`wrapFigure`）。

**bug 修复（2026-06）：无标题分区之间（图↔表各独占一个分区时）的间距不随「字段间距」变**。项目报告里「检测方法/结果表/图片/设备」各是一个**无标题分区**（`renderGroupEntry` 的 `hide_title` 分支 → 裸 `#block(width:100%)`）。`sectionGap`（`#set block(spacing:)`）只管分区**内部**块间距，分区**之间**（即两个 `#block(width:100%)` 之间）走 Typst 默认块距 → 表现为"分区内的文字字段间距大(2em)、但表格↔图片(各独占分区)的间距小"（用户报"前三个字段间距大、表格备注和图片间距小"）。`typst query` 实测：text↔text 27pt、分区↔分区(图/表) 仅 0–6pt。修法＝给 `hide_title` 的 `#block` 加 `spacing: <分区 block_spacing ?? 文档 line_gap>`，使相邻无标题分区按字段间距等距（实测修后分区间距→32pt，与字段一致；PNG 复核：结果表/图片/设备表之间间距与上方文字字段一致）。**有标题分区(`#section`)之间仍走 `v(paragraph_gap)`＝「分组间距」**（有可见标题、是另一档，不动）。影响文件：`shared/typst-generator.ts`（`renderGroupEntry`）。

**bug 修复（2026-06）：图片记录分区里多张图片之间的间距不随「字段间距」变**。文字、矩阵、报告自动表的字段间距都已响应文档/分区「字段间距」（靠 `generateGroupContent` 末尾的 `sectionGap` = `#set block(spacing: 分区block_spacing ?? 文档line_gap)`），但 `section_role='images'` 分区走 `generateImageGroupContent` **早返回**、绕过了 `sectionGap` → 多张图片之间走 Typst 默认块距、调文档「字段间距」纹丝不动（文字却变 → 用户报"文字 2em、图却不是"）。修法＝把 `sectionGap` 提到 `generateGroupContent` 顶部、并**前置到 images 早返回**（`return sectionGap + generateImageGroupContent(group)`）。分区「字段间距」已设时仍由 `applyBlockStyle` 负责、仅作用本分区（覆盖文档值）。`typst query` 实测：图↔图间距 line_gap 0.6→3.0 Δ=0→24pt（修复后响应）；分区 block_spacing=40pt 覆盖后图间距 +34pt（本组生效）。影响文件：`shared/typst-generator.ts`。

**bug 修复（2026-06）：项目「图片记录」独占分区不渲染**。`proj_images` 预设把 `report_image_gallery` 放进 `section_role='images'` 分区，而 `generateGroupContent` 对 `section_role==='images'` 一律走 `generateImageGroupContent`（只 `filter(f.type==='image')` —— 原始记录图片字段），对 gallery 返回空串 → 空 `#section("图片记录")[]` → 整分区不出（`typst query` 实证 section body 为空）。修法＝该路由加 `&& group.fields.some(f=>f.type==='image')`，gallery 落常规链路出 `__REPORT_IMAGES__` 锚点、由 `injectReportFieldsIntoTypst` 替换为 `renderReportImageGalleryTypst`。复测：注入后 section 非空、编译通过。影响文件：`shared/typst-generator.ts`。

**仍未做（后续可选）**：实例期（`InstanceEditor` 生成后编辑）的结果表列宽/行高拖拽（模板期已全有）；report_conclusion_table 的 wrapFigure 版式（它已有自带 show_title/title_text，暂不并入）。

---

## 17. 三编辑器功能对等 + 去重 + 能力开关化（✅ P1/P2/P4 已实现 2026-06，回应"三编辑器应差不多、可共用底层"）

> 用户问：原始记录/首页/项目三个编辑器功能是否能对等、共用底层。核查结论：**编辑核心（`FieldEditor`）三者本就共用同一组件**，差异只在"页面外壳"层（顶栏/预览上下文）与少量字段层语义分支。据此做了 P1+P2+P4（P3 报告受控登记暂缓）。

**核查发现的真实差距**：① 版本管理 UI（`TemplateVersionPanel`：提交审核/历史/回退/归档/血缘）record 编辑器顶栏内置，cover/project **没有**——只能退回列表页操作（后端却完全支持，列表页就用了 `kind="report"`）；② cover/project 两页各复制 ~150 行加载/保存逻辑；③ `editorMode === 'record'` 等判断散落多处。字段层其余差异（对应项目名/大标题/作为检测结论 vs 取值绑定/页眉页脚/binding 校验）都是"录入模板 vs 报告映射/版面"的**有意语义差异**，不强行统一。

**P1 版本管理对等**：CoverEditor + ProjectEditor 顶栏加 `<TemplateVersionPanel kind="report" editorPathBase=…>`，版本元信息由公共 hook 的 `versionMeta` 提供。现三编辑器版本 UX 一致——编辑首页/项目模板时可直接提交审核/看历史/回退。
**P2 抽共享外壳**：新 `pages/ReportTemplate/useReportTemplateEditor.ts`，cover/project 共享加载/保存/守卫/版本元/PDF 跳转/示例开关；页面只留各自预览与特有 UI（首页页眉页脚、项目关联记录+binding）。删两页 ~300 行重复。record 编辑器差异大（POST 建模板/受控/试录），暂不并入。
**P4 能力开关化**：`field-types.ts` 加 `editorCapabilities(mode)→{projectName,titlePanel,autoTitleToggle,conclusionField,valueBinding,linkedRecordBinding}`，`index.tsx`/`DocumentStylePanel`/`FieldPropsPanel` 全改用 `caps.*`，散落的 `editorMode ===` 判断收敛到一张表。加第四种编辑器只改这里。
**影响文件**：新增 `useReportTemplateEditor.ts`；改 `ReportTemplate/{CoverEditor,ProjectEditor}.tsx`、`FieldEditor/{field-types,index,DocumentStylePanel,FieldPropsPanel}.tsx`。
**验证（2026-06）**：client `tsc --noEmit` 0 错；`vite build` 通过（3243 模块）；散落 `editorMode ===` 仅剩注释。
**仍未做（后续可选）**：P3 报告模板受控登记（需先加后端 `report-templates/:id/controlled` 接口）；record 编辑器并入公共 hook（差异大、收益小，record 已具备全部功能）。

---

## 18. 项目报告表格对齐数据表格的「列头备注 + 分组表头 + 表格版式」（✅ 已实现 2026-06-19，回应"项目报告编辑不够灵活，数据表格表头能编备注、项目模板没有"）

> 用户反馈：原始记录的「数据表格」(data_matrix) 表头编辑很丰富（备注/分组/默认值…），但项目报告的「检测结果表」(report_result_table) 简陋——例如**列头不能编备注**。要求参考原始记录与首页编辑器，把可补的能力补到项目模板编辑里。

**核查结论（第 16 节已对齐"图/表版式"，本节补"表格内部结构/显示"这一层）**：报告表是**展示型**（值来自 binding、**无录入环节**），所以数据矩阵里那些**录入相关**的能力——录入时可选备注 `note_options`/`note_allow_custom`、默认值 `default_value`、列级/单元格公式、Excel 导入——对报告表**没有意义**，不补。但有一批**纯展示/版式类**能力，数据表格有、结果表/设备表却缺，**完全适用**，本节补齐：

| 能力 | 数据表格 | 结果表/设备表（改前） | 改后 |
|---|:--:|:--:|:--:|
| 列头括号备注/单位（如 长度（mm）） | ✅ | ❌ | ✅ `columns[].note`（静态）/`source.columns[].note`（动态，与 unit 合并） |
| 多级分组表头（相邻同组合并超级表头） | ✅ | ❌ | ✅ `columns[].group` / `source.columns[].group` → `groupedHeaderCells()` |
| 跨页续页重复表头 | ✅ | ❌ | ✅ `repeat_header_on_break` → `table.header(repeat:)` |
| 尽量整表同页 | ✅ | ❌ | ✅ `keep_together` → 外层 `#block(breakable:)` |
| 行内留白（行与行疏密） | ✅ | ❌（写死 8pt） | ✅ `cell_inset_y` → `#table(inset:(x:8pt,y:…))` |
| 单元格对齐（左/中/右） | ✅ | ❌（写死居中） | ✅ `cell_align` |
| 空单元格显示符 | ✅ | ❌（写死 —） | ✅ `empty_cell_display`（缺省 —） |

**数据模型（全可选、向后兼容，缺省走旧行为、出片逐位不变）**：`result_table.columns[]` + `source.columns[]` 加 `note?`/`group?`；`equipment_table` 与 `result_table` 都加 `keep_together?/repeat_header_on_break?/cell_inset_y?/cell_align?/empty_cell_display?`。

**渲染（`shared/typst-generator.ts`，三表共用两个新 helper）**：
- `resultTableLayout(opts)` → 统一解析 keep/repeat/inset/align/empty 五项；
- `groupedHeaderCells(dataCols, leading, trailing, groupFmt)` → 无分组单行表头、有分组排两行（`table.cell(colspan)` 超级表头 + 未分组列 `rowspan:2`），与数据矩阵列分组同口径；
- 三个 `render*Typst`（`renderReportResultTableTypst` 静态 / `renderMatrixResultTableTypst` 动态 / `renderReportEquipmentTableTypst` 设备）全部接上：表头走 `table.header(repeat:)`、外层 `#block(breakable:)`、`inset`/`align` 用 layout、空值/默认用 `empty`。

**UI（复用原始记录数据表格的「列配置」卡，不在表头里塞输入框）**：
- `MatrixEditor/HeaderConfigCard.tsx`：加两个开关——`noteFixedOnly`（报告无录入 → 备注只保留"固定文字"、隐藏"录入时可选"档）、`showUnit`（单位独立成一项，留空继承镜像矩阵列；动态结果表用）+ `labelPlaceholder`/`unitPlaceholder`（显示"继承：xxx"）。
- `MatrixEditor/ReportResultTableCanvas.tsx`：静态列头**双击/右键弹「列配置」卡**（标题·备注·分组表头·左右移·删除，与原始记录同款交互），列头只显示标题 + 灰色小字预览（▤分组 /（备注）），不再有内联输入；动态参数列每项一个「配置」按钮开同款卡（含单位）；表级「表格版式」（跨页/行距/对齐/空值）独立成顶部工具栏（两模式共用，因挂在 `result_table` 而非 `source`）。
- `FieldEditor/FieldPropsPanel.tsx`：设备表配置加「表格版式」Form.Item（同五项；设备表是固定列、无列头配置卡需求）。

**验证（2026-06-19）**：client/server `tsc --noEmit` 0 错；smoke（generateTypst→injectReportFieldsIntoTypst→typst compile→PNG）实测——静态表「尺寸」超级表头跨长/宽、列头 `长度（mm）`、左对齐、空值符 `/`、汇总行正常；动态表分组表头 + `宽（mm，客户要求）` + 样品名 + 空值符 `N/A`；设备表空型号显示 `—`、跨页重复表头开关生效。

**仍未做（后续可选）**：实例期（`InstanceEditor` 生成后编辑）的列头备注/分组/版式入口（模板期已全有）；结果表汇总行/汇总列的备注（展示型意义小，暂不补）；图片表（image_gallery）的版式（它走 renderImageGroupTypst、不是 `#table`，另议）。

**设备表「校准有效期」拆为「溯源日期 + 到期日期」，原合并列移除（2026-06）**：设备库 `equipment_library` 本就有 `trace_date`(溯源日期)/`expire_date`(到期日期) 两列，原先 `reports.ts` 建 `equipment_rows` 时合并成 `calibration:"溯源 ~ 到期"` 单列。现：① `equipment_rows` 改为带 `trace_date`/`expire_date`（**去掉 `calibration`**）；② `equipment_table.columns` 类型＝`name|model|asset_code|trace_date|expire_date`（**移除 `'calibration'`**），缺省列 `['name','model','asset_code','trace_date','expire_date']`；③ `renderReportEquipmentTableTypst` 列标签/单元格映射用 `溯源日期/到期日期`，并**过滤未知列**（存量模板里残留的 `calibration` 被丢弃、不出空列）；④ `FieldPropsPanel` 列选项＝两日期列（无 `校准有效期`）；⑤ **建字段默认 + 分区预设**（`field-types.ts` `report_equipment` 两处、`section-presets.ts` `proj_equipment`）改为两日期列；⑥ 编辑器预览 mock（`ProjectEditor`/`TemplateVersionPanel` 的 `equipment_rows`）改 `trace_date`/`expire_date`。无 DB 改动；v2 `report-blocks.ts`（冻结路径）未动。验证：默认列渲染"溯源/到期"两栏分列、编译通过；存量含 `calibration` 的列被过滤掉、不出残留表头。影响文件：`shared/types.ts`、`shared/typst-generator.ts`、`server/routes/reports.ts`、`FieldEditor/{FieldPropsPanel,field-types,section-presets}.tsx`、`ProjectEditor.tsx`、`TemplateVersionPanel.tsx`。

---

## 19. 图片功能全链路改造：统一图位模型 + 无缝整表 + 项目模板可排版 + 录入/文员灵活编辑（✅ 已实现 2026-06-19）

> 回应三点：① 项目模板编辑器图片记录无法做任何排版编辑；② 所有编辑器图片每行被独立表格框住、框间有空隙、空标题也占位；③ 录入/文员端上传照片多于/少于模板时、想调排版时不灵活。

**核心：统一「图位」模型（ImgSlot）+ 统一渲染 `renderImageSlotsTypst`**（`shared/typst-generator.ts`）。一个图位＝标题（可空＝不显示）+ N 张照片 + 布局（loose/compact·ratio、每行张数 cols、尺寸）。原始记录图片组（`renderImageGroupTypst`）与报告图片表（`renderReportImageGalleryTypst`）都把各自字段/图位转成 ImgSlot 后共用同一渲染：
- **无缝整表**（`image_seamless` / `image_gallery.seamless`）：整组渲成**一张连续 `#table`**——loose 行 `table.cell(colspan:N)` 占满、compact 行分列，边框共享、无空隙、`breakable:true` 可跨页；关＝逐行独立 `#block+#table`（向后兼容）。
  - **bug 修复（2026-06）：无缝表里标题与图被页边界拆开**。旧实现把每个图位的标题与图分成两行 table row，`breakable:true` 的表会在这两行之间断页（标题留上页、图到下页）。修法＝每个图位的**标题+图合成一个 `#block(breakable:false)` 放进同一 `table.cell`**（图位内原子不拆），整表仍 `breakable:true`，跨页只发生在图位之间。代价＝图位内标题与图之间不再有 table 行分隔线（无缝模式本就追求无空隙，可接受）。验证：6 个 8cm 高 loose 图位、seamless 渲成 3 页 PDF，每图位均为 breakable:false 块、标题与图同块。`shared/typst-generator.ts` 的 `renderImageSlotsTypst` seamless 分支。
- **空标题不出标题行**：label 空或 hide_label → 该图位只显示图片。
- **多张照片按 `image_cols` 铺成 `#grid`**（修了紧凑模式只渲染第一张的 bug）；照片多/少自动换行、不堆叠不丢弃。

**① 项目模板拥有图片排版 + 绑定（采用"直接编辑+绑定映射"而非继承）**：`image_gallery` 升级为 `{seamless,cols,source_field_codes,items[]}`。手动模式（推荐）＝在项目模板里定义图位、每个图位 `source_field_code` **绑定**一个原始记录 image 字段作为照片来源 + 本模板的标题/布局/张数/尺寸；自动模式＝按 source_field_codes 继承原始记录。与"结果表绑定/检测结论声明"同一心智（项目模板定结构+绑定、原始记录供数据）。UI 在 `FieldPropsPanel`（自动/手动切换、「转手动」从关联记录预填图位、逐图位绑定+排版+增删排序）。

**② 录入端**（`FormRenderer.ImageField`）：缩略图加 **‹/› 排序**（多张时）；数量本就可多可少（allow_multiple）。

**③ 文员端**（`InstanceEditor.ImageGalleryEditor`）：图片表从"只读·自动生成"升级为**可排版 + 可直接编辑照片**——改无缝/每行张数/手动图位（绑定+标题+布局+排序），并**直接增删/上传/排序本份报告的照片**（`PhotoStrip` 经新增的 `GroupEditor.onMutateCtx` 写 `ctx.record_raw_data` 快照；上传走 `/api/images/upload` 取 `server_path`）。全部只落 content_doc、不回写 record_data。

**数据模型**：`FieldDefinition.image_cols`/`image_seamless`；`image_gallery.{seamless,cols,items[{id,source_field_code,label,hide_label,layout,row_ratio,cols,width_cm,height_cm}]}`。全部可选、缺省＝旧行为、出片向后兼容。

**验证（2026-06-19）**：client/server tsc 0 错、vite build 通过；generator→typst(`--root /`)→PNG 实测——原始记录组（独立框 vs 无缝）、多张照片 2 列栅格、空标题图位无标题、项目图片表手动绑定+无缝（检测前满行 + 中/后并排，与参考样张一致）均正确。

**追加（2026-06-19）：文员端实例直接增删/上传/排序照片** ✅——`GroupEditor` 新增 `onMutateCtx`（改本段 ctx 快照），`ImageGalleryEditor` 加 `PhotoStrip`（缩略图 + 上传/删除/‹›排序），写 `ctx.record_raw_data[source_field_code]`，自动模式按来源字段、手动模式按图位绑定的字段；上传走 `/api/images/upload`（返回 server_path 供出片 + url 供预览）；只落 content_doc。client tsc 0 错、vite build 通过。

**追加（2026-06-19）：图位绑定失效纳入 `binding-integrity`** ✅——`RecordFieldIndex` 加 `imageCodes`；`validateReportBindings` 遍历 report_image_gallery：手动 `items[].source_field_code`（须是关联记录 image 字段、未绑定也报）、自动 `source_field_codes`，失效项随 `binding_warnings` 在 ProjectEditor 实时标红 + 服务端 PUT/review/binding-check 兜底（无需改调用方，已统一走 validateReportBindings）。单测 5 例（绑定存在/不存在/未绑定/自动选中不存在/未关联）输出符合预期。

**仍未做（后续可选）**：`image_phase` 自动分组（已弃用，由手动图位取代）。

---

> 评审通过后，我会按第 8 节逐期实现，每期走 modify-system 五阶段（改代码 → 同步 README → 自审 → 端到端验证），并在本文件对应期标注「✅ 已实现」。

---

## 20. 自由表格编辑（受约束的 Excel 式）+ 语义标记（🟡 设计待评审 2026-07-13）

> 回应用户诉求：**所有编辑器的表格编辑更自由，像 Excel 一样——工程师先自定义几行几列，然后自由合并、把表头做成可选的。**
> 本节讲「编辑器交互」侧；**表格的存储模型 / 动态样品映射 / 项目模板继承**的设计放在 `报告映射方案.md 第十节`（两节配套，勿分裂）。

### 20.1 诉求与边界

- **诉求**：自定义行列数、自由合并单元格、表头可选（可从数据来源里选、选后即显示）、自由排版。
- **边界（必须明确、否则会翻车）**：**不能做成「纯 Excel、完全无约束」**。本系统的卖点（报告自动取数、动态样品展开、公式/限值判定、按版本可复现）都依赖「系统知道每一格是什么」。纯自由格子没有语义 → 这些能力全断。
- **可行解**：**受约束的自由 =「自由布局 + 语义标记」**。给足视觉自由，但每张表底层保留一层标记：哪一轴是**样品轴**、哪些是**数据格**、哪些是**表头/说明格**。映射与动态取数靠标记，不靠位置或表头文本。详细论证与存储三层模型见 `报告映射方案.md §10.1–10.4`。

### 20.2 编辑器交互设计

1. **建表**：工程师设定初始行列数，得到一张可编辑网格（复用现有 `MatrixCanvas` / `ReportResultTableCanvas` 画布）。
2. **标记语义**（关键新增）：在画布上把某一行/列**标记为「样品轴」**（对应 `报告映射方案` 的试样带 band，方向 行/列），并区分数据格 / 表头格。这一步是「自由表格」能对接自动映射与动态样品的桥——**是必需项**。
3. **自由合并**：选区合并/拆分（底层 `cells[].colspan/rowspan` 与出片渲染**已支持**，见 20.4 历史）。约束：**样品轴那一维上的合并要受限**——因为该维会按实际样品数展开成 N 份，跨样品的合并无法稳定复制。非动态维度可自由合并。
4. **表头可选**：表头格支持「固定文字 / 从数据来源选（绑定）」两态，复用已有的统一绑定选择器（`报告映射方案 §9.3` 的 `record_header` 来源 + 单一 BindingPicker）。选后表头即显示所选值。
5. **录入端联动**：`FormRenderer`（数据录入）也要按同一模型渲染——支持转置(sample_axis)、合并、自由行列。目前录入网格是固定形态，这块要跟着改（也顺带修 #7「允许改表头却改不动」的转置错位 bug）。

### 20.3 与统一文档模型的关系

本节的「自由布局 + 语义标记」正是本文档 §2/§3「结构化文档树 + 层叠样式」思想在表格上的落地：**语义（身份/绑定/公式）与版式（合并/表头分组/样式）分层**——语义层保证可绑定、可复现，版式层给 Excel 式自由。二者不可混为一层（那会退化成自由画布，和合规/复现打架，见 §10「非目标」）。

### 20.4 历史教训（避免重复踩坑）

- **合并单元格曾做过、又删了**（`报告映射方案 13c-19`）：前端 Shift 框选一直不可用而移除，但**底层 `colspan/rowspan` 类型 + 出片合并渲染保留着**。→ 任务一的「自由合并」应在此底座上**补一个真正可用的选区交互**，不必新造存储。
- **「动态·矩阵驱动」结果表曾做过、整套删了**（`报告映射方案 13c-15`），收敛为「静态网格 + 试样带」。→ 不要以「让表格更自由」为名把全动态复杂度请回来；走**增量扩展**（现有网格上加合并交互 + 版式层 + 样品轴标记），而非推倒重写存储。

### 20.5 分期建议

- **P0**：修 #7 转置改表头 bug + #13 切换样式丢内容（止血，都是现网缺陷）。
- **P1**：复活「合并单元格」可用交互（底层已支持）；表头「固定/绑定」可选（复用统一 BindingPicker）。
- **P2**：样品轴显式标记 + 与项目模板继承（`报告映射方案 §10.5`）打通，让自由表格也能动态样品 + 自动映射。
- **P3（按需）**：若增量撑不住"真正自由表格"，再评估迁到打散存储（`报告映射方案 §10.4-C`，L 级，先小范围验证）。

> 评审通过后按 modify-system 五阶段分期实现，回本节标「✅ 已实现」。
